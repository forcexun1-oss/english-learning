# English Learning — 2026-06-07

Today's logs cover **TTS audio pipeline** work and a return to **proxy traffic** debugging. You asked which scheduled job generates English audio, compared TTS engines in Mac-assistant, and set `ENGLISH_AUDIO_SPEED=0.75`. You also found a quiz bug where every MC answer sat at index 1.

## Vocabulary

- **TTS (text-to-speech)** – converts article text into spoken audio.
- **scheduled task** – a timer that runs a script automatically (your English audio job).
- **bandwidth / traffic** – network data used; "跑了1G的流量" means **about 1 GB of data was consumed**.
- **MC quiz** – multiple-choice quiz; you spotted every correct answer at **index 1**.
- **revert** – undo recent changes ("回退了" → **I reverted it; handle it again.**).

## Grammar for Agent Instructions

| Rough | Better |
|-------|--------|
| *dont generate the audio from original markdown files* | **Don't** generate audio **from** the original markdown files. |
| *check if all MC quiz's answer is index 1* | Check **whether** all MC quiz **answers are** at index 1. |
| *查一下什么东西有跑了一个G的流量* | Check **what is using about 1 GB of bandwidth.** |
| *gpt正在跑什么流量* | **What traffic is GPT generating?** |

When fixing audio or quiz logic with a coding agent, **name the constraint and symptom**: which file format to skip, which env var to set, or which answer index looks wrong. Paste command output (`afplay` errors, `ffplay` info) as evidence. For proxy checks, pair a yes/no question ("Does iQiyi use the proxy?") with a bandwidth audit.

## Today's Writing Prompt

Write 2-3 sentences asking an agent to compare two TTS engines for English shadowing audio, set playback speed to 0.75, and confirm generated files no longer come from raw markdown sources.
