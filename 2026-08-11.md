# English Learning Log — 2026-08-11

- 09:44 [ZH] 啊

- 09:50 [ZH] 现在还能查吗
- 09:52 [ZH] 你要怎么查
- 10:04 [EN] what's the status
- 10:07 [ZH] 哎,别动了,把信息都写到项目的docs的文件中吧
- 13:21 [EN] 1

- 15:09 [ZH] ollama pull muse-glimmer:30b-q8_0-dflash 这个有国内的镜像源吗,不走代理
- 16:02 [ZH] OLLAMA_HOST=http://127.0.0.1:11435 ollama pull muse-glimmer:30b-q8_0-dflash 怎么让他开始下载,去掉代理的配置
- 16:02 [ZH] 你执行一下
- 16:18 [ZH] 怎么显式只有16G,不是30多G吗
- 16:20 [EN] OLLAMA_HOST=http://127.0.0.1:11435 ollama show muse-glimmer:30b-q8_0-dflash                                                                                      v1.95.0
Error: model 'muse-glimmer:30b-q8_0-dflash' not found
- 16:23 [ZH] 官网显示的是muse-glimmer:30b-q8_0-dflash

33G
- 16:28 [ZH] 或者他要分两层下?
- 20:05 [ZH] - 完整的 dispatch 调查报告：docs/experiments/2026-08-11-qwen35-paged-qmm-dispatch-inventory.md
  - 实验索引：docs/experiments/README.md
  - 总结果摘要：MLX_RESULTS.md

  文档包含：

  - 严格的 A/B 环境、运行命令和 artifact 路径
  - A 13.131 ms/token、B 22.052 ms/token
  - A/B 输出 SHA 完全一致
  - 每次 forward 固定 120 次 QMM
  - B 的 2,280 = 19 × 120 次 resident primitive 调用
  - 777/430 个 raw GPU interval 及其不能用于归因的原因
  - B Instruments rules-engine 错误和无 Metal kernel label 的限制
  - 已排除、尚未排除的假设
  - 明确声明目前没有找到可证明的根因
  - 如果以后恢复调查，最小的下一步实验是什么

  git diff --check 已通过，没有 commit。原始 trace 和测量数据保留在 .codex_logs/qwen36_dispatch_inventory_20260811/。看看codex这段时间做的
- 20:20 [ZH] 分析一下怎么做,全量缓存和部分差太多了,用全量的倒查一下
- 20:34 [ZH] 让codex去确认
- 20:36 [EN] Confirm or refute the hypothesis in /Users/youxun/project/mlx-lm/docs/plans/2026-08-11-paged-qmm-memory-starvation.md. Read it in full, plus its two prereq experiment reports.

WHY THIS PLAN EXISTS. Your dispatch-inventory report says no root cause was found and proposes an MLX_METAL_DEBUG=ON rebuild for kernel-family attribution. But the watchdog logs YOU collected for that report contain an unanalysed answer. Parsed across the decode window:

  A packed mmap : free memory 6.94-28.92 GB, 17.90 GB paged in, ~83 tok/s
  B all-resident: free memory 0.06-8.25 GB,  7.29 GB paged in, ~49 tok/s

B drives the box to 60 MB free while A never drops below ~7 GB. B reads 40% LESS from disk and runs 40% slower. That is not an I/O bound; it is a machine with nothing left to reclaim.

This also explains three things your own reports recorded as exonerating or puzzling: the isolated per-projection QMV test found compact FASTER (0.3912 vs 0.5042 ms) because it had no memory pressure; the 1/5/10/20/40-layer anonymous-vs-file-backed sweep found no difference because it held one representation at a time and never reached the wall; and A pages in more yet wins because its pages are clean file-backed and free to drop while B's 26 GiB anonymous cannot be reclaimed at all.

So your proposed next step is aimed at a hypothesis your own data weakens. Do the MLX_METAL_DEBUG rebuild only if this plan's falsifier fires.

DO THE TASKS IN ORDER:
1. Confirm the floor from existing artifacts only, no model runs. Parse every watchdog log under .codex_logs/qwen36_dispatch_inventory_20260811/ and the Gate 1 run dirs; produce free-memory-vs-time for A and B; report the minimum during the DECODE phase specifically. Gate: if B's floor is only a tail-of-run artifact rather than sustained through decode, say so and STOP -- the lead is wrong.
2. The reverse lookup: sweep B's resident budget 25/20/16/12/8 GiB with the remainder on packed mmap, 16 tokens each, agent prompt, one model at a time. Per point record tok/s, MINIMUM decode-time free memory, page-in rate, stored compressor, mlx_active/mlx_external, and token-id SHA.
3. Single-variable confirmation: find the budget where decode-time free memory returns to ~7 GB and run it to 200 tokens; check whether throughput returns to A's ~83 tok/s.

THE PREDICTION IS FALSIFIABLE AND YOU MUST TEST IT BOTH WAYS: tok/s should track minimum decode-time free memory, NOT resident GiB. Plot and report BOTH axes. Do not report only the one that looks better.

FALSIFIER: any sweep point with decode-time free memory back at 7-8 GB while tok/s stays pinned near 49 kills the hypothesis. Say so plainly and hand back to dispatch attribution. Do NOT adjust the sweep to preserve the hypothesis. A refutation is a valid and valuable result.

IMPORTANT -- WORKING TREE: there are ~1,528 lines of UNCOMMITTED changes (switch_layers.py +529, utils.py +147, tests +777, etc.) that implement the paged-QMM path you need. Do not revert, stash, or lose them. The new path is env-gated off by default and 231 tests pass; I verified that.

Ground rules: interpreter /Users/youxun/project/mlx/.venv_build/bin/python; model qwen3.6-35b-mlx-q6-aligned; one large model at a time; existing composite watchdog, TERM only. Memory gate before each run: available = free + inactive + purgeable >= ~8 GB AND stored compressor flat over 60s -- use 'stored' not 'occupied', and do NOT gate on bare 'Pages free', which excludes reclaimable pages and has caused two false stops already. Record the starting memory state with every measurement; that is the entire point of this plan. Do not push, origin is an unplugged external drive.
- 20:59 [ZH] 什么情况了
- 21:13 [ZH] 什么情况了
- 21:21 [EN] <task-notification>
<task-id>b0dt1dojf</task-id>
<tool-use-id>toolu_01Gb6vjCBUTwFB45sfnpZeS7</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b0dt1dojf.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex to confirm the memory-starvation hypothesis" completed (exit code 0)</summary>
</task-notification>
- 21:35 [ZH] 把混淆和 ballast 重做交给 codex
- 21:36 [EN] Two follow-ups on /Users/youxun/project/mlx-lm/docs/plans/2026-08-11-paged-qmm-memory-starvation.md. Your refutation stands -- do NOT re-run the sweep or re-derive it. I committed your results as 212dac4.

TASK 1 -- correct the record. The sweep I asked you to run was CONFOUNDED and your results doc must say so plainly, because otherwise someone will later cite it as 'memory was tested and excluded'. Dropping the resident budget moved two variables at once: available memory rose 8.45 -> 13.14 GiB, AND the backing path switched on (hit rate 1.000 -> 0.490, 0 -> 353,981 backing routes). The second dominates by far, so the sweep cannot isolate memory. That is a defect in my plan, not in your execution -- say it that way. Also record what the sweep DOES establish, which is more valuable than what it was aimed at: backing routes are catastrophic, 124k of them cost 68% of throughput, so a mixed resident/paged cache is only viable at a hit rate essentially equal to 1.0 -- at which point it has degenerated into full residency. That is a hard design constraint on the paged-QMM direction and belongs in the conclusion.

Note also that my hypothesis was built on the wrong metric: raw 'Pages free' (A 6.94-28.92 GB vs B 0.06-8.25 GB) looks damning, but on reclaimable memory it is only 9.69 vs 6.34 GiB. Your Task 1 analysis using reclaimable was the correct choice; make that explicit so the next reader does not repeat my error.

TASK 2 -- the single-variable ballast test, which is what should have been run instead. The 200-token confirmation (B at 8.50 GiB floor, 37.25 tok/s, vs A at 9.69 GiB floor, ~83 tok/s) compares two DIFFERENT configurations -- anonymous vs file-backed, different code paths -- so it is 'comparable memory, everything else different', not a controlled test. Fix that by holding B completely fixed and moving only system memory:

  - Config fixed: 25 GiB resident budget, full residency, ZERO backing routes, agent prompt, 16 tokens. Verify resident_hit_rate == 1.0 and backing_routes == 0 at every point; if a point drifts off full residency, discard it, do not report it.
  - Vary only a ballast process holding touched anonymous memory: 0, 2, 4, 6 GB.
  - Per point record tok/s, minimum decode-time available memory, page-in rate, stored compressor, and the token SHA. SHA must stay constant across points; flag it if not.

Interpretation, stated before you run:
  - If tok/s stays essentially flat while the floor is squeezed, memory is robustly excluded and the refutation is confirmed -- this is the EXPECTED result and a flat line is a real result, not a failed experiment. Do not hunt for a trend in noise.
  - If tok/s degrades monotonically from the first ballast step, B sits near a memory cliff after all and the refutation needs qualifying. Say so.

SAFETY: 26 GiB resident + ballast on a 48 GB box is the one genuinely risky thing here. Cap total so the machine keeps headroom; make the ballast trivially killable and kill it in a trap on exit; keep it under the existing composite watchdog; abort the point rather than pushing the box into swap. Stop the ballast ladder early if a step drives stored compressor toward the abort threshold -- an aborted high-ballast point is fine, an unresponsive machine is not.

Memory gate unchanged: available = free + inactive + purgeable >= ~8 GB and stored compressor flat over 60s before each run; 'stored' not 'occupied'; never bare 'Pages free'. TERM never -9. Do not push. Do not revert or lose the ~1,528 lines of uncommitted paged-QMM implementation in the worktree -- you need them. Commit your doc corrections and the ballast results.
- 21:52 [EN] <task-notification>
<task-id>bz0mn62nk</task-id>
<tool-use-id>toolu_01S2vBPhnmGoywRGPvXxVmHb</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bz0mn62nk.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex for confound correction and ballast test" completed (exit code 0)</summary>
</task-notification>
- 21:55 [EN] Your ballast ladder failed for a reason you misdiagnosed. Task 1 (the record correction, fda9d42) is good and is done -- do not redo it. Redo Task 4, the ballast test, with the correct environment.

YOUR DIAGNOSIS IS WRONG. You reported 'the historical driver is incompatible with the current uncommitted loader implementation'. It is not. The same driver ran five sweep points successfully against that exact uncommitted code roughly an hour earlier. What changed is that you built a different environment. Diffing .codex_logs/run_qwen36_ballast_ladder_20260811.sh against the known-working .codex_logs/run_qwen36_paged_b_allhit.sh:

  dropped: MLX_EXPERT_PACKED_DECODE=1
  dropped: MLX_EXPERT_PACKED_PREFILL=1
  dropped: MLX_QWEN35_PAGED_QMM_DECODE=1
  dropped: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1
  dropped: MLX_QWEN35_PAGED_QMM_PLACEMENT=<run_dir>/full_placement.json
  changed: MLX_EXPERT_RESIDENT_GB  0 -> 25
  added:   MLX_EXPERT_MMAP=1
  added:   MLX_EXPERT_MMAP_PACKED=1

You dropped every flag that enables the paged-QMM resident path and added MLX_EXPERT_MMAP_PACKED=1, which selects a different loader that never populates . That is exactly the AttributeError at switch_layers.py:1111. The KeyError: 'weight' in the invalid_baseline directory is the same MLX_EXPERT_MMAP_PACKED failure you already documented once in the dispatch-inventory report as 'the additional mmap flag combination'.

This matters beyond the rerun: the working tree holds ~1,528 lines of uncommitted paged-QMM implementation, and a report saying the loader is incompatible invites someone to 'fix' code that is not broken. Correct that statement wherever you wrote it.

REDO THE BALLAST LADDER using the canonical working B environment from run_qwen36_paged_b_allhit.sh verbatim -- MLX_EXPERT_STREAM=1, MLX_EXPERT_PACKED_DECODE=1, MLX_EXPERT_PACKED_PREFILL=1, MLX_EXPERT_RESIDENT_GB=0, MLX_EXPERT_STREAM_CACHE_GB=25, MLX_QWEN35_PAGED_QMM_DECODE=1, MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1, MLX_QWEN35_PAGED_QMM_PLACEMENT pointing at a generated full placement -- and do NOT set MLX_EXPERT_MMAP or MLX_EXPERT_MMAP_PACKED. Before starting the ladder, run the 0 GB point alone and confirm it reproduces roughly 37-40 tok/s with resident_hit_rate == 1.0 and backing_routes == 0. If that baseline does not reproduce, stop and report -- do not run the ladder against a broken baseline.

Everything else about Task 4 is unchanged and already written into the plan: fixed 25 GiB resident, full residency, agent prompt, 16 tokens, ballast at 0/2/4/6 GB of touched anonymous memory, per-point tok/s + minimum decode-time available memory + page-in rate + stored compressor + token SHA, SHA must stay constant, discard any point that drifts off full residency. Interpretation is predeclared: a flat line means memory is robustly excluded and is a REAL result, not a failed experiment -- do not hunt a trend in noise; monotone degradation from the first step means B sits near a memory cliff and the refutation needs qualifying.

Safety unchanged: ballast trivially killable with a TERM trap, under the composite watchdog, cap total pressure, abort a point rather than pushing the box into swap, stop the ladder early if stored compressor approaches the threshold. Memory gate: available = free + inactive + purgeable >= ~8 GB and stored compressor flat over 60s; never bare 'Pages free'. TERM never -9. Do not push. Do not revert, stash, or modify the uncommitted paged-QMM implementation.
- 22:19 [EN] <task-notification>
<task-id>by27qvyyt</task-id>
<tool-use-id>toolu_01XpaXR7Uy4C5kRmomYaRZXe</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/by27qvyyt.output</output-file>
<status>completed</status>
<summary>Background command "Re-dispatch codex with the correct environment" completed (exit code 0)</summary>
</task-notification>
- 22:31 [EN] go ahead
- 22:32 [EN] Three tasks. Read docs/plans/2026-08-11-paged-qmm-memory-starvation.md and its results doc first.

TASK 1 -- your ballast rerun is NULL and the record must say so. The treatment never landed. Your own four points show min decode-time available memory at 8.158 / 8.187 / 8.212 / 8.290 GiB for 0/2/4/6 GiB of ballast -- it did not fall, it rose slightly. The ballast was compressed away: stored compressor grew 5.25 -> 8.93 GiB on the 2 GiB point and 6.44 -> 12.80 GiB on the 6 GiB point, i.e. by roughly the ballast size each time. Cause is in .codex_logs/ballast_anon.py: it allocates a zero-filled bytearray and writes one byte per 1 MiB, so the pages are perfectly compressible and never re-touched. A flat dependent variable measured against a flat independent variable is not evidence. Correct the docs: the ballast run must NOT be cited as controlled confirmation. The refutation still stands on the earlier 200-token comparison (B 8.50 GiB floor / 37 tok/s vs A 9.69 GiB floor / ~83 tok/s), which is 'comparable memory, everything else different' -- real but not controlled. Say exactly that.

TASK 2 (PRIMARY) -- dispatch attribution, the experiment you originally proposed and I wrongly steered you away from. Memory is excluded well enough to proceed. What remains unexplained is A ~83 tok/s vs B 37-40 tok/s at full residency, zero backing, 120 QMM primitives per forward on both, with B's kernel measured FASTER in isolation (gate 0.3912 vs 0.5042 ms). That leaves kernel-family selection, per-forward auxiliary dispatch count, or command-buffer scheduling.

Rebuild MLX with Metal labels enabled. metal-cpp is already extracted for you at /private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/scratchpad/metalcpp/metal-cpp (the reboot wiped the old copy; both local-source overrides are still mandatory or CMake hangs on this network):

  cd /Users/youxun/project/mlx
  CMAKE_ARGS="-DUSE_SYSTEM_FMT=ON -DFETCHCONTENT_SOURCE_DIR_METAL_CPP=/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/scratchpad/metalcpp/metal-cpp -DMLX_METAL_DEBUG=ON" .venv_build/bin/pip install -e . --no-build-isolation

CRITICAL -- THIS REPLACES THE INTERPRETER THE PRODUCTION BRIDGE RUNS ON. .venv_build is shared. MLX_METAL_DEBUG is a debug build and its own timings are not comparable to release numbers, so use it ONLY for dispatch attribution, never to re-measure throughput. When you are done you MUST rebuild release (same command without -DMLX_METAL_DEBUG=ON) and verify hasattr(mx,'get_external_memory')==True. State clearly in your report whether the interpreter was restored. The bridge is currently stopped; leave it stopped.

Then capture matched A and B with token-boundary windows or explicit signposts, and report per forward: named kernel families dispatched, dispatch counts, and command-buffer submission counts. The question to answer is narrow: does B issue different or more dispatches per forward than A, or schedule them differently. If Instruments again fails with 'Rules engine appears to be stuck', say so and stop rather than exporting a partial trace as evidence -- you correctly refused to over-read 777 vs 430 last time, hold that line.

TASK 3 (secondary, only if Task 2 completes or is blocked) -- fix and rerun the ballast ladder on the RELEASE build, never the debug one. Fill with incompressible random bytes, touch every page at the 16 KiB page stride, and re-touch continuously in a loop so pages stay hot. Then enforce the rule your last run lacked: ASSERT THE TREATMENT LANDED. A point is only valid if decode-time available memory falls by roughly the ballast size versus the 0 GiB point; if it does not, mark the point invalid and say the ladder failed rather than reporting a flat line.

Canonical B env, verbatim, do not improvise: MLX_EXPERT_STREAM=1, MLX_EXPERT_PACKED_DECODE=1, MLX_EXPERT_PACKED_PREFILL=1, MLX_EXPERT_RESIDENT_GB=0, MLX_EXPERT_STREAM_CACHE_GB=25, MLX_QWEN35_PAGED_QMM_DECODE=1, MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1, MLX_QWEN35_PAGED_QMM_PLACEMENT=<run_dir>/full_placement.json. Do NOT set MLX_EXPERT_MMAP or MLX_EXPERT_MMAP_PACKED -- that was the cause of the AttributeError and KeyError last time, not any loader incompatibility.

Unchanged: memory gate available = free+inactive+purgeable >= ~8 GB and stored compressor flat over 60s, never bare 'Pages free'; one large model at a time; TERM never -9; do not push; do not revert, stash, or modify the ~1,528 lines of uncommitted paged-QMM implementation. Commit doc corrections and results.
- 22:43 [EN] what's the stutas
- 23:01 [EN] <task-notification>
<task-id>bxwk1c1pf</task-id>
<tool-use-id>toolu_018v87xvcs9fUYv3YGg1dYAi</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bxwk1c1pf.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex for dispatch attribution plus ballast fix" completed (exit code 0)</summary>
</task-notification>
- 23:12 [ZH] 开始吧
- 23:15 [EN] New experiment: in-situ per-call QMM timing. Abandon the Instruments path.

WHY WE ARE ABANDONING IT. The MLX_METAL_DEBUG=ON rebuild did not fix anything: .codex_logs/qwen36_dispatch_debug_20260811/A/cb.xml is 1941 bytes with ZERO rows, exactly as before the rebuild. The whole premise was that CMakeLists.txt:39 defaults MLX_METAL_DEBUG OFF so utils.h:34 never labels the command buffer -- we turned it on and the tables were still empty. You reported that as 'unresolved' but did not check whether the flag actually took effect. That check was the cheap prerequisite: confirm labels appear on a five-line MLX program under xctrace BEFORE spending a full A/B capture. Two failed attempts with identical empty tables is enough; stop paying for this tool.

THE REPLACEMENT, which needs no Instruments. The isolated per-projection QMV comparison already showed B FASTER than A: gate 0.3912 vs 0.5042 ms, up 0.4831 vs 0.5236, down 0.4747 vs 0.5912. Yet end-to-end B is 37-40 tok/s against A's ~83. Those two facts cannot both describe the same kernel doing the same work, so measure the SAME per-projection timing INSIDE the real model forward and compare in-situ against isolated:

  - Instrument the 120 QMM invocations per decode forward (40 layers x gate/up/down) to record per-call wall time, in both A (packed mmap) and B (full-resident, zero backing).
  - Use the canonical environments already established. B: MLX_EXPERT_STREAM=1, MLX_EXPERT_PACKED_DECODE=1, MLX_EXPERT_PACKED_PREFILL=1, MLX_EXPERT_RESIDENT_GB=0, MLX_EXPERT_STREAM_CACHE_GB=25, MLX_QWEN35_PAGED_QMM_DECODE=1, MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1, MLX_QWEN35_PAGED_QMM_PLACEMENT=<run_dir>/full_placement.json. Never set MLX_EXPERT_MMAP or MLX_EXPERT_MMAP_PACKED. Verify resident_hit_rate == 1.0 and backing_routes == 0 for B.
  - Beware the measurement trap: MLX is async, so a naive wall-clock around a call measures enqueue, not execution. Force per-call completion (mx.eval / synchronize) or use a method that genuinely attributes GPU time, and SAY which you used. Note in the report that forcing synchronisation perturbs the pipeline -- report the perturbed total alongside the unperturbed baseline so the reader can judge.

The question is narrow and has three possible answers, all publishable:
  (a) B's in-situ per-call time is much worse than its isolated 0.39-0.48 ms while A's roughly matches its isolated time -> the cost is contextual (scheduling, contention, cache/TLB behaviour at model scale), not the kernel. Then report where in the forward it concentrates: uniformly across all 120, or specific layers/projections.
  (b) Both match their isolated times, so 120 x per-call does not sum to the observed per-token time -> the gap is OUTSIDE the QMM calls, in whatever else the forward does, and you have localised it by elimination.
  (c) Neither -- report the numbers and say the accounting does not close.

Sanity anchor: at 83 tok/s a whole token is ~12 ms and at 37 tok/s ~27 ms, while 120 x 0.45 ms would be 54 ms. The isolated numbers already do not compose with either, so treat isolated timings as relative-only and say so.

IMPORTANT -- the worktree is now CLEAN. I committed everything that was outstanding: the paged-QMM implementation as 0647c77 (default off, negative result in the message), the thinking EOS guard as 665dd2c, the docs as f7a344b, and I moved the three root .trace bundles into the gitignored .codex_logs/instruments_traces/. Start from a clean tree and commit your own work.

Unchanged: interpreter /Users/youxun/project/mlx/.venv_build/bin/python (release build, verify hasattr(mx,'get_external_memory')==True); model qwen3.6-35b-mlx-q6-aligned; agent prompt; one large model at a time; composite watchdog, TERM never -9; memory gate available = free+inactive+purgeable >= ~8 GB with stored compressor flat over 60s, never bare 'Pages free'; record the starting memory state with every measurement; do not push.
- 23:17 [EN] <task-notification>
<task-id>b1vwhrino</task-id>
<tool-use-id>toolu_01WVJr57WJmeER8sgD1h3GMj</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b1vwhrino.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex for in-situ per-call QMM timing" completed (exit code 0)</summary>
</task-notification>
- 23:18 [EN] Approved: option 1, instrumentation plus runner/reporting. Proceed and do NOT stop for approval again on this task -- your design matches what was asked for.

Your plan is right on the points that matter: opt-in env gate, per-call rows tagged with layer/projection/route, mx.eval per call for the attributed mode, an unforced baseline preserved alongside it, both totals reported, B verified at resident_hit_rate == 1.0 and backing_routes == 0, and isolated timings treated as relative-only.

ONE REFINEMENT YOU ARE MISSING, and it changes how you must read the result. Forcing mx.eval after every call serializes the pipeline -- which can ERASE the very effect we are hunting. If B's penalty comes from poor overlap or scheduling rather than from kernel execution, then per-call forced timings for A and B will look identical while the unforced totals still differ by 2.2x. That is not a null result; it is the signature of a scheduling/overlap cause and it is arguably the most informative outcome available. So add it explicitly as a fourth classification:

  (d) forced per-call times match between A and B, but unforced end-to-end totals still differ -> the cost is not in kernel execution but in how the work is pipelined or scheduled around it.

Report the forced-vs-unforced delta for A and for B SEPARATELY, not just as a combined perturbation note. If forcing synchronisation costs A little but costs B a lot -- or the reverse -- that asymmetry is itself the finding, because it says how much each path depends on overlap.

Also: aggregate per-call rows by layer index, not only by projection. If the penalty concentrates in the early or late layers rather than spreading evenly across all 40, that points somewhere completely different from a uniform per-call tax, and a per-projection-only average would hide it.

Everything else stands: canonical B environment verbatim, never MLX_EXPERT_MMAP or MLX_EXPERT_MMAP_PACKED; release interpreter, verify hasattr(mx,'get_external_memory')==True; agent prompt; one large model at a time; composite watchdog with TERM only; memory gate available = free+inactive+purgeable >= ~8 GB and stored compressor flat over 60s, never bare 'Pages free'; record starting memory with every measurement; instrumentation disabled unless its env var is set so the production decode path is untouched; run the full test suite before committing; do not push.

The worktree is clean at f7a344b. Commit your instrumentation, runner, and results.
- 23:43 [EN] <task-notification>
<task-id>bv4z87ql2</task-id>
<tool-use-id>toolu_01Spqr86V1uFxZJHCXQChgNb</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bv4z87ql2.output</output-file>
<status>completed</status>
<summary>Background command "Approve option 1 and add the overlap-erasure refinement" completed (exit code 0)</summary>
</task-notification>
