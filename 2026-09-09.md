# English Learning Log — 2026-09-09

- 00:00 [ZH] 你看过agent的真实请求吗,这样切会切3层是什么样的
- 00:03 [ZH] 第一个进来,不存可以吗,或者全存在1层,等后面进来再分裂
- 00:11 [ZH] 重新,方案有存库把
- 00:12 [ZH] 我的意思是,kv cache有存盘把
- 00:17 [ZH] 当然要存盘了,内存和盘都要有最大配置,淘汰策略
- 10:26 [ZH] 给我一个mac-assistant接入的Hermes memory的技术方案,先把他在prompt的里面的各个部分的构成讲清楚,每部分的功能是什么

- 10:35 [ZH] 让你把现状讲清楚就行了,不是让你改架构,太啰嗦了,把记忆部分的构成和每部分的作用讲清楚就行了

- 10:56 [ZH] mermaid在展示的时候,加个功能,可以选择看到原始的文本,就叫code把,组件应该本身就支持把
- 11:01 [ZH] 可以就这么干吧
- 11:23 [ZH] code的前面加个空格,同时要能看出来可以点,展示的code加个围栏把
- 11:39 [ZH] 隐藏的时候就能看到一条黑线,可点击状态有问题,影响了其他的区域,其他区域鼠标的状态也会变
- 11:41 [ZH] 黑线应该是整个code的fence漏了一部分
- 13:05 [ZH] file:///Users/youxun/project/mac-assistant/docs/superpowers/proposals/mac-assistant-memory-architecture.html 看看这个架构图搞错了,graphiti作为外部记忆应该和记忆插件连,不是直接和pi agent 连接, digest和记忆插件应该也有连线
- 13:16 [ZH] 可以
- 13:17 [ZH] 当前分支有哪些内容,都是在改kv cache,重构的话,这个分支可以不要了把

- 13:22 [ZH] 我要重构了,哪些东西是有价值的,需要单独cherry pick的

- 13:25 [ZH] 是否可以开始重构了,还有什么方法不清楚吗
- 13:34 [ZH] 90346b4   必要时保留：Qwen4-Exp PLE repack 修复
f2dc060   建议保留：server 不存在模型返回 404
ddfdfba   可选保留：APC prompt 调试日志
69246bb   手工保留 .gitignore 规则即可 要新拉分支,是不是这些可以移植过来,还有别的吗
- 13:41 [ZH] 写个方案让dsh用flash模型去试试看
- 13:48 [ZH] 现在是一开始还是能看到那个黑线,点了code再点diagram切回去的时候,看不到了
- 13:58 [EN] <task-notification>
<task-id>b5rlu1rp0</task-id>
<tool-use-id>toolu_013JqKpdcVN6h5B5dy1Vy7mH</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-vlm/d1754701-599f-44cb-8ec4-9331b7d6a4fa/tasks/b5rlu1rp0.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch the replay task to dsh" completed (exit code 0)</summary>
</task-notification>
- 14:00 [ZH] code旁边加copy按钮,直接把文本拷贝走
- 14:03 [ZH] 意思是还是重写,但是直接在APC上改,不移植mlx-llm
- 14:04 [ZH] 意思是还是重写,但是直接在APC上改,不移植mlx-llm的代码了吗?
- 14:09 [ZH] 方案B还是不变是这个意思吗
- 14:11 [ZH] 改, 分支的问题已经解决了?
- 14:16 [ZH] 你的方案中不包含,类和接口的设计吗
- 14:21 [ZH] 2个方案地址给我
- 14:22 [ZH] review一下方案 A  /Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md
  方案 B  /Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md
- 14:33 [ZH]  ### 严重问题

  - 方案 B：fork_owner() 会覆盖 parent 条目，无法保证 fork 隔离。
    方案 B 要求 fork 把 parent 的最深条目复制给 child，并保证 parent 后续仍可命中（session-cache.md:124 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:124)、session-cache.md:153 (/Users/youxun/project/mlx-
    vlm-main/docs/proposals/2026-09-08-session-cache.md:153)、session-cache.md:160 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:160)。但当前 APC 的内存 key 只由 token_ids + extra_hash + block_size 计算，写
    入时直接赋值到该 key（/Users/youxun/project/mlx-vlm-main/mlx_vlm/apc.py:3578、/Users/youxun/project/mlx-vlm-main/mlx_vlm/apc.py:3582）。parent 和 child 的前缀相同，因此 child 的 fork 副本会替换 parent，而不是形成两个独立条目；
    pin_owner() 只能阻止淘汰，阻止不了同 key 覆盖。
    建议改为：内容 hash 与条目 identity 分离，允许同一内容被多个 owner 引用，最好采用引用计数或 copy-on-write；同时明确 lookup 在多个 owner 引用下如何选择、如何统计字节。

  ### 高风险问题

  - 方案 B：owner 只加在内存条目上，磁盘缓存和重启路径会丢失归因。
    方案只描述给 APCExactCacheEntry 增加 owner（session-cache.md:87 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:87)），但当前磁盘索引是 cache_hash -> path（/Users/youxun/project/mlx-vlm-main/mlx_vlm/
    apc.py:1187），磁盘查找只按 extra_hash 和 token 前缀匹配（/Users/youxun/project/mlx-vlm-main/mlx_vlm/apc.py:1530），save_exact_cache() 也没有 owner 参数。这样会产生几个后果：重启后 /v1/sessions 看不到磁盘条目的 owner；DELETE /v1/
    sessions/{id} 无法释放已落盘的该 owner 条目；匿名条目可能被重新加载并绕过 owner-class 淘汰策略。
    方案 B 必须补充磁盘 metadata/index/删除/恢复协议，或者明确“带 owner 的条目禁止落盘”并接受相应的容量和重启语义。

  - 方案 A：cut 来源没有真正传到存储层，现有流水线会把 semantic cut 当成 final。
    方案要求每个切点带有 semantic/stride/final 来源（prefix-cache.md:158 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:158)、prefix-cache.md:175 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-
    prefix-cache.md:175)），但示例签名仍返回 List[int]，没有来源信息（prefix-cache.md:160 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:160)）。当前生成器也会把 checkpoint 处理成排序后的整数集合（/Users/
    youxun/project/mlx-vlm-main/mlx_vlm/generate/ar.py:462），回调只收到 processed_tokens，存储调用只传 token、cache 和 extra_hash（/Users/youxun/project/mlx-vlm-main/mlx_vlm/generate/ar.py:2005）。因此实现后很容易出现：切点确实生成了，但
    APCExactCacheEntry.cut 永远使用默认值 "final"，日志和淘汰统计无法证明语义刀生效。
    建议把 checkpoint 描述改成带来源的结构，例如 (length, cut_kind)，贯穿 coordinator、prefill checkpoint callback、store_exact_cache()；相同 length 去重时要定义 semantic 优先级。

  - 方案 A：默认日志要求输出解码后的 token，存在敏感信息泄露风险。
    方案明确要求 cached_tok/request_tok 必须带字面量，且可选输出分歧点前后上下文（prefix-cache.md:256 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:256)、prefix-cache.md:261 (/Users/youxun/project/mlx-vlm-
    main/docs/proposals/2026-09-08-prefix-cache.md:261)）。真实 prompt 可能包含 API key、文件路径、用户内容、工具参数或会话摘要；即使上下文开关默认关闭，单个 token 字面量也可能泄露敏感数据。方案没有定义转义、截断、权限、保留期或日志清理策
    略。
    建议默认只记录 token id、稳定 hash 或分类后的差异类型；完整字面量和上下文必须显式 opt-in，并限制长度、字符集和访问范围。

  ### 中等问题和设计缺口

  - A、B 并不真正独立，缺少合并顺序和联合验收。
    两份方案都修改同一个 APCExactCacheEntry、store_exact_cache()、_make_room()、stats 和日志路径（prefix-cache.md:199 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:199)、session-cache.md:100 (/Users/youxun/
    project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:100)）。A 的淘汰键是 (cut_rank, owner_rank, last_used)，B 又要求 (owner_class, last_used)；两者的优先级、同级排序和开关关闭时的行为没有形成一个完整定义。应增加一个联合矩
    阵：semantic/final × owned/anonymous × memory/disk，并明确每种组合的淘汰、统计和回滚行为。

  - 方案 B 的 owner header 需要定义信任边界。
    B 依赖客户端发送 X-KV-Session（session-cache.md:42 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:42)），但没有说明 header 的长度、字符集、命名空间、认证绑定和伪造处理。若服务端可被多个调用方访问，任意客
    户端都可以冒用另一个 session id，影响淘汰、删除和 fork。至少需要限制格式和长度，并明确它是否必须绑定认证主体/租户。

  - 方案 A 的语义边界在多模态和模板分支下需要更明确。
    方案要求对 messages[:k] 使用与完整 prompt 相同的 tools/num_images/template_kwargs（prefix-cache.md:127 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:127)）。如果 num_images 是整请求计数，而前缀只包含部分
    图片，前缀渲染可能并不代表真实前缀。对齐断言能避免错误命中，但会把这类请求静默降级为没有 semantic cut。应在验收中覆盖“图片位于 system 前后”和工具列表变化的情况，并把“切点被丢弃”的计数纳入 stats。

  方案 A 可以在补齐来源传播、日志隐私和 A/B 联合淘汰规则后推进。方案 B 的 fork 需要先解决条目 key 覆盖问题，并把 owner 元数据扩展到磁盘和重启路径；在这两点解决前，S3、S6、S8 的验收即使在纯内存测试中通过，也不能代表线上语义成立。看看这些问题是否真实存在
- 14:37 [ZH] 这2个方案还有关联吗,有必要吗,独立存储不行吗
- 14:38 [ZH] 方案B是什么,方案B不是session相关的吗,缓存一个session吗
- 14:40 [ZH] 会不会搞复杂了,实现不了啊
- 14:42 [ZH] 一开始的设计不是说独立存储吗,session 完整保留一份吗,现在这样复杂度是不是太高了, 会不会和之前一样实现完一堆bug
- 14:44 [ZH] 改一下,2个方案,独立掉,这样两者就不会有依赖了把
- 14:47 [ZH] mac-assistant的脚本,启动这个服务的参数有问题吧: 启动参数要了 26GB 权重 + 24GB expert cache + 8GB kvsession > 48GB 物理内存。平时勉强撑住，一旦空闲（那次空了 3 小时 37 分），系统就把权重换出去；下一条请求要花 83 秒把 26GB 从磁盘读回来。
- 14:53 [ZH] 改完了,再review一下
- 14:55 [ZH] 可以
- 15:05 [ZH] 这版比上一版明显清楚，尤其是 B 改成独立 session cache 后，之前“owner 条目覆盖 parent”的问题已经消失了。两案现在可以真正独立推进。

  但目前仍有几个实现级阻塞点。我的结论是：设计方向可行，方案还不能直接进入开发，需要先补齐接口和资源边界。

  ### 高风险问题

  - 方案 B 没有定义如何接入实际生成路径，当前两条服务路径都没有 session cache 接口。
    B 只定义了 SessionCache.lookup/store/fork/drop（session-cache.md:105 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:105)），但没有明确在哪一步调用。当前代码有两条生成路径：
      - 普通 stream_generate() 路径；
      - ResponseGenerator / BatchGenerator 的 continuous batching 路径。

    现有 APC lookup 在 /Users/youxun/project/mlx-vlm-main/mlx_vlm/generate/dispatch.py:956，continuous batching 的 lookup 在 /Users/youxun/project/mlx-vlm-main/mlx_vlm/generate/ar.py:2568。B 必须分别说明：
      1. session lookup 发生在 APC 之前还是之后；
      2. session hit 如何构造成 prompt_cache；
      3. session miss 后如何保证 APC 路径完全不变；
      4. 生成完成后在哪个回调保存完整 session snapshot；
      5. streaming 中途断开或异常时是否保存。

    目前只有 B5 的概念流程，没有对应到这两条实际调用链。建议增加一节“Runtime integration”，列出 dispatch.py 和 ar.py 两条路径的精确调用点。否则很容易只实现 fallback 路径，continuous batching 下完全不生效。

  - 方案 A 仍然没有闭合 semantic cut 的实际数据来源。
    方案声称不需要修改 ar.py，但当前生成流程直接调用：

    apc_coordinator.checkpoint_lengths(full_input_ids_list, multimodal_token_ids)

    见 /Users/youxun/project/mlx-vlm-main/mlx_vlm/generate/dispatch.py:1033。这里没有 semantic_lengths，也没有请求的 messages、tools 或 template_kwargs。方案 A 定义了 semantic_prefix_strings()（prefix-cache.md:118 (/Users/youxun/project/
    mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:118)），但没有说明这些消息对象如何从 API normalization 层传到 dispatch.py。

    目前文档中的“多捕获一张表”只是伪代码（prefix-cache.md:196 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:196)）。需要明确：
      - semantic cut 在哪个 API 层计算；
      - 计算结果放在哪个 request/generation 字段；
      - stream_generate() 和 BatchGenerator 如何读取；
      - reused_prefix_len 存在时，cut_kind_by_length 使用绝对 token 长度还是 suffix 长度。

    在这点补齐前，A 仍可能出现“语义切点算出来了，但实际 checkpoint 仍按 stride 存储”的情况。

  ### 中风险问题

  - B 的“独立预算”会带来总设备内存失控风险。
    B 定义 SESSION_CACHE_RAM_MAX_GB，并明确与 APC 预算互不侵占（session-cache.md:181 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:181)）。这解决了 session 条目互相淘汰的问题，但也意味着：

    APC_MEMORY_MAX_GB
    + SESSION_CACHE_RAM_MAX_GB
    + 当前模型 KV
    + prefill reserve

    可能同时驻留。比如 APC 允许 8 GB、session cache 允许 2 GB，加上正在生成的 KV，设备内存可能超过实际可用量。独立存储应该是独立淘汰域，不应该完全绕过全局设备安全上限。

    建议区分两个概念：
      - session cache 有自己的逻辑预算；
      - 所有 cache 加当前请求必须服从一个全局 hard limit。

    当总内存不足时，session cache 应先从 RAM 降级到磁盘，而不是继续保留到自己的预算上限。

  - B 的磁盘格式还不够可实现。
    文档指定“一个 session 一个文件”，并要求支持落盘、重启、回填和删除（session-cache.md:190 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:190)），但没有定义：
      - 文件格式是 safetensors、pickle 还是现有 APC 格式；
      - 如何保证写入原子性；
      - 进程崩溃时如何处理半文件；
      - 模型、量化配置、cache 类型变化后如何拒绝旧文件；
      - 并发请求同一 session 时，旧写入是否可能覆盖新写入；
      - 磁盘加载期间是否阻塞 GPU 线程。

    这不是文档细节，而是 B 的核心实现。建议至少加上 format_version、模型/量化指纹、临时文件 + os.replace、单 session 写锁和损坏文件删除策略。

  - B 的 session key 还需要包含模型运行环境约束。
    文档把 key 定义为 (tenant, session_id)（session-cache.md:82 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:82)），而快照还依赖 extra_hash。如果同一 tenant/session 在不同模型、不同 KV quantization、不同
    cache layout 或不同 processor 配置下复用，可能得到无法加载或语义不兼容的 KV。

    不能只依赖请求 token 和 extra_hash。应在 snapshot metadata 中加入至少：
      - model identity/version；
      - cache layout/version；
      - KV quantization 配置；
      - tokenizer/template 版本或稳定指纹。

  - B 的“整份快照”与当前生成结果的边界还不明确。
    B 说存本次请求的 len - guard（session-cache.md:146 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:146)），但当前生成结束时 prompt_cache 已经包含 prompt 和生成出的 assistant tokens；现有 APC 的 commit 逻
    辑也是在生成后处理完整 token 序列，见 /Users/youxun/project/mlx-vlm-main/mlx_vlm/generate/dispatch.py:1142。

    需要明确 snapshot 对应的是：

    输入 prompt
    还是
    输入 prompt + 已生成 assistant 回复

    对连续会话来说，下一轮通常需要后者。如果只保存输入 prompt，下一轮会重复计算上一轮 assistant 回复；如果保存生成结果，则必须保证只在正常完成时提交，并定义 tool call、截断、客户端断开时的行为。

  - B 的 fork 仍然需要定义失败和覆盖语义。
    独立槽位解决了 parent 被覆盖的问题，但 fork(parent, child) 如果 child 已经有旧快照，文档没有说明是拒绝、覆盖还是先删除。X-KV-Fork-From 指向不存在 session 时“降级为冷跑”（session-cache.md:91 (/Users/youxun/project/mlx-vlm-main/docs/
    proposals/2026-09-08-session-cache.md:91)），但 child 是否仍会保存新快照也需要写清楚。

  ### 低风险问题

  - A 的“完全独立”表述基本成立，但两边仍共享同一生成参数传递通道 APC_PRIVATE_PROMPT_KEYS。这不是架构问题，不过建议将其描述为“共享传输机制，不共享缓存状态”，避免后续维护者误以为完全没有代码耦合。
  - B1b 工具稳定排序是有价值的，但它不是 session cache 的必要条件。建议单独列为优化项，避免它阻塞 B 的核心 MVP。
  - B 的 S8 要求“重启后回填 RAM”，但没有定义回填失败时是否保留磁盘文件、是否计为 miss、是否删除损坏文件。验收标准需要补上。

  ### 修订后的结论

  现在两案的关系已经合理：

  - A：修改全局 APC 的 checkpoint 切点。
  - B：新增独立的 session-scoped cache。
  - 两者不共享条目、预算、淘汰和磁盘目录。
  - 带 session 的请求可以先查 B，B miss 后走 APC；无 session 的请求直接走 APC。

  我建议按下面顺序推进：

  1. 先实现 B 的纯内存 MVP：lookup/store/fork/drop，只接普通 stream_generate()。
  2. 增加 parent/child、分支 miss、异常结束不提交的测试。
  3. 再接 continuous batching。
  4. 最后做磁盘和重启恢复。
  5. A 单独补齐 semantic cut 从 request normalization 到两条生成路径的传递链路。

  目前没有再发现上一版那种“设计模型本身互相矛盾”的致命问题；但如果不补上 B 的 runtime integration 和 A 的 semantic cut 数据流，仍然会出现方案写得完整、实际请求路径却没有使用它们的风险。
- 15:12 [ZH] 再review一下
- 15:14 [ZH] 一条建议我不采纳，理由在代码里

  "下一轮通常需要 prompt + 生成的回复" —— 对这个模型，那份存了也永远用不了：

  # dispatch.py:733
  n_drop = max(0, cached_len - prefix_len)
  if n_drop and not all(c.is_trimmable() and _cache_fully_retained(c) for c in kv_cache):
      return None          # → 冷 prefill

  存了生成结果，下一轮共享前缀必然短于 cached_len（模板重渲染尾部）→ n_drop > 0；40 层里 30 层是 ArraysCache，is_trimmable() 为 False → 返回 None，冷 prefill，快照白存。存在 len - guard 才有 n_drop == 0。这正是上游 checkpoint
  模式存在的理由。

  代价是本轮回复那几百 token 下轮重算 —— 实测 21,084 里对话部分总共 234 个，1.1%，上游就是这么存的，实测复用 98%。

  而且这个选择顺带消掉一整类异常路径：快照在 prefill 途中落定，与生成是否完成无关，所以断开、超时、tool call 中断都不需要特殊处理。你问的第 5 点因此不存在。 他说的这个你认同吗
- 15:17 [ZH] 再次review结果: 这版已经接近可执行方案了，B 的独立存储方向也成立。之前的架构性问题基本解决，但还有两个高风险接口断点，建议修完再开始实现。

  ### 高风险

  - 方案 A：continuous batching 路径仍然无法把 cut=semantic 传到存储层。
    文档现在补了两个 checkpoint_lengths() 调用点（prefix-cache.md:300 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-cache.md:300)），但实际存储路径在 ar.py 的 _store_apc_exact_checkpoints()，它调用的是：

    coordinator.store_checkpoint(
        ...,
        extra_hash=meta.get("extra_hash", 0),
    )

    当前 APCCoordinator.store_checkpoint() 也只接受 extra_hash，没有 cut 参数。文档只给 dispatch.py 的 exact_checkpoint() 写了 cut 传递示例（prefix-cache.md:196 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-prefix-
    cache.md:196)），没有定义 continuous batching 如何保存来源。

    结果是：continuous batching 可能正确生成 semantic checkpoint，但存储时仍全部落成默认 "final"，G10 会失败，或者为了修复而在两条路径中产生不同逻辑。

    需要明确并统一：
      - checkpoint_lengths() 返回长度和来源映射；
      - ar.py 的 apc_meta 保存 cut_kind_by_length；
      - _store_apc_exact_checkpoints() 把对应 cut 传给 coordinator.store_checkpoint()；
      - APCCoordinator.store_checkpoint() 再传给 store_exact_cache()；
      - dispatch.py 和 ar.py 使用同一个绝对 token 长度映射。

    这会修改 ar.py，所以文档中“ar.py 与回调签名零改动”的表述已经不成立，应删除。

  - 方案 B：continuous batching 的 session hit 如何合并到 batch 仍未定义。
    文档指定在 _apc_pick_for() 之前查 session cache（session-cache.md:296 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:296)），但这个函数是按单行生成 APC pick，后面还要经过 _build_prompt_batch /
    merge_rows，把多行不同长度的 cache 合并成一个 batch cache。

    Session snapshot 是“整份 prompt cache”，而不是 APC 的 block hit。方案没有说明：
      - session hit 如何转换成 _apc_pick_for() 当前返回的 warm_cache 结构；
      - 多行请求中一部分命中 session、一部分命中 APC、一部分冷启动时如何合并；
      - session cache 的 extra_hash、prefix length 如何进入 apc_meta；
      - session cache 命中后是否仍然允许同一行走 APC；
      - session snapshot 的不同 cache layout 是否能被现有 merge_rows() 合并。

    如果只在 stream_generate() 路径接入，B 可以先实现；如果要支持线上 continuous batching，必须补一份类似 SessionCachePick 的中间结构和逐行合并规则。否则“查 B → 命中后构造 prompt_cache”仍然只是概念描述。

  ### 中风险

  - 方案 B 的“磁盘命中”语义前后矛盾。
    B4 说下次请求会从磁盘加载并回填 RAM（session-cache.md:219 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:219)、session-cache.md:230 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-
    cache.md:230)；但 B5 又规定加载在工作线程做，加载期间 lookup 返回 reason=on_disk，本轮按 miss 冷跑、不阻塞（session-cache.md:347 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:347)）。

    这两种行为需要二选一：
      1. 同步命中模型：请求等待磁盘加载，加载成功后本轮命中；实现简单，但会增加首 token 延迟。
      2. 异步预热模型：本轮明确 miss，后台加载供下一轮使用；那 S8 就不能写成“请求时从盘加载并回填”，而应验证“下一次请求命中”。

    目前 S8 的验收条件和 B5 的运行时语义不一致。

  - B 的全局 headroom 方案还缺少并发安全边界。
    文档现在正确地承认独立淘汰域不能等于独立设备内存，并要求 session cache 复用 APC 的 headroom（session-cache.md:207 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:207)）。但 session cache 和 APC 分属两个管
    理器，单独查询 headroom 后再各自保留，无法防止竞态：

    session cache 检查 headroom -> APC 同时 restore
    APC 检查 headroom -> session cache 同时回填

    两边都可能认为空间足够，最终超过设备上限。至少需要一个共享的 cache admission lock / reservation API，或者明确 B 的 RAM 快照不计入设备常驻、每次使用前都从磁盘恢复。当前 S12 只能验证串行场景，不能证明并发安全。

  - B 的 fork 对“parent 在磁盘上”仍然是功能降级，而不是 fork。
    文档规定 parent 已落盘时 fork 返回 0 并冷跑（session-cache.md:168 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:168)）。这可以接受，但它与“session cache 支持磁盘恢复”的目标不完全一致：磁盘中的 parent 明
    明存在，fork 却不能使用。建议把它明确命名为 MVP 限制，或者让 fork() 允许等待/显式加载 parent；否则 S3 只覆盖 RAM parent，不能覆盖完整 session 生命周期。

  ### 低风险

  - B2 的 prompt-only 结论现在有解释，但需要把前提写成不变量。
    文档认为 len - guard 的输入 prompt 会成为下一轮请求的前缀，因此不需要保存生成出的 assistant token（session-cache.md:137 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:137)）。这个判断依赖客户端下一轮请求
    始终保留上一轮 user 消息，并由模板重新插入 assistant 消息。如果某个客户端裁剪、重排或省略历史，lookup 必须安全 miss。建议在 S1 增加“历史消息重排/删除时 miss，不得复用”的断言。

  - B1b 已经标成独立优化项，这个调整是正确的。
    它不应阻塞 session cache MVP，最好从 B 的主交付清单中移到单独任务，避免工具排序和 session 存储在同一个 PR 中耦合。

  当前结论：

  - A 的核心设计可行，但必须补齐 continuous batching 的 cut 来源传递。
  - B 的独立 session cache 方向可行，但 continuous batching 的 cache 合并方式仍是实现阻塞点。
  - B 的磁盘恢复需要在“本轮等待命中”和“后台预热、下轮命中”之间明确选择。
  - 两个方案现在可以独立开发；如果想降低风险，建议先只实现 B 的普通 stream_generate() + 内存 MVP，暂时把 continuous batching、磁盘恢复和 A 的 semantic cut 分开验收。 关于你不认同的哪一条: 我认同这条建议，而且基于当前代码应该撤回我之前“通常应保存 prompt + 生成回复”的那条意见。那是从通用对话缓存语义出发的判断，没有充分考虑这个项目当前 cache layout 的实际约束。

  关键证据就是 _prefix_cache_trim_amount()：

  n_drop = max(0, cached_len - prefix_len)
  if n_drop and not all(
      c.is_trimmable() and _cache_fully_retained(c)
      for c in kv_cache
  ):
      return None

  代码明确规定：如果缓存比下一轮可复用的前缀更长，就必须回退；而只要有不可 trim 的 cache，就直接冷 prefill。对包含 ArraysCache 的 hybrid 模型，这种回退是常态。因此保存“prompt + 本轮生成结果”并不等于下一轮可用，反而很可能保存了一个无法裁剪
  的快照。

  len - guard 的选择在这个前提下是合理的：

  - 快照长度不超过下一轮需要复用的安全边界；
  - 命中时通常 n_drop == 0；
  - 不需要依赖所有 cache 类型都支持 trim；
  - 生成中途断开不会破坏已落定的 prompt checkpoint；
  - 不需要等待 assistant 回复、tool call 或完整生成结束。

  所以这部分可以作为方案 B 的正式设计结论，而不是临时妥协。

  不过我建议把表述再收紧三点。

  第一，“下一轮共享前缀必然短于 cached_len”最好改成“在当前模型和请求模板下，下一轮可复用前缀通常短于包含生成结果的缓存；只要需要回退，就必须满足所有 cache 可 trim，因此对当前 hybrid 模型不安全”。“必然”是针对特定客户端/模板观察到的事实，不
  宜写成所有调用方式都成立的普遍不变量。如果某个客户端把完整 assistant 历史原样提交，理论上可能出现不需要回退的情况，但方案不应依赖这种行为。

  第二，len - guard 不是任意的 prompt 长度，而是 APC 已定义的安全 checkpoint 边界。应明确：

  snapshot_tokens = full_prompt_tokens[:len(full_prompt_tokens) - guard_tokens]

  并且只有这个长度大于最小缓存阈值、且对应的 cache state 已经完成 prefill 时才保存。短 prompt 或中途尚未到达该边界时，不应生成 session snapshot。

  第三，“断开、超时、tool call 中断都不需要特殊处理”要稍微限定一下：只对“已经完成并成功写入 len - guard checkpoint”的请求成立。如果请求在到达该 prefill 边界之前就被取消，就没有快照；如果写盘或复制失败，也不能把它当作已保存。也就是说，不需
  要生成完成状态机，但仍需要处理 checkpoint 写入失败。

  我会把这段结论改写成：

  > 当前模型包含不可回退的 ArraysCache，因此保存超过下一轮安全共享前缀的状态没有复用价值：一旦需要丢弃尾部 token，_prefix_cache_trim_amount() 会拒绝复用并触发冷 prefill。Session cache 只保存 prefill 达到 len - guard_tokens 时的 prompt
  > checkpoint，确保命中时无需 trim。该 checkpoint 与生成结果无关，因此生成中断、tool call 或客户端断开不会影响已完成 checkpoint 的可用性；只有在 checkpoint 尚未达到或写入失败时不产生快照。

  所以这条建议本身我接受，也建议从 B 的设计中保留。它同时意味着我之前提出的“B 需要定义生成完成时是否提交”不再是有效阻塞项，应该从 review findings 中移除。剩下真正需要解决的重点仍是 continuous batching 如何接入 session hit，以及 A 中
  semantic cut 来源如何在 ar.py 存储时传递下去。
- 15:21 [ZH] B：这条不成立，checkpoint 模式下已经能接

  # apc_coordinator.py:148  merge_rows
  if self.is_checkpoint:
      row_caches = [pick["warm_cache"] if pick is not None else self.fresh_cache()
                    for pick in picks]
      return make_warm_batch_exact_cache_multi(row_caches, prefix_lens, ...)

  checkpoint 模式下一个 pick 就是 {"warm_cache": <整份 prompt_cache>, "prefix_len": n} —— 正是 session snapshot 的形状。make_warm_batch_exact_cache_multi（apc.py:4382）逐 entry 调 _merge_exact_cache_entries(rows, prefix_lens)，prefix_lens
  是逐行的，冷行由 fresh_cache() 补位。混合批次现成就处理。

  所以不需要 SessionCachePick，不需要新的合并规则。

  但你这条问出了一个真的限制，我写进 README 了：本方案只支持 is_checkpoint 为真的模型。block 模式下 pick 是块形状，make_warm_batch_kv_cache_multi() 收不了整份快照 —— 那种模型上 session cache 直接禁用，不做降级实现（S18 卡）。我们在用的
  Ornith 就是 checkpoint 模式。 这对吗
- 15:22 [ZH] 方案他改了,再review一下
- 15:28 [ZH] 还有一个高风险矛盾，另外两个中风险缺口。

  ### 高风险

  - B 的全局 admission lock 方案会破坏“独立淘汰域”。
    文档一方面要求 session cache 与 APC 独立淘汰（session-cache.md:238 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:238)），另一方面又规定 session store/回填要在 APC manager 的 self.lock 下调用
    _make_room()（session-cache.md:254 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:254)）。

    但当前 _make_room() 的职责不是单纯“检查锁”，而是实际淘汰 APC 的 _exact_cache 或 block pool。session cache 调它，会让 session 的内存准入直接淘汰 APC 条目，违反“抖音入库不能挤掉 agent APC/session”的独立性目标。

    这里应该拆成两个概念：

    shared admission lock       只保护全局内存准入临界区
    session_cache._make_room()  只淘汰 session 自己的 RAM 条目
    apc_manager._make_room()    只淘汰 APC 自己的条目

    两者可以共享一把新的进程级 CacheAdmission 锁，或者共享一个 reservation API，但不能让 session cache 调 APC 的 _make_room()。S12/S12b 需要验证的是共享 reservation 下的总驻留量，不是调用 APC 淘汰函数。

  ### 中风险

  - B 的 stream_generate 接入点和“prefill 中途保存”的机制还没有完全对齐。
    文档写 stream 路径在 dispatch.py:933 查、在 dispatch.py:1136 存（session-cache.md:310 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:310)），但 1136 当前是生成结束后的 prompt_cache_state.update()。B 又明
    确要求 session snapshot 在 len - guard 的 prefill checkpoint 回调中落盘（session-cache.md:339 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:339)。

    这两者不是同一个时机。需要明确 stream 路径也要在 generate_step 的 checkpoint callback 中调用 SessionCache.store()；1136 只能作为已有 PromptCacheState 的参考，不能作为 session snapshot 的实际提交点。否则短请求、生成中断、或
    prompt_cache_state 未启用时，B 的快照可能根本不会保存。

  - B 的 session hit 在 continuous batching 中虽然可以复用现有 pick 形状，但接入位置还应写成“替换 _apc_pick_for 的 lookup 结果”，而不是只说在它开头查询。
    当前 _build_mixed_prompt_batch() 会统一执行：

    picks = [self._apc_pick_for(s) for s in sequences]

    因此实现必须保证：

    _apc_pick_for:
        session hit -> 返回 checkpoint-shaped pick
        session miss -> 调用现有 APC lookup

    而不是另加一个旁路结果，之后再尝试拼接。否则 “同一行 session 命中后不再走 APC” 和 S10 的“不回头”无法由现有调用结构自动保证。建议把这一点写成接口契约，并为 _apc_pick_for 增加混合测试：session hit、APC hit、cold 三行同时存在。

  ### 另外建议补一处

  - session 文件名需要明确不可碰撞且不可越界。
    session id 的正则本身不含 /，这一点足够防止最直接的路径穿越；但 tenant 没有同样的格式约束，且不同 (tenant, session_id) 不能只靠字符串拼接文件名。建议文件名使用编码或固定 hash，例如 sha256(tenant + "\0" + session_id)，原始 id 只放
    metadata，避免碰撞和特殊字符问题。

  当前结论：

  - A：semantic cut 的来源传递已经补到两条路径，之前的主要缺口已修复。
  - B：checkpoint-shaped pick、checkpoint-only 限制、磁盘同步加载和 prompt-only snapshot 设计都合理。
  - 必须修改的是全局内存准入：共享锁不能等同于调用 APC 的 _make_room()。
  - 还应把 stream session store 从生成结束点移到真实的 prefill checkpoint callback，并把 _apc_pick_for 的替换契约写死。
- 15:31 [ZH] 再review一下
- 15:33 [ZH] 这版已经把上次两个主要问题修掉了：

  - session cache 不再调用 APC 的 _make_room()，而是独立 CacheAdmission + 各自 shrink()；
  - stream 路径明确在 exact_checkpoint 回调保存，不再误用生成结束后的 prompt_cache_state.update()；
  - continuous batching 也明确要求在 _apc_pick_for() 内部返回 checkpoint-shaped pick；
  - checkpoint-only 限制、三行混合批测试、磁盘 fork 和并发准入测试都补上了。

  不过还有一个高风险矛盾需要修正。

  ### 高风险

  - S13c“APC 关闭时 session cache 仍工作”与当前 continuous batching 入口条件不一致。

    方案 B 明确要求：

    > APC_ENABLED=0 + session cache 开 时，快照仍在 len - guard 落地，下一轮仍命中（S13c）。

    但现有 BatchGenerator 路径是由 APC 状态驱动的：
      - _apc_pick_for() 一开始如果 self.apc_manager is None，直接返回 None；
      - _build_mixed_prompt_batch() 如果 self.apc_manager is None，直接返回 None；
      - _apc_uses_checkpoints() 也通过 coordinator 或 apc_mode == "exact" 判断是否启用 checkpoint。

    所以只按文档中“在 _apc_pick_for() 里加 session lookup”实现，APC 关闭时 session cache 不会自动生效，甚至不会进入 mixed checkpoint batch 流程。S13c 目前不是实现自然得到的行为，而是需要额外改造。

    需要在方案中明确一个选择：
      1. 保留 S13c：把 session cache 视为独立的 checkpoint provider。即使 APC 关闭，BatchGenerator 仍须初始化 checkpoint-compatible cache、执行 session lookup、在 len - guard 触发 session snapshot callback，并允许 session hit 进入
         make_warm_batch_exact_cache_multi()。

      2. 删除 S13c：规定 session cache 依赖 APC 的 checkpoint runtime，只在 APC enabled 且 is_checkpoint 时启用。

    以当前“B 独立于 A”的目标来看，我建议保留 S13c，但必须把“APC disabled 时仍启用 checkpoint execution”写成明确的 runtime 前置条件，而不是只写一条验收用例。

  ### 中风险

  - 实现顺序仍有一处过时引用。

    “实现顺序”第 1 步仍写：

    只接 stream_generate()（dispatch.py:933 / :1136）

    但前文已经明确 :1136 不是 session snapshot 提交点（session-cache.md:340 (/Users/youxun/project/mlx-vlm-main/docs/proposals/2026-09-08-session-cache.md:340)）。这里应改为 dispatch.py:933 查找和 dispatch.py:1043 checkpoint callback 保
    存，否则执行者可能按旧位置实现。

  - CacheAdmission 的 reservation 生命周期还需要写清楚。

    文档已经正确区分了“共享准入锁”和“各自淘汰”，但 admit(who, nbytes) 只描述了准入，没有定义：
      - reservation 何时释放；
      - session hit 取得的 cache 在生成期间是否算已预留；
      - fork 的 clone 过程是否一次预留完整目标大小；
      - admission 成功后 clone/写盘失败如何回滚 reservation；
      - APC 现有 _make_room() 产生的临时 restore/prefill reserve 如何登记到同一本账。

    如果这部分不明确，S12b 可能只验证“进入函数时不超”，但 clone 或 restore 的实际分配仍可能超出 headroom。至少应定义 reserve -> materialize -> commit/release 三阶段协议。

  - B 的“APC 关闭仍保存”对 stream 路径也要单独写清楚。

    dispatch.py 当前只有在 apc_coordinator is not None and is_checkpoint 时才设置 prompt_cache_checkpoint。因此 APC disabled 时，B 不能只复用已有 APC callback；必须由 session cache 自己创建 checkpoint length 和 callback。这个问题与 S13c 相
    同，但 stream 和 continuous batching 两条路径都要分别覆盖。

  ### 结论

  目前两案的核心模型已经合理，之前关于 session pick、batch merge、保存生成结果和 APC _make_room() 的问题都可以关闭。

  剩余真正的阻塞点只有一个：B 是否真的要在 APC disabled 时独立工作。 如果答案是“要”，需要把 session cache 作为独立的 checkpoint runtime provider 接入两条生成路径；如果不想扩大实现范围，就删除 S13c，并把 B 的启用条件收紧为：

  session cache enabled
  AND APC checkpoint runtime available
  AND coordinator.is_checkpoint

  另外修正实现顺序中的 dispatch.py:1136 -> dispatch.py:1043，并补全 reservation 生命周期，方案就可以进入实现阶段。
- 15:36 [EN] continue
- 15:43 [ZH] 2个按钮不用框起来,还是和超链接一样,顺眼一点,code 和前面的文字自己加个空格
- 15:44 [ZH] 再review一下
- 15:47 [ZH] ，四个入口全核过了。

  那个选择：删掉 S13c

  ar.py:2568  _apc_pick_for()             apc_manager is None → return None
  ar.py:2597  _build_mixed_prompt_batch()  "Returns None if APC is disabled"
  ar.py:1915  _apc_uses_checkpoints()      靠 coordinator 或 _apc_mode == "exact"
  dispatch.py:1032                         if apc_coordinator is not None and is_checkpoint

  要让 S13c 成立，BatchGenerator 得在没有 apc_manager 的情况下初始化 checkpoint 兼容的 cache、走 mixed-batch 合并、在 len - guard 触发回调 —— _apc_meta、_apc_prompt_cache_for_store 这些也全都假设 APC 在。等于把 APC 的 checkpoint runtime
  重实现一遍，为一个本项目不跑的配置。

  你倾向保留，我选删。理由是这几轮下来一直在收敛复杂度，而这条是纯粹的范围扩张换一个假设场景。

  但这条约束比脚注严重，我查了：APC_ENABLED 默认是 "0"（apc.py:4937、runtime_config.py:337）。所以写进去了：部署 B 必须同时 APC_ENABLED=1，写进 README，不满足时启动日志明说并禁用 B。S13c 改成"干净禁用"的验收。

  关键是这不损害 A/B 独立性 —— 方案里写清了：

  ▎ B 依赖的是 APC 的 checkpoint runtime（上游既有代码，方案 A 一个字都没改它），不是依赖方案 A 的任何改动。A 没落地 B 照常，B 没落地 A 照常。

  "把 checkpoint runtime 从 apc_manager 上解耦"列为后续路线，并注明不要顺手做。

  reservation 生命周期

  你说得对，只有 admit() 挡不住 —— 真实分配在 clone/读盘那一刻，进函数判一次没用。改成三段：

  h = admission.reserve(who, nbytes)      # 锁内; 不够只调 who.shrink(); 仍不够返回 None
  try:
      obj = materialize()                 # clone / 读盘,真实分配在这里,锁外
      admission.commit(h, actual_nbytes)
  except BaseException:
      admission.release(h); raise         # 取消也回滚

  你那五个问题逐条定死了，其中两个值得单独说：

  - session hit 交出的 cache 算已预留，而且 prefill 的增长也要预留 —— 只算交出那一刻的大小是漏的，用上游 _observe_cache_size() 的 _bytes_per_token 估。S12d 卡。
  - APC 现有的 allocation_bytes / _prefill_reserve_bytes 必须并进同一本账 —— 现在是 APC 私账，不并就是两本账各算各的，共享 headroom 形同虚设。这是本方案对 apc.py 的唯一改动，写明了。

  另加了泄漏防护：h 带创建时间，stats 暴露 outstanding 数与最老年龄 —— 泄漏一个 reservation 等于永久扣掉那么多 headroom，必须看得见。S12c 卡回滚。

  实现顺序

  dispatch.py:1136 → :1043 改了。 他对之前的review的说法
- 15:48 [ZH] 你最终的review结论是什么我怎么给他
- 15:49 [ZH] 你前面的那2个风险呢
- 15:51 [ZH] 详细点吧
- 15:53 [ZH] 现在还剩两个实现级风险。这两个不再是架构阻塞，但实现时必须明确，否则容易出现内存统计错误或并发问题。

  ### 一、CacheAdmission 可能重复计算实际内存

  方案现在使用：

  reserve -> materialize -> commit / release

  这个生命周期是对的，但要先明确 admission 账本记录的到底是什么。

  假设 session cache 要加载一个 492 MB 的 snapshot：

  h = admission.reserve(who, 492 * MB)
  obj = materialize()
  admission.commit(h, actual_nbytes)

  这里有两种可能的 headroom 语义。

  第一种是 device_headroom() 读取真实的设备/MLX当前占用，例如 Metal working set 或 MLX allocator 状态。这样在 materialize() 完成后，新增的 492 MB 已经反映在真实内存指标里。

  这种情况下，CacheAdmission 最好只记录尚未反映到实时指标里的 reservation：

  reserve:
      记录临时预留，防止并发任务重复使用同一空间

  materialize:
      实际分配，真实内存指标增加

  commit:
      删除 outstanding reservation
      不再额外扣一份 committed bytes

  如果 commit() 同时把 492 MB 加入一个 committed ledger，而 device_headroom() 又已经把这 492 MB 计算进当前占用，那么下一次准入会把同一份内存扣两次。结果是：

  - session cache 过早写盘；
  - APC restore 被错误拒绝；
  - 可用 headroom 越运行越小；
  - 看起来像内存泄漏，但实际是账本重复扣减。

  第二种是 device_headroom() 只提供一个外部可用上限，并不可靠地包含 managed cache 的占用。这时才需要维护：

  committed APC bytes
  + committed session bytes
  + outstanding reservations

  因此实现前应该明确采用哪一种模型。结合当前文档里 device_headroom() 来自 Metal working set、空闲内存和 MLX allocator 的描述，更接近第一种：实时指标已经包含真实分配，admission 主要追踪 outstanding reservation。

  建议把规则写成：

  CacheAdmission 的 reservation 只防止并发 materialize 超卖；
  commit 负责把 reservation 转换为“已被实时 headroom 反映的分配”，
  不得再次从同一 headroom 指标中重复扣除 committed bytes。

  无论最后采用哪种实现，都应该增加一个测试：

  连续执行 reserve -> materialize -> commit；
  每次 commit 后再次 reserve；
  可用空间不应因为同一份已提交对象被重复计账而异常下降。

  这属于 S12b/S12d 的一部分。

  ### 二、全局 admission 锁内不能做长时间磁盘 I/O 或 shrink

  方案现在要求共享一把 admission lock，这是为了避免以下竞态：

  线程 A：检查 headroom，认为还有空间
  线程 B：同时检查 headroom，也认为还有空间
  A/B 同时 materialize，最终超出设备上限

  共享准入锁可以解决这个问题，但不能把所有实际操作都放在锁里。

  例如 session cache 需要把一份 492 MB 的条目从 RAM 降级到磁盘。如果在 admission lock 内同步执行：

  acquire global lock
  写 492 MB safetensors
  释放 global lock

  那么在写盘期间：

  - APC restore 会被阻塞；
  - 其他 session lookup 会被阻塞；
  - 新请求不能完成 admission；
  - 如果写盘触发 MLX/系统内存变化，锁持有时间会变得不可预测；
  - 如果 shrink() 内部又申请内存或尝试 reserve，可能形成锁顺序问题。

  因此推荐把流程拆成锁内和锁外两部分。

  锁内只负责：

  检查 reservation
  决定是否需要 shrink
  从本域 LRU 中摘除待降级条目
  更新 reservation/entry 状态

  锁外执行：

  clone
  safetensors 写盘
  safetensors 读盘
  真实 materialize

  完成后重新进入锁：

  成功：
      commit reservation
      更新 RAM/disk 状态

  失败或取消：
      release reservation
      恢复索引状态或确认条目已经安全降级

  可以采用这样的状态：

  resident -> spilling -> on_disk
  resident -> loading -> resident
  resident -> evicted

  其中 spilling/loading 状态要避免同一个 session 被并发重复读写。

  需要特别注意：如果条目已经从 RAM LRU 摘除、但磁盘写入还没完成，不能立刻把这部分空间当成完全释放，除非对应 tensor 引用已经释放且 reservation ledger 已正确更新。否则会出现“索引认为已释放、对象实际上仍在内存”的短暂超卖。

  建议增加两个测试：

  1. shrink 写盘期间并发 APC restore：
     APC 不应被 session 的磁盘 I/O 长时间阻塞。

  2. shrink/materialize 被取消或抛异常：
     outstanding reservation 最终归零；
     session 条目不会同时出现在“可用 RAM”和“未完成写盘”两个状态。

  ———

  你可以给他的最终结论写成下面这样：

  > 这版方案通过，可以进入实现阶段。
  >
  > 前面提出的架构问题已经解决：
  >
  > - B 改为独立 session storage，不再与 APC 共用条目或 key；
  > - fork 使用独立槽位，不会覆盖 parent；
  > - checkpoint 模式下 session snapshot 可以直接复用现有 APC pick 和 make_warm_batch_exact_cache_multi()；
  > - block 模式明确禁用 B；
  > - 删除“APC 关闭时 B 仍工作”的 S13c 是正确的，B 只依赖已有 APC checkpoint runtime，不依赖方案 A；
  > - stream 路径改为在 dispatch.py:1043 的 prefill checkpoint callback 保存；
  > - session 不再调用 APC _make_room()，改为独立淘汰域 + 共享准入控制；
  > - reservation 已定义为 reserve -> materialize -> commit/release，并覆盖异常回滚和并发场景。
  >
  > 现在只剩两个实现级注意事项，不阻塞方案通过：
  >
  > 1. 明确 CacheAdmission 的账本语义，避免 device_headroom() 已包含实际 materialize 分配时，commit() 又重复扣一份 committed bytes。
  > 2. admission 全局锁内不要做 safetensors 读写、clone 或长时间 shrink。锁内只做 reservation 和索引状态变更，真实 I/O/materialize 放锁外，完成后 commit，失败或取消时 release。
  >
  > 这两点属于实现约束，不需要再改变方案架构。建议按纯内存 MVP、fork、continuous batching、admission、磁盘恢复的顺序实现；每一步通过对应 Gate 后再进入下一步。
- 15:53 [ZH] 现在还剩两个实现级风险。这两个不再是架构阻塞，但实现时必须明确，否则容易出现内存统计错误或并发问题。

  ### 一、CacheAdmission 可能重复计算实际内存

  方案现在使用：

  reserve -> materialize -> commit / release

  这个生命周期是对的，但要先明确 admission 账本记录的到底是什么。

  假设 session cache 要加载一个 492 MB 的 snapshot：

  h = admission.reserve(who, 492 * MB)
  obj = materialize()
  admission.commit(h, actual_nbytes)

  这里有两种可能的 headroom 语义。

  第一种是 device_headroom() 读取真实的设备/MLX当前占用，例如 Metal working set 或 MLX allocator 状态。这样在 materialize() 完成后，新增的 492 MB 已经反映在真实内存指标里。

  这种情况下，CacheAdmission 最好只记录尚未反映到实时指标里的 reservation：

  reserve:
      记录临时预留，防止并发任务重复使用同一空间

  materialize:
      实际分配，真实内存指标增加

  commit:
      删除 outstanding reservation
      不再额外扣一份 committed bytes

  如果 commit() 同时把 492 MB 加入一个 committed ledger，而 device_headroom() 又已经把这 492 MB 计算进当前占用，那么下一次准入会把同一份内存扣两次。结果是：

  - session cache 过早写盘；
  - APC restore 被错误拒绝；
  - 可用 headroom 越运行越小；
  - 看起来像内存泄漏，但实际是账本重复扣减。

  第二种是 device_headroom() 只提供一个外部可用上限，并不可靠地包含 managed cache 的占用。这时才需要维护：

  committed APC bytes
  + committed session bytes
  + outstanding reservations

  因此实现前应该明确采用哪一种模型。结合当前文档里 device_headroom() 来自 Metal working set、空闲内存和 MLX allocator 的描述，更接近第一种：实时指标已经包含真实分配，admission 主要追踪 outstanding reservation。

  建议把规则写成：

  CacheAdmission 的 reservation 只防止并发 materialize 超卖；
  commit 负责把 reservation 转换为“已被实时 headroom 反映的分配”，
  不得再次从同一 headroom 指标中重复扣除 committed bytes。

  无论最后采用哪种实现，都应该增加一个测试：

  连续执行 reserve -> materialize -> commit；
  每次 commit 后再次 reserve；
  可用空间不应因为同一份已提交对象被重复计账而异常下降。

  这属于 S12b/S12d 的一部分。

  ### 二、全局 admission 锁内不能做长时间磁盘 I/O 或 shrink

  方案现在要求共享一把 admission lock，这是为了避免以下竞态：

  线程 A：检查 headroom，认为还有空间
  线程 B：同时检查 headroom，也认为还有空间
  A/B 同时 materialize，最终超出设备上限

  共享准入锁可以解决这个问题，但不能把所有实际操作都放在锁里。

  例如 session cache 需要把一份 492 MB 的条目从 RAM 降级到磁盘。如果在 admission lock 内同步执行：

  acquire global lock
  写 492 MB safetensors
  释放 global lock

  那么在写盘期间：

  - APC restore 会被阻塞；
  - 其他 session lookup 会被阻塞；
  - 新请求不能完成 admission；
  - 如果写盘触发 MLX/系统内存变化，锁持有时间会变得不可预测；
  - 如果 shrink() 内部又申请内存或尝试 reserve，可能形成锁顺序问题。

  因此推荐把流程拆成锁内和锁外两部分。

  锁内只负责：

  检查 reservation
  决定是否需要 shrink
  从本域 LRU 中摘除待降级条目
  更新 reservation/entry 状态

  锁外执行：

  clone
  safetensors 写盘
  safetensors 读盘
  真实 materialize

  完成后重新进入锁：

  成功：
      commit reservation
      更新 RAM/disk 状态

  失败或取消：
      release reservation
      恢复索引状态或确认条目已经安全降级

  可以采用这样的状态：

  resident -> spilling -> on_disk
  resident -> loading -> resident
  resident -> evicted

  其中 spilling/loading 状态要避免同一个 session 被并发重复读写。

  需要特别注意：如果条目已经从 RAM LRU 摘除、但磁盘写入还没完成，不能立刻把这部分空间当成完全释放，除非对应 tensor 引用已经释放且 reservation ledger 已正确更新。否则会出现“索引认为已释放、对象实际上仍在内存”的短暂超卖。

  建议增加两个测试：

  1. shrink 写盘期间并发 APC restore：
     APC 不应被 session 的磁盘 I/O 长时间阻塞。

  2. shrink/materialize 被取消或抛异常：
     outstanding reservation 最终归零；
     session 条目不会同时出现在“可用 RAM”和“未完成写盘”两个状态。

  ———

  你可以给他的最终结论写成下面这样：

  > 这版方案通过，可以进入实现阶段。
  >
  > 前面提出的架构问题已经解决：
  >
  > - B 改为独立 session storage，不再与 APC 共用条目或 key；
  > - fork 使用独立槽位，不会覆盖 parent；
  > - checkpoint 模式下 session snapshot 可以直接复用现有 APC pick 和 make_warm_batch_exact_cache_multi()；
  > - block 模式明确禁用 B；
  > - 删除“APC 关闭时 B 仍工作”的 S13c 是正确的，B 只依赖已有 APC checkpoint runtime，不依赖方案 A；
  > - stream 路径改为在 dispatch.py:1043 的 prefill checkpoint callback 保存；
  > - session 不再调用 APC _make_room()，改为独立淘汰域 + 共享准入控制；
  > - reservation 已定义为 reserve -> materialize -> commit/release，并覆盖异常回滚和并发场景。
  >
  > 现在只剩两个实现级注意事项，不阻塞方案通过：
  >
  > 1. 明确 CacheAdmission 的账本语义，避免 device_headroom() 已包含实际 materialize 分配时，commit() 又重复扣一份 committed bytes。
  > 2. admission 全局锁内不要做 safetensors 读写、clone 或长时间 shrink。锁内只做 reservation 和索引状态变更，真实 I/O/materialize 放锁外，完成后 commit，失败或取消时 release。
  >
  > 这两点属于实现约束，不需要再改变方案架构。建议按纯内存 MVP、fork、continuous batching、admission、磁盘恢复的顺序实现；每一步通过对应 Gate 后再进入下一步。 实现之前还是补全一下把
- 15:59 [ZH] 最后在review,是不是可以开工了
- 16:01 [ZH] 开工前只建议做两处文档清理，不影响 go/no-go：

  1. A 的交付清单仍写“G1–G8”，但文档已经增加到 G12，应改为 G1–G12，并明确 G11/G12 也是必验项。
  2. B 的实现顺序第 4 步只列 S6 S12 S12b，但现在新增了 reservation 相关的 S12c/S12d/S12e，应改成：

     S6 S12 S12b S12c S12d S12e

  实现顺序建议照方案执行：

  1. SessionCache 纯内存 MVP
  2. fork 和 parent/child 隔离
  3. continuous batching 接入
  4. CacheAdmission 和 reservation
  5. safetensors 磁盘、重启、损坏恢复

  第一阶段不需要等方案 A 完成；B 可以先基于现有 APC stride checkpoint 工作。A 也可以单独开发。真正开始实现时，优先把 S12c reservation 回滚和 S13b 三行混合 batch 写成回归测试，这两项最能验证生命周期和接入是否完整。
