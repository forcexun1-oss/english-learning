# English Learning Log — 2026-07-20

- 08:46 [ZH] 把测试路线追加到 CU1_RESULTS.md, 下载 Qwen3-30B-A3B Q4_K_M
- 08:56 [EN] <task-notification>
<task-id>bl16bf8hy</task-id>
<tool-use-id>toolu_01TPsBqHRbVYMWKuGmFFGJ2T</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-llama-cpp/7dc0bdb2-3a74-4899-a5d1-2aa7c4ec77be/tasks/bl16bf8hy.output</output-file>
<status>completed</status>
<summary>Background command "Download Qwen3-30B-A3B Q4_K_M from hf-mirror without proxy (background)" completed (exit code 0)</summary>
</task-notification>
- 17:48 [ZH] 4G显存, 要在机器上测试吗, 都准备好了吗
- 17:49 [ZH] 4G显存, 要在机器上测试吗, 都准备好了吗
- 19:48 [ZH] 17g内存还是显存?
- 19:48 [ZH] 做三级缓存
- 19:57 [ZH] 让coda过编译, 然后都实现完, 交给subagent使用sonnet模型去做
- 20:28 [EN] <task-notification>
<task-id>bi1egdpos</task-id>
<tool-use-id>toolu_01HaiMEXBoVgBnJEMBiXC65D</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-llama-cpp/7dc0bdb2-3a74-4899-a5d1-2aa7c4ec77be/tasks/bi1egdpos.output</output-file>
<status>completed</status>
<summary>Background command "Verify CUDA image pull is transferring via proxy" completed (exit code 0)</summary>
</task-notification>
- 21:05 [EN] <task-notification>
<task-id>a0ab9a8a952901471</task-id>
<tool-use-id>toolu_0117recvisRwLFuJUspwgctV</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-llama-cpp/7dc0bdb2-3a74-4899-a5d1-2aa7c4ec77be/tasks/a0ab9a8a952901471.output</output-file>
<status>completed</status>
<summary>Agent "CU1 compile-fix + CU2 implementation" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Everything is in order. Here's the summary of the work.

## Summary

**Part 1 — llama-cli compile+link gate: PASSED.**

Portability fix (source change): `common/expert-cache.cpp:9` — added `#include &lt;climits&gt;` so `SSIZE_MAX` (used at line ~696, `common_expert_cache_pread_one`) resolves on Linux/glibc. No logic changed.

Environment-only fix (container, not repo): the sandbox has no NVIDIA driver, so `libcuda.so.1` (needed by `ggml-cuda`'s VMM code) wasn't findable at final-executable link time even though `libggml-cuda.so` itself links fine. Symlinked the CUDA toolkit's stub (`.../targets/sbsa-linux/lib/stubs/libcuda.so`) to `libcuda.so.1` under an `ldconfig`-registered path inside the container and re-ran `ldconfig`. No repo files touched; a real machine with an NVIDIA driver would never hit this.

Verified via `ninja -t clean` + full rebuild: `bin/llama-cli` links (`72472` bytes), `--help` runs and exits 0.

**Part 2 — CU2 three-level expert cache: Phases A–C implemented and compiling; Phase D deferred.**

Files touched/added (detailed file-by-file breakdown, including the deliberate deviation of extending `ggml/include/ggml-cpu.h` + `ggml/src/ggml.c` with a new `ggml_moe_gpu_slab_set_l2_fns` registration surface — required by the existing layering CU1 established, not listed in the proposal's file table but necessary for the same reason the other 14 slab ops go through it):
- `common/expert-cache.h`, `common/expert-cache.cpp` — L2 fields/state, rewritten `gpu_slab_stage_entries()` three-way miss path, `print_stats()` L2 line
- `common/expert-cache-backend.{h,cpp}` — 3 new ops (`l2_arena_alloc`/`l2_arena_free`/`l2_copy`)
- `ggml/include/ggml-cpu.h`, `ggml/src/ggml.c` — backend-agnostic registration for the 3 new ops
- `ggml/src/ggml-cuda/expert-slab.cu` — CUDA implementation (`cudaMallocHost`/`cudaFreeHost`/`cudaMemcpyAsync` HostToHost on the existing MoE copy stream)
- `ggml/src/ggml-metal/*` — **untouched** (null-safe fallback to portable `malloc`/`memcpy` in `common/` covers Metal automatically)
- `common/common.h`, `common/arg.cpp`, `tools/cli/cli.cpp` — `--expert-cache-ram-budget` / `LLAMA_EXPERT_CACHE_RAM_BUDGET`
- `docs/ds4/CU2_RESULTS.md` (new), `docs/ds4/README.md` (index links)

`ram_budget_bytes == 0` guarantee: the per-entry loop in `gpu_slab_stage_entries()` branches `if (ram_budget_bytes == 0)` and calls the exact original `common_expert_cache_gpu_slab_prepare_pread_tasks(...)` with the exact original surrounding rollback code; the L2 arena setup in `init()` and the L2 stats block in `print_stats()` are wrapped in `if (ram_budget_bytes != 0)`. Full reasoning is in `CU2_RESULTS.md` §3.

Deferred: Phase D (true async L2→L1 copy-compute overlap, prefetch-ahead warming L2). The copy currently blocks (`cudaStreamSynchronize`) rather than overlapping — documented as a conscious scope cut, not a hidden gap.

**Runtime/GPU verification was explicitly not performed** — no GPU in this environment; everything beyond compile+link is a structural/code-reading argument, stated as such throughout `docs/ds4/CU2_RESULTS.md` (see §5 "Genuinely unverified").

Key files: `/Users/youxun/project/llama.cpp/common/expert-cache.{h,cpp}`, `/Users/youxun/project/llama.cpp/common/expert-cache-backend.{h,cpp}`, `/Users/youxun/project/llama.cpp/ggml/src/ggml-cuda/expert-slab.{cu,cuh}`, `/Users/youxun/project/llama.cpp/ggml/include/ggml-cpu.h`, `/Users/youxun/project/llama.cpp/ggml/src/ggml.c`, `/Users/youxun/project/llama.cpp/tools/cli/cli.cpp`, `/Users/youxun/project/llama.cpp/common/arg.cpp`, `/Users/youxun/project/llama.cpp/common/common.h`, `/Users/youxun/project/llama.cpp/docs/ds4/CU2_RESULTS.md`.</result>
<usage><subagent_tokens>287944</subagent_tokens><tool_uses>117</tool_uses><duration_ms>1548879</duration_ms></usage>
</task-notification>
