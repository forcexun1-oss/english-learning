First, investigate why it got shorter. If it can't be shortened, how did it become shorter? Also, regarding the caching solution mentioned earlier — there's no room for modification. Fix whatever is wrong: if the server is wrong, fix the server; if the client is wrong, fix the client.
