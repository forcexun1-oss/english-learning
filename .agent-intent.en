No PushNotification needed. These are routine APC_TRACE store events (mode=exact, cache write succeeded) — not cache hit/miss results. Benign output; keep monitoring and only alert when an actual hit/miss line appears.
