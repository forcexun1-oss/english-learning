Write into SSOT:
1. HTTP/uniffi adapters are decoupled from the capability crate — agree to write it in as a target direction (implementation is a later concern)?
2. PyO3 converges to trait backend — write into the design now, or just note it as a TODO? How to phrase this?
