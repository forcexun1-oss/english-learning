I want the best on-device inference engine — not a model, not a result cobbled together by tweaking other variables. Also, we're now targeting the Gemma model, which needs MoE and exceeds memory capacity — that's the goal. So first select a suitable inference engine, then modify it.
