Take over the task of adding image input support to the agent panel. First review the code — if it's too messy, refactor it. Also, drop the drag-and-drop feature for images. When selecting an image, the file picker dialog pops up behind the window. The input box size is now wrong — it's too big. And command + v doesn't paste the image into the input box.
