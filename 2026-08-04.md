# English Learning Log — 2026-08-04

- 00:16 [EN] <task-notification>
<task-id>a995489be9b3be3d9</task-id>
<tool-use-id>toolu_01WdKqdxz5mWBMFE5a3eSvDb</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/a995489be9b3be3d9.output</output-file>
<status>failed</status>
<summary>Agent "Task 7: skip empty + self-proving log" failed: Agent terminated early due to an API error: API Error: Connection closed mid-response. The response above may be incomplete.</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
</task-notification>
- 08:52 [EN] continue
- 08:57 [EN] <task-notification>
<task-id>a47b287f6a61b07ea</task-id>
<tool-use-id>toolu_01Mwid3Yr2gCqXmVuxE9TV2m</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/a47b287f6a61b07ea.output</output-file>
<status>completed</status>
<summary>Agent "Task 7 Step 2-6: implement" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE.

Commit SHA: `44e246567b62efca9f5e2a3cfef74867a51a8c02` (branch `feat/offline-session-digest`, only `mac-assistant/src/session_digest.rs` changed).

Test summary: `cargo test -p mac-assistant` → 161 passed, 0 failed (155 baseline + 6 new tests, all GREEN).

Concerns (full detail in report):
1. `write_marker` in the brief's Step 3 snippet takes 4 args, but the real signature already has a 5th `gave_up` param (added in a later commit than the brief snippet). Used `write_marker(&amp;markers_dir, &amp;stem, true, 0, false)` to match the actual signature — necessary for compilation, consistent with existing call sites.
2. The brief never specifies what path to pass as `projects_root`. I inspected the real pi home on this machine and found `pi_home/projects-memory/&lt;project&gt;/MEMORY.md` is the actual per-project store, so `run_digest_pass` now passes `Some(pi_home.join("projects-memory"))`.
3. Did not attempt a screen lock or real digest pass, per your instructions — that verification is yours.
4. No objection to the brief's markdown-entries-over-sqlite rationale.

Full report: `/Users/youxun/project/mac-assistant/.superpowers/sdd/2026-08-03-offline-session-digest/task-7-report.md`</result>
<usage><subagent_tokens>96019</subagent_tokens><tool_uses>31</tool_uses><duration_ms>266722</duration_ms></usage>
</task-notification>
- 09:04 [ZH] <task-notification>
<task-id>ab72c2267d87c1802</task-id>
<tool-use-id>toolu_01YTthRY7fFf8yhjcF9fbvTy</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/ab72c2267d87c1802.output</output-file>
<status>completed</status>
<summary>Agent "Review Task 7" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Code Review Summary — Task 7 (offline-session-digest)

**Files Reviewed:** 1 (`mac-assistant/src/session_digest.rs`), plus brief and report cross-checked
**Verification method:** full file read (903 lines), `cargo check -p mac-assistant` (clean, no warnings), `grep` for residual `"digested"` log usage, live trace of `pending_sessions`/`write_marker`/`activate_process`/`reset()`/`warmup_spare()` control flow, and a sub-agent fact-check against `pi_agent_rust`'s session-file implementation.

### (a) Spec compliance verdict: **PASS with one gap the brief didn't anticipate**

Everything the brief's steps 1–6 literally asked for is present and correctly wired:

- `count_messages`/`memory_entry_total` match the brief's code verbatim (`session_digest.rs:291-320`).
- The `None`/`Some(0)`/`Some(_)` match at `session_digest.rs:384-397` is exactly right: only `Some(0)` hits `continue`; `None` falls through to the full pipeline unchanged (verified — no path lets `None` reach the skip branch).
- The converse holds too: a genuine `Some(0)` writes `write_marker(..., true, 0, false)`, and `pending_sessions` (`:63-78`) dequeues purely on marker-file existence, so it correctly leaves the queue.
- `digested` is gone as a log word. `grep -n '"digested"'` on the committed file shows only the `digested/` marker-directory name, the `digestedAt` JSON field, and comments — matches the implementer's claim.
- Delta sampling window is correct: `mem_before` is sampled immediately before `Instant::now()`/spawn, `mem_after` only on the success arm, right after `wait_for_exit_or_kill_with_cancel` returns — brackets the child's actual run, not more or less.
- Marker success is gated only on `s.success()`; the `no_change`/`delta` computation never influences `write_marker`'s `ok` argument. Confirmed no coupling.
- Both named deviations check out: `write_marker`'s 5th `gave_up` arg is `false` on the new skip path (`:388`), consistent with "a skip is not a give-up"; `projects_root = pi_home.join("projects-memory")` is a reasonable, well-justified choice given the brief never specified it.
- Scope respected: only `session_digest.rs` changed; nothing under `sessions/` is deleted (only the `.tmp/` copy is removed).
- 1–3 message sessions are untouched (`Some(_) =&gt; {}`), no new threshold introduced.

**The gap:** the brief's own framing ("empty sessions are steady-state artifacts of the warm pool") is not actually true for every `Some(0)` file this code will encounter, and the brief didn't ask the implementer to check this — see CRITICAL-1 below. The diff does exactly what the brief specified; the specification itself has a hole.

### (b) Task quality verdict: **REQUEST CHANGES**

### By Severity
- CRITICAL: 1
- IMPORTANT: 1
- MINOR: 2

### Issues

**[CRITICAL] Permanent skip-marker can be written for a session file that is still open and will receive real messages later**
File: `mac-assistant/src/session_digest.rs:384-392` (new skip logic), in combination with `pi_agent.rs:292-339` (`warmup`/`warmup_spare`), `pi_agent.rs:1473-1508` (`reset()` promotes the spare process rather than starting a new one), and `panels/agent.rs:103-116` (`agent_screen_locked` flushes only *retiring* processes — the currently-active session and the idle spare-pool session are never touched).

I verified with a sub-agent against `pi_agent_rust/src/session.rs` that a `pi` session's JSONL file is opened once (`OpenOptions::new().append(true)`) and kept for the process's whole lifetime — the header lines (`session`/`model_change`/`thinking_level_change`) written at process start and any later real `type:"message"` entries land in the *same file, same path*. There is no rotation.

Consequence: both the main active `pi` process (started by `warmup()`) and the idle spare (started by `warmup_spare()`, later promoted verbatim by `reset()` via `activate_process()`) write their session file the instant they start, before any user message. If the screen locks in that window — e.g., app just launched and the user steps away before typing anything, or a spare is sitting in the pool waiting for the next "clear" — `count_messages` correctly returns `Some(0)` for that file (it genuinely has zero messages *right now*), and the new code writes a permanent `ok:true` marker and dequeues it. Later, when the user returns and actually chats — through that *same* live process/session — real messages get appended to that *same* file, but `pending_sessions` will never look at it again because the marker already exists. That is exactly the "silent permanent loss" class of bug this whole feature exists to prevent, just reached via a legitimately-empty-at-scan-time file instead of a misclassified unreadable one.

Important nuance: this exact risk is not newly *introduced* by Task 7's logic — before this diff, the same outcome was already reachable (any successful digest child run, on any session, wrote the same unconditional `ok:true` marker regardless of content, so a still-live empty session could already be marked "digested" if a spawned child happened to process it while empty). What Task 7 changes is that this outcome is now reached far more cheaply and therefore more often: previously it required a 12-19s inference run to complete successfully; now it's a free file-read with no spawn, so a 5-session batch (`DIGEST_BATCH_MAX`) can permanently mark up to 5 live/spare sessions per lock event instead of being throttled by inference time. The brief's stated justification for the skip ("these are steady-state artifacts of the warm pool") is empirically true of the 55/246 files sampled from history, but the code has no way to distinguish a genuinely dead orphan from a currently-open live/spare session file — there is no liveness check (e.g., against `pi_agent::state()`'s current session id, or the `.jsonl.lock` sibling file the code's own comment at `:28` already knows about).

Fix suggestion: before treating `Some(0)` as skippable, exclude the currently-active session id (exposed via `pi_agent::state()` or a small accessor) and the spare-pool's session id (if it has emitted one yet) from the skip path — or more robustly, check for a `.jsonl.lock` sibling / any signal that the file is still owned by a running process, and only apply the permanent skip-marker to files whose owning process is confirmed gone. This is arguably a Task 5 architectural fix rather than a Task 7 one, but since Task 7 is what makes it cheap to hit at scale, it should not ship without at least a targeted follow-up task, and the risk should be logged in "已知风险"/known-risks rather than left silent.

**[IMPORTANT] `memory_entry_total`'s `projects_root` aggregation branch is completely untested**
File: `mac-assistant/src/session_digest.rs:297-320` (impl), `:889-902` (the two `memory_entry_total` tests).

Both existing tests call `memory_entry_total(&amp;dir, None)` — neither exercises the `if let Some(root) = projects_root { ... }` branch that walks `projects-memory/*/MEMORY.md` and sums entries across project subdirectories. That branch is exactly the part the implementer added on their own judgment (the brief never specified `projects_root`'s value), and it's now live in production at both `mem_before`/`mem_after` call sites (`:425`, `:471`) — i.e., every digest delta number depends on code with zero regression coverage. The logic itself reads correctly on inspection (`fs::read_dir` + `is_dir()` filter + per-dir `MEMORY.md` entry count), but per the task's own core thesis — "a line asserting `delta=+1` that measured the wrong interval is worse than no line" — an untested aggregation path deserves at least one test with two project subdirectories to pin the summing behavior.

Fix suggestion: add `memory_entry_total_aggregates_across_project_dirs` — create `projects_root/proj-a/MEMORY.md` and `projects_root/proj-b/MEMORY.md` with known entry counts, assert the sum, and assert a non-directory entry under `projects_root` is skipped (the `e.path().is_dir()` guard).

**[MINOR] `no-change` bucket and log line don't distinguish "flat" from "shrank"**
File: `mac-assistant/src/session_digest.rs:483` (`no_change = matches!((mem_before, mem_after), (Some(b), Some(a)) if a &lt;= b)`).

A concurrent consolidation that shrinks the entry count (e.g. `mem=19→16 delta=-3`) is bucketed identically to a true no-op (`delta=+0`) and logged with the same `result=no-memory-change` suffix. The brief's own doc comment on `memory_entry_total` says a shrink from consolidation "is not lost memory," so this isn't wrong, but the label is imprecise — a reader scanning for "no-memory-change" won't immediately realize some of those lines actually show a negative delta. The raw `delta=` number is still printed correctly, so this is cosmetic, not a defect. The implementer already flagged this themselves in the report (concern #2) — worth a one-line ack, not a blocker.

**[MINOR] Marker-attempts semantics for the give-up path folded into `failed` counter, not distinguished from a real child failure**
File: `mac-assistant/src/session_digest.rs:373-382`.

The `attempts &gt; MAX_DIGEST_ATTEMPTS` give-up branch increments `failed`, same bucket as an actual nonzero-exit or killed child. The brief's Step 5 didn't specify a separate bucket for give-ups, so this isn't a spec violation, but the pass-summary line (`pass done processed=... failed=N`) will overcount "children that actually ran and failed" by folding in give-ups that never spawned anything this round. Low impact, worth a one-word note (`failed(N incl. M give-up)`) if this log line is ever used for alerting.

### Test Adequacy Assessment

The 6 new tests are honest — each targets one specific behavior and would fail if that behavior regressed:
- `count_messages_counts_only_message_entries` — would fail if the `type == "message"` filter broke or counted headers.
- `count_messages_reports_zero_for_a_warmup_only_session` — pins `Some(0)` for the exact warm-pool shape; would fail if it returned `None` or a nonzero count.
- `count_messages_tolerates_a_malformed_line_without_discarding_the_file` — would fail if a bad line panicked or zeroed the whole count.
- `count_messages_returns_none_when_the_file_cannot_be_read` — the single most important test in the set (guards the exact Critical-severity mistake the brief warned about); would fail immediately if `?` were replaced with `.unwrap_or(String::new())` or similar.
- `memory_entry_total_counts_entries_across_the_stores` / `_treats_an_absent_store_as_zero_not_as_failure` — correctly pin the `§`-delimiter split and the "missing file is 0, not failure" contract, but only for the `memory_dir`-local files; the `projects_root` branch has no coverage (see IMPORTANT finding above).

No test exercises `run_digest_pass` end-to-end for the skip path itself (i.e., that a `Some(0)` session actually gets `write_marker(..., 0, false)` and is excluded from a subsequent `pending_sessions` call) — that integration is only verified by code inspection here, not by a test. Given the module's existing test style (heavy use of `tmpdir` fixtures around `pending_sessions`/`write_marker` already in the file, e.g. the `a_second_pass_is_refused_while_one_is_running` test), an integration-level test for the skip-then-dequeue behavior would have been feasible and is the kind of test most directly protecting against the CRITICAL finding above (well, against a *regression* of the None/Some(0) correctness — it would not catch the live-session risk, which is architectural, not a code-logic bug).

### Positive Observations

- The doc comments on `count_messages` and `memory_entry_total` are unusually good: they state the invariant, explain *why* (not just what), and preemptively warn future readers about the exact mistake this task was written to catch (`None` ≠ `Some(0)`). This is the right way to armor a subtle invariant against future edits.
- The `elapsed`/`messages`/`mem=`/`delta=` log format is well-designed — self-proving, greppable, and the failure branches were extended consistently (`elapsed=`/`messages=` added to the killed/spawn-failed/nonzero-exit paths too, not just the happy path).
- The implementer's report is candid and specific: it flags the `attempts=0` forever-marker quirk and the `no-change`-includes-shrink ambiguity themselves, unprompted, rather than glossing over them. That's exactly the self-scrutiny this brief's process is designed to produce.
- Correctly resisted scope creep — did not touch `sessions.db`, `store/session-parser.ts`, or delete anything under `sessions/`, matching "明确不做的事."

### Recommendation

**REQUEST CHANGES** — not because the diff fails to implement the brief (it implements it faithfully and the None/Some(0) control-flow correctness the brief centers on is solid), but because tracing the *consequences* of that implementation against the rest of the codebase (as instructed) surfaces a real path to the exact silent-permanent-loss failure this task exists to prevent, via the currently-active/spare-pool session's file rather than an unreadable one. At minimum this needs to be raised to whoever owns Task 5/the marker architecture before this ships to real usage, since it will now trigger cheaply and repeatedly rather than only on the rare occasion a slow full-inference run happened to land on a still-live empty session. The IMPORTANT test-coverage gap on `projects_root` aggregation should also be closed before merge, given how central the delta number is to this task's stated purpose.</result>
<usage><subagent_tokens>88992</subagent_tokens><tool_uses>20</tool_uses><duration_ms>333693</duration_ms></usage>
</task-notification>
- 09:11 [EN] <task-notification>
<task-id>a47b287f6a61b07ea</task-id>
<tool-use-id>toolu_01UR63nRSc9vHG3nmC2rN9dH</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/a47b287f6a61b07ea.output</output-file>
<status>completed</status>
<summary>Agent "Task 7 Step 2-6: implement" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Status: DONE.

Commit SHA: `df3339cc567c5069daa15b669307220bac015322` (branch `feat/offline-session-digest`, only `mac-assistant/src/session_digest.rs` changed — `pi_bootstrap.rs` is pre-existing unrelated uncommitted work I did not touch).

Test summary: `cargo test -p mac-assistant` → 165 passed, 0 failed (33 in `session_digest` module: 32 prior + 1 new `projects_root` coverage test).

Concerns:
1. Moved the empty-check ahead of the `MAX_DIGEST_ATTEMPTS` give-up check, so a permanently-empty session's attempt counter never increments and it has no bound on how long it gets re-scanned — acceptable per the brief's own "costs nothing" framing, but flagging the tradeoff.
2. The new `memory_entry_total` test pins the documented "ignore non-directory entries" contract but I could not construct a case where omitting the `is_dir()` guard actually changes output (a non-directory path can't have an existing child, so `entries()` already returns 0 either way) — noting this so you can judge if stronger coverage is wanted.
3. Have not run a live screen-lock test; that remains yours per your original instruction.</result>
<usage><subagent_tokens>129487</subagent_tokens><tool_uses>30</tool_uses><duration_ms>327539</duration_ms></usage>
</task-notification>
- 09:15 [ZH] <task-notification>
<task-id>a55fc6452d349281b</task-id>
<tool-use-id>toolu_01H9jUvNzF5JpYyk2P9QFYqE</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/a55fc6452d349281b.output</output-file>
<status>completed</status>
<summary>Agent "Re-review Task 7 fix" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed the diff is applied at HEAD and matches the working tree I traced through. Verdict below.

## Scoped Re-Review: `44e2465..df3339c`

**File:** `/Users/youxun/project/mac-assistant/mac-assistant/src/session_digest.rs`

### Finding 1 [CRITICAL] — permanent marker for empty session — **ADDRESSED**

Traced the full path (lines 365–420, current HEAD):

- `scanned == Some(0)` branch (line 390–394) logs `"skipped reason=no-messages... remains pending"`, increments `skipped`, and `continue`s — **no `write_marker` call anywhere on this path**. The only other exit from the loop that touches this session before that point is the cancel/cap/stem checks, none of which write a marker either. Confirmed: an empty session leaves *no* marker, so `pending_sessions` (unchanged, line 63–78, filters purely on `marker_path(...).exists()`) will surface it again next pass.
- `scanned.is_none()` (unreadable) correctly falls through rather than early-continuing (line 395–397) — distinct from `Some(0)`, matching the original invariant that "can't tell" must never be treated as "empty."
- **Batch-cap mechanism verified, not just the comment**: `pending_sessions` is now called with `usize::MAX` (line 355), and `pending.truncate(max)` (line 76) — the only `max`-dependent step in `pending_sessions` — becomes a no-op; discovery/filter/sort still scan the whole directory either way, so no new discovery cost is introduced. `real_slots_used` (line 363, 398) increments *after* the empty-check clears, i.e., for every session that will consume an attempt (give-up, copy-failure, spawn, or unreadable-fallthrough) — never for a skipped-empty one. The loop-top guard `if real_slots_used &gt;= max { break }` (line 370) means: given a backlog of 200 empties + 5 real sessions in any order, the pass still processes all 5 real sessions (each costing one `count_messages` file read to skip the 200 empties is cheap and doesn't burn a slot). Verified by trace, not by trusting the comment.
- Minor comment-accuracy nit (not a functional issue): the code comment at line 353 says max limits sessions that "真正花了一次子进程" (actually spent a subprocess), but give-up sessions (line 401–408) also consume a `real_slots_used` slot without spawning a child. This matches the commit message's own broader framing ("give-up, spawn, or digest") and is functionally fine — the comment is just slightly narrower than the code. Not blocking.
- Ordering (empty-check before `MAX_DIGEST_ATTEMPTS` give-up check): correct as placed. A genuinely-empty session never had `attempts` incremented by prior real failures, so there's no interaction with pre-existing attempt counts, and no way for a session with real accumulated failures to "become empty" and dodge give-up under normal (non-corrupting) conditions. The tradeoff the implementer flags — a permanently-dead empty file (e.g., orphaned by a crash before any input) is rescanned every pass forever with no way to ever retire it — is real, but it is the explicit, deliberate consequence of Finding 1's own requirement (lock-file liveness was explicitly ruled out as unreliable, and there is currently no other liveness signal). This is a known, accepted design tradeoff already surfaced honestly in the report's Concern #1, not an unaddressed defect.
- `status.success()` still gates the marker exclusively (line 480, 498) — the `no_change` bucketing (line 493–503) affects only which counter/log-suffix is used, not whether the marker is written. Delta computation does not gate success.

### Finding 2 [IMPORTANT] — `projects_root` branch untested — **ADDRESSED**

New test `memory_entry_total_sums_across_project_directories_and_ignores_non_dir_entries` (session_digest.rs:917) exercises the previously-uncovered branch: root `MEMORY.md` (2 entries) + `proj-a/MEMORY.md` (3) + `proj-b/MEMORY.md` (1), asserting total `2+3+1=6`. This is the genuinely valuable part Finding 2 called out (summing across multiple project directories) and it is now properly covered.

The implementer's own caveat is accurate and I confirmed it by reading `entries()` (line 298–303): for a non-directory entry, `e.path().join("MEMORY.md")` resolves to a path that can't exist (ENOTDIR on read), so `entries()` returns 0 via the catch-all `_ =&gt; 0` arm regardless of whether the `is_dir()` guard (line 125 pre-fix / the equivalent in the current function) is present. The `not-a-project.txt` stray-entry portion of the test is decorative — it pins the documented contract but doesn't prove the guard is load-bearing. This is an honest, correctly-self-assessed gap, not a hidden one, and doesn't undermine the branch's real coverage.

### New Critical/Important breakage introduced by this diff

None found. No panics, no marker-write regressions, no logic that could let a real conversation get silently orphaned by this round's changes, and the batch-cap accounting doesn't change discovery cost or introduce an infinite loop (bounded by `Vec` iteration over `backlog`, a fixed snapshot per pass).

### Recommendation

**APPROVE.** Both findings are addressed with verified mechanisms, not just comments. The one open item (permanently-dead empty sessions rescanned forever, no reclamation path) is an explicit, previously-acknowledged design tradeoff mandated by Finding 1's own text, already flagged honestly by the implementer — it's a candidate for a future follow-up (e.g., a separate low-cost "dead file" heuristic), not a blocker on this fix.</result>
<usage><subagent_tokens>73362</subagent_tokens><tool_uses>8</tool_uses><duration_ms>142043</duration_ms></usage>
</task-notification>
- 09:19 [EN] <task-notification>
<task-id>a9822896bee91b770</task-id>
<tool-use-id>toolu_015F8pvWvnoYvfjD9nkmdyPu</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mac-assistant/7650af33-01af-4ade-b0dd-188b71f03612/tasks/a9822896bee91b770.output</output-file>
<status>completed</status>
<summary>Agent "Cross-task consistency check" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Cross-Task Consistency Review — commits `9bfa1a6..df3339c`

Scope respected: not re-litigating any commit's individual review. Only checking whether the final fix wave (`032bd61`) and Task 7 (`8c168ff`,`44e2465`,`df3339c`) broke an invariant the earlier work relied on.

### 1. `PASS_RUNNING` guard vs. new loop exit paths

Confirmed safe. `spawn_digest_pass` (`session_digest.rs:559-564`) is unchanged:
```rust
pub fn spawn_digest_pass(max: usize) {
    if !try_begin_pass() { ... return; }
    thread::spawn(move || {
        run_digest_pass(max);
        end_pass();
    });
}
```
Every new exit inside `run_digest_pass` — the empty-session `continue` (line 393), the give-up `continue` (407), and the new batch-cap `break` (372) — is a normal `continue`/`break`/fallthrough inside the `for` loop. None of them return early from `run_digest_pass` itself; they all fall through to the `log("pass done"...)` line and a normal function return, so `end_pass()` in the wrapping closure always runs. No re-entrancy leak introduced.

### 2. Cancellation predicate vs. backlog-wide iteration

Confirmed safe, and better than the ticket's worry suggested. The cancel check is the **first statement in every loop iteration**, not just between "real" sessions:
```rust
for session in backlog {
    if cancel_flag_path(&amp;memory_dir).exists() { ...; break; }
    if real_slots_used &gt;= max { ...; break; }
    ...
    let scanned = count_messages(&amp;session);
    if scanned == Some(0) { ...; continue; }   // empty-session skip
    ...
}
```
An empty-session skip costs one `fs::read_to_string` of a few hundred bytes, then loops back to the top where cancel is re-checked immediately. A long stretch of empty backlog entries does not create a window where cancellation is starved — the child-process cancel probe (`wait_for_exit_or_kill_with_cancel`, 500 ms poll) only matters once a real child is spawned, and the outer loop's own cancel check bounds the empty-stretch case independently. No interaction defect.

### 3. `MAX_DIGEST_ATTEMPTS`/give-up path vs. empty-skip

Confirmed correctly ordered. The empty-skip check (`scanned == Some(0)`) sits **before** `read_attempts`/give-up (line 390 vs. 400), and the empty path never increments `attempts` or touches the marker at all — it just `continue`s. So an empty warm-pool artifact can never be misrouted into the give-up path, and a session with real content can never be silently absorbed by the empty-skip. This matches the plan's Step 3 note and is verified in code, not just comments.

One real (but pre-existing-pattern, non-blocking) nuance: a give-up session still increments `real_slots_used` (line 398, before the attempts check at 400), so on the pass where a session finally gives up it consumes one of the 5 per-lock-screen slots even though no child is spawned. This only happens once per dead session (marker is then permanent), so it's not a starvation risk — noting for completeness, not as a blocker.

### 4. Memory-delta sampling vs. child deadline/kill

Correctly attributed per-child in the single-threaded case: `mem_before` is sampled immediately before `cmd.spawn()` for *that* session (line 437), `mem_after` only on that same session's successful exit (line 483), and processing is strictly sequential (one child at a time inside the `for` loop, serialized by `PASS_RUNNING`). Failed/killed children never sample `mem_after`, so no delta is ever attributed to a child that didn't finish.

The one real gap: if the **active** (non-digest) `pi` agent process writes to `MEMORY.md`/`USER.md` concurrently with a digest child (already flagged as a known risk in the plan's risk table for lock contention, not for delta correctness), the logged `delta=+N` for that digest child could include entries the active agent wrote, not the digest child. This is a diagnostics-only log line — it doesn't affect marker correctness, retry logic, or the give-up path — so it's a LOW-severity observation, not a merge blocker.

### Plan-doc vs. shipped code divergence (new finding)

`docs/superpowers/plans/2026-08-03-offline-session-digest.md` Task 7 Step 3 (lines ~1511-1530, added whole-cloth by `8c168ff`) still shows the **pre-fix** pseudocode:
```rust
Some(0) =&gt; {
    ...
    let _ = write_marker(&amp;markers_dir, &amp;stem, true, 0);   // &lt;- writes a permanent marker
    skipped += 1;
    continue;
}
```
The actually-shipped code (`44e2465`/`df3339c`, confirmed live in `session_digest.rs:390-394`) does **not** write a marker on empty-skip — that's precisely the CRITICAL fix the ledger records ("writing a permanent marker for a zero-message session could orphan a real conversation"). The plan document was never amended after that fix landed, so a reader following the plan alone would reconstruct the orphaning bug. This is stale documentation, not a code defect — the ledger already has the accurate account. Worth a follow-up doc fix, not a merge blocker.

---

## Ledger triage (`minor (deferred)` / `parked` / `Accepted tradeoff`)

| Item | Status now | Verdict |
|---|---|---|
| Task 4: `write_marker`'s attempt-file cleanup on success is untested | Still untested — no test asserts the `.attempt` file removal on success path | Fine to carry. Testing gap, not a behavior gap; low value to fix now. |
| Task 4: child stderr discarded even on failure | **Resolved** by `032bd61` — stderr now piped, captured, and tailed into failure/kill log lines (`stderr_excerpt`) | Closed, no action needed. |
| Task 4 (plan-level): give-up path reuses `ok:true`, no way to distinguish real success from give-up | **Resolved** by `032bd61` — `gaveUp` field added, tested (`a_give_up_marker_is_still_ok_true_but_flagged_gave_up`) | Closed. |
| Final review I1: cancel flag only checked between sessions, hung child delays cancel up to 330s | **Resolved** by `032bd61`'s `wait_for_exit_or_kill_with_cancel`, re-reviewed and approved | Closed. |
| Final review I2: stale comment above `agent_screen_locked` claiming retiring processes still summarize | **Resolved** — comment rewritten in `032bd61` (`panels/agent.rs`) | Closed. |
| Low-severity: stderr drain thread accumulates unbounded `String` before truncating to 500 chars | Still present, still "not actioned," still bounded by the ~330s deadline and failure-only path | Genuinely fine to carry as-is — no change needed before merge. |
| Own: recursion guard lives outside repo (`~/pi-hermes-memory-src`), unpinned, undocumented | **Resolved** — `CLAUDE.md` now documents it explicitly (Runtime Dependencies section) | Closed. |
| GEMINI_API_KEY not forwarded to digest children (dormant) | **Resolved** — forwarded in `032bd61`, matches `pi_agent.rs`'s existing forwarding pattern | Closed. |
| Task 7: Accepted tradeoff — a permanently-dead empty session file is rescanned every pass forever | Confirmed still true in shipped code (no marker ever written for `Some(0)`) | Genuinely fine — one ~520-byte read per pass, no reliable liveness signal exists to do better. Not a merge blocker. |

All previously-deferred items that were actionable have been fixed by this delta. The two that remain "carry as-is" (unbounded stderr buffer, untested attempt-cleanup, forever-rescanned dead empties) are correctly triaged as non-blocking — each has a documented, still-valid reason.

---

## Aside: unrelated uncommitted work in the tree

Current `git status` shows uncommitted changes in `mac-assistant/src/pi_bootstrap.rs` (proxy env passthrough, npm package identity de-duplication, web-search SSRF config merge) and `mac-assistant/src/pi_agent.rs` (a new watchdog total-timeout for the agent's own request path, `abort_active_request` refactor). These are **not part of the reviewed delta** — `pi_bootstrap.rs` isn't touched by any of the 4 commits in `9bfa1a6..df3339c`, and the `pi_agent.rs` uncommitted diff is orthogonal (agent-side request watchdog, not digest). They don't interact with anything examined above and aren't in scope for this consistency check. Flagging only so they aren't mistaken for part of this branch when it's merged.

---

## Verdict

**Safe to merge as it stands.** All four interaction points named in the brief hold up under direct code inspection: the re-entrancy guard is unconditionally released on every new exit path, cancellation is checked on every loop iteration (not just real ones) so a long empty stretch can't starve it, the empty-skip strictly precedes the give-up/attempts logic so a session can't be misrouted between them, and the memory-delta sampling is correctly paired per sequential child with only a pre-existing (already-documented) concurrent-write caveat. Every ledger item that was actionable has been fixed in this delta; the ones still marked "carry" are correctly judged non-blocking. The one gap I found beyond the ledger — Task 7's plan-doc Step 3 snippet not reflecting the shipped no-marker fix — is a documentation staleness issue, not a code defect, and doesn't block merge.

Files referenced:
- `/Users/youxun/project/mac-assistant/mac-assistant/src/session_digest.rs`
- `/Users/youxun/project/mac-assistant/mac-assistant/src/pi_agent.rs`
- `/Users/youxun/project/mac-assistant/mac-assistant/src/panels/agent.rs`
- `/Users/youxun/project/mac-assistant/CLAUDE.md`
- `/Users/youxun/project/mac-assistant/docs/superpowers/plans/2026-08-03-offline-session-digest.md` (Task 7, lines ~1375-1585)
- `/Users/youxun/project/mac-assistant/.superpowers/sdd/2026-08-03-offline-session-digest/progress.md`</result>
<usage><subagent_tokens>125541</subagent_tokens><tool_uses>11</tool_uses><duration_ms>216592</duration_ms></usage>
</task-notification>
- 13:22 [ZH] [2026-08-04 13:16:35.563] [pi] flush_retiring_now: called, 3 retiring process(es) pending
[2026-08-04 13:16:35.563] [pi] flush_retiring_now: flushing 3 retiring process(es)
[2026-08-04 13:16:35.564] [pi] stderr: 2026-08-04T05:16:35.564739Z ERROR [hermes-review] session_shutdown: skipped, reviewOnShutdown is false
[2026-08-04 13:16:35.571] [pi] pi stdout loop ended, process likely exited
[2026-08-04 13:16:35.665] [pi] stderr: 2026-08-04T05:16:35.665143Z ERROR [hermes-review] session_shutdown: skipped, reviewOnShutdown is false
[2026-08-04 13:16:35.671] [pi] pi stdout loop ended, process likely exited
[2026-08-04 13:16:35.769] [pi] stderr: 2026-08-04T05:16:35.769626Z ERROR [hermes-review] session_shutdown: skipped, reviewOnShutdown is false
[2026-08-04 13:16:35.781] [pi] pi stdout loop ended, process likely exited
[2026-08-04 13:16:35.869] [pi] flush_retiring_now: done 什么情况,总结报错了吗
- 13:24 [ZH] 那怎么知道总结成功了呢,没有日志吗
- 13:26 [ZH] 什么时候的日志
- 13:27 [ZH] 空会话,会标记掉,或者清理掉嘛
- 13:28 [ZH] 下次来不会又来总结把
- 13:28 [ZH] 空会话是不是可以删了
- 13:29 [ZH] 或者不让他,每次总结的时候都去扫一下,标记一下也行
- 13:30 [ZH] 你先手动把空的删了把,正常就不会有空的
