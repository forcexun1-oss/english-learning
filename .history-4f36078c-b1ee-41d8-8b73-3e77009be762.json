["不带 profile 默认路由重跑", "怎么还有nodes.py", "<task-notification>\n<task-id>bc6d2cdwr</task-id>\n<tool-use-id>toolu_01F5GzcuScXymVTSd5XiaViD</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/bc6d2cdwr.output</output-file>\n<status>completed</status>\n<summary>Background command \"Re-dispatch task to codex with default routing\" completed (exit code 0)</summary>\n</task-notification>", "<task-notification>\n<task-id>ada3f74aede440730</task-id>\n<tool-use-id>toolu_017JiyiB4NMaUsKC9vjFSjn2</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/ada3f74aede440730.output</output-file>\n<status>completed</status>\n<summary>Agent \"Deep review LLM streaming diff\" came to rest</summary>\n<note>A task-notification fires each time this agent comes to rest with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>\n<result>Yes -- if `ToolCallsFinished` was already handled inside the loop (line 906-911), `finalize_all` at line 203 returns an empty vec (because `finalize_all` uses `std::mem::take`). If the stream ended without `ToolCallsFinished` (e.g., connection dropped mid-tool-call), the post-loop `finalize_all` catches the residual. This is correct and defensive.\n\nNow let me reconsider the HIGH issue. The real gap I should escalate is the asymmetry between OpenAI and Ollama post-loop handling:\n\n**[HIGH] Ollama `chat_stream` missing post-loop `finalize_all` -- asymmetric with OpenAI path**\n\nFile: `/Users/youxun/project/edgeAgent/rust/crates/capabilities/src/llm.rs`, lines 309-315\n\n**Issue:** The OpenAI `chat_stream` (lines 198-207) has a post-loop `finalize_all()` that flushes any residual tool-call fragments. The Ollama `chat_stream` (lines 309-315) does NOT. Currently this is harmless because `parse_ollama_ndjson_line` emits `StreamEvent::ToolCall` (complete) rather than `StreamEvent::ToolCallDelta`. However:\n\n1. The `handle_stream_events` function (line 874) handles BOTH `StreamEvent::ToolCall` and `StreamEvent::ToolCallDelta` -- nothing prevents Ollama's parser from being updated to use deltas in the future.\n2. The `ToolCallAccumulator` is instantiated at line 297, creating an expectation that it might be used.\n3. The asymmetry between the two backends is a maintenance hazard -- someone modifying the Ollama parser to handle fragmented tool-calls would not know to also add `finalize_all`.\n\nThis is a latent correctness issue. If someone adds `ToolCallDelta` support to Ollama parsing without also adding the post-loop flush, tool-calls will be silently dropped.\n\n**Fix:** Add the same post-loop finalization to Ollama's `chat_stream`, after line 313:\n```rust\nfor call in tool_calls.finalize_all() {\n    tx.send_async(Chunk::ToolCall(call))\n        .await\n        .map_err(|err| anyhow!(\"llm stream receiver closed: {err}\"))?;\n}\n```\n\nThis is zero-cost when the accumulator is empty (which it always is today) and provides safety for future changes. Alternatively, if the accumulator is intentionally unused, remove it from the Ollama path and add a comment explaining why.\n\n---\n\n## Final Issue Table\n\n| # | Severity | Issue | File:Line |\n|---|----------|-------|-----------|\n| 1 | HIGH | Ollama `chat_stream` missing post-loop `tool_calls.finalize_all()` -- asymmetric with OpenAI path, latent data-loss risk for future tool-call delta support | `llm.rs:313-315` |\n| 2 | MEDIUM | `first_token_ms` start point is after HTTP response headers, not at request send -- measures parsing latency rather than true TTFT (B8 observability gap) | `llm.rs:183,294` |\n| 3 | MEDIUM | No test for receiver-dropped-mid-stream scenario (design doc section 6 explicitly calls this out) | `llm.rs` tests section |\n| 4 | MEDIUM | Spawned backend task not explicitly aborted on failover -- correct today but the invariant is non-obvious and fragile | `llm.rs:1079,1093` |\n| 5 | LOW | `emit_response_as_stream` is now dead code, suppressed with `#[allow(dead_code)]` -- should be removed per project's \"remove what your changes orphaned\" principle | `llm.rs:931-948` |\n\n---\n\n## Recommendation\n\n**REQUEST CHANGES**\n\nThere is one HIGH issue (#1) that should be fixed before merging: the Ollama path's missing `finalize_all` post-loop. It is a two-line fix that costs nothing at runtime and closes a real maintenance trap.\n\nThe MEDIUM issues (#2, #3, #4) are strongly recommended but individually non-blocking. Issue #2 (`first_token_ms` start point) is the most impactful for observability correctness -- if the purpose of this metric is to measure perceived TTFT for the edge-agent use case, the current placement underreports by the full HTTP round-trip time.\n\nThe code quality is overall quite high. The architecture is clean, the parsing is well-separated, the error handling is thorough, and the test coverage is solid for a first pass. This is production-ready pending the HIGH fix.</result>\n<usage><subagent_tokens>74082</subagent_tokens><tool_uses>18</tool_uses><duration_ms>228878</duration_ms></usage>\n</task-notification>", "commit it. next time, ask them create a feature branch when distributing tasks.", "And ask them create a workspace too", "Will it be deleted when the task completed", "1 edge-agent app stays", "agent_core is the eventual orchestrator for the edge-agent app", "ASR和TTS在重构之后还是在python侧执行的?", "按这个边界去写 B7 集成 handoff", "提交任务吧,记得加--profile chatgpt", "什么进度了", "<task-notification>\n<task-id>bh136ocx3</task-id>\n<tool-use-id>toolu_01Xyq5BuKK6QtpbxUgwWRxk3</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/bh136ocx3.output</output-file>\n<status>completed</status>\n<summary>Background command \"Dispatch B7 task to codex with chatgpt profile\" completed (exit code 0)</summary>\n</task-notification>", "<task-notification>\n<task-id>b4vvb2wyh</task-id>\n<tool-use-id>toolu_01PBZzkadvjC4qf895Gf39WZ</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/b4vvb2wyh.output</output-file>\n<status>completed</status>\n<summary>Background command \"Re-run B7 workspace tests with correct venv path\" completed (exit code 0)</summary>\n</task-notification>", "yes, run the gitnexus reindex", "<task-notification>\n<task-id>bhhomftnq</task-id>\n<tool-use-id>toolu_018RPf86Af6uRUgtZ8miwwaD</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/bhhomftnq.output</output-file>\n<status>completed</status>\n<summary>Background command \"Reindex repo with GitNexus\" completed (exit code 0)</summary>\n</task-notification>", "现在的测试写的专业吗,我想要专业的自动化测试, 既然代码都是生成, 而且,我们也不会有测试的, 那么怎么测试呢, 有什么技能吗, 或者subagent吗", "先派 test-engineer 出审计报告", "<task-notification>\n<task-id>a962ed83e16964050</task-id>\n<tool-use-id>toolu_01S7wkCqNTfpsKfKkWSygp2m</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/4f36078c-b1ee-41d8-8b73-3e77009be762/tasks/a962ed83e16964050.output</output-file>\n<status>completed</status>\n<summary>Agent \"Test adequacy audit report\" came to rest</summary>\n<note>A task-notification fires each time this agent comes to rest with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>\n<result># 测试充分性审计报告 — edgeAgent @ b685a26\n\n## 总体评级\n\n现有测试在「专业自动化测试」标尺上属于 **中上水平的单元测试纪律 + 明显的集成/对抗性测试缺口**。`capabilities::llm` 这次的流式重写测试写得相当扎实：纯函数解析逐case覆盖、用真实 TCP socket 模拟分片到达的 SSE/NDJSON、覆盖了 receiver-drop 错误路径,这已经超出大多数 AI 生成代码的平均水准,**不是循环验证的典型重灾区**。但 `agent_core`/`runtime_bridge` 这块——尤其是 barge-in 取消竞态、VAD 端点的边界情况、Runtime 主循环的并发交织——测试覆盖明显薄,而且确实存在几处「测试在断言当前实现的具体路径,而非契约」的案例(下面逐一列出)。集成测试层基本是装饰:`tests/e2e` 里真正跑 LLM 推理/MLX 路径的两个测试默认 `skip`,CI 配置完全不存在(无 `.github/`),意味着**这个仓库目前没有任何自动门禁**——单测全绿只代表「代码自洽」,没有任何机制保证它在真实环境下行为正确。\n\n---\n\n## 1. 循环验证识别\n\n| 证据 | 严重度 | 说明 |\n|---|---|---|\n| `rust/apps/edge-agent/src/agent_core/reasoning.rs:433-447` `model_router_uses_fast_for_short_text_without_image` | Medium | 测试直接断言 `route()` 的阈值实现(`&lt; 20 chars`且无图→Fast),而 handoff 文档里这条规则本身只是「MVP 占位」,没有产品侧契约支撑「20」这个数字。测试锁死了一个可能完全是猜的魔法数,以后想调整阈值时这条测试会变成「防止改进」的阻力而非「捕获回归」的保险。\n| `rust/apps/edge-agent/src/agent_core/reasoning.rs:294-319` `StubLlm`/`ScriptedLlm` | High | `chat()` 永远返回 `LlmResponse{content:None, tool_calls:vec![], usage:None}`,从未被 `run_turn` 用到、也从未被测试调用验证。这是「为了实现 trait 而凑的样板」,不构成对契约的验证,只是让编译通过。如果未来 `run_turn` 真的调用了非流式 `chat()`(例如 fast-path 优化),这两个 double 会悄悄返回空响应而没有任何测试会失败——典型的「test double 行为迁就实现」而非反映真实后端语义。\n| `rust/apps/edge-agent/src/agent_core/reasoning.rs:255-266` 取消/失败路径与 `usage_task.abort()` | High | `run_turn` 里所有 `cancel.cancelled()` 分支都调用 `usage_task.abort()` 后 `return Ok(())`,但**没有任何测试验证「取消后流确实停止、没有残留 chunk 被发送、没有半成品 Action 漏出」**。当前测试只验证了\"正常跑完\"和\"工具结果回灌\",从未验证 barge-in 的核心契约——这恰好是 handoff 文档(B7 §4.1, §5 G2)反复强调的「barge-in 有效」要害路径,完全没测,等于把\"看起来能取消\"当成了\"validated\"。\n| `rust/apps/edge-agent/src/agent_core/runtime.rs:474-510` `perception_lanes_publish_final_asr_as_voice_input` | Medium | 用 `StaticAsr::new(\"open map\")` 这个 stub,它的行为是\"无条件返回这段固定文本\",测试断言的实质是「StaticAsr 返回了我配置的文本」,而不是「PyAsrAdapter 的 VAD 切句逻辑产出了正确文本」。真正承载 VAD 契约的 `PyAsrAdapter`/`next_speech_segment` 在 `runtime.rs` 层完全没被这条测试触达——`Runtime::start_perception_lanes` 的契约被验证了「能转发」,但没验证「VAD 端点判断是对的」(后者在 `runtime_bridge.rs` 单独测,见下条)。\n| `rust/apps/edge-agent/src/runtime_bridge.rs:617-645` `vad_endpoint_returns_segment_after_trailing_silence` | Medium | 只测了一种几何形状:1 段语音 + 1 段超长静音(12800 字节,远超 `trailing_silence_ms=700` 阈值)。`min_speech_ms=240` 这个边界(语音刚好不够长就被静音打断会发生什么?)、`max_segment_ms=12_000` 强制截断分支、`bail!(\"mixed ASR sample rates...\")` 错误路径,全部没有覆盖。这条测试验证的是「最舒服的一条路径」,契约里写明的三个端点条件(min_speech/trailing_silence/max_segment)只验证了一个。\n\n**结论**:循环验证的风险集中在 `agent_core`(ReasoningStage 的取消语义、ModelRouter 阈值)、不在 `capabilities::llm`(后者的纯函数+mock server 测试是按 spec 逐条对照写的,质量明显更高)。\n\n---\n\n## 2. 覆盖缺口(基于代码阅读,非行覆盖率推断)\n\n### `capabilities::llm`(覆盖已经相当完整,仅剩小缺口)\n| 位置 | 缺口 | 严重度 |\n|---|---|---|\n| `llm.rs:686-700` `LineBuffer::push` 对非 UTF-8 字节序列 | 完全没测。SSE/NDJSON 上游若发出截断的多字节 UTF-8(网络分片刚好切在字符中间),`String::from_utf8(line)?` 会返回 `Err`,整个 `chat_stream` 直接失败退出。Handoff §6 只提到\"跨 chunk 断行\"的字节序列测试,没提多字节字符跨块场景——这是真实 CJK 文本流式输出的高发故障点(项目场景里 LLM 大概率吐中文)。 | **High** |\n| `llm.rs:931-935` `capture_first_token` 的 `.max(1)` | 没有测试覆盖\"耗时恰好为 0ms\"分支(mock server 太快时可能触发),只是隐式依赖 mock server 的 20ms sleep。 | Low |\n| `LlmRouter::chat_stream` 多于2个后端、且第二个也失败 | 只测了2-backend 场景,3+backend 链式 failover(尤其\"第一个失败,第二个转发了部分chunk后失败,但因为 forwarded_any 已经是 true,不应该再尝试第三个\")没有专门测试。`llm.rs:1041-1092` 的状态机相对复杂,这条路径(转发后失败→不再 failover)虽然实现里写对了,但缺一条直接证明它的测试。 | Medium |\n| `ToolCallAccumulator` 多个 index 交错到达(index 1 的分片先于 index 0 完成) | `tool_call_accumulator_concatenates_fragments_and_finalizes`(`llm.rs:1452`)只测了单个 index 的拼接,`finalize_all` 用 `BTreeMap` 排序,多 tool-call 并行分片到达时的顺序行为没测。 | Medium |\n| OpenAI 流式响应里 `delta.content` 为显式 `null`(role-头 chunk) | `OpenAiStreamDelta.content: Option&lt;String&gt;` 理论上能处理,但没有用真实 mock server 跑过\"只有 role,没有 content\"的首 chunk(handoff §6 明确提到这是陷阱点)。 | Medium |\n\n### `agent_core` / `runtime_bridge`(缺口明显更大)\n| 位置 | 缺口 | 严重度 |\n|---|---|---|\n| `reasoning.rs:115-278` `run_turn` 取消竞态 | **完全没有**针对 barge-in 的测试:cancel token 在 chat_stream 进行中被取消、在工具执行中被取消、在 TTS filler 发送中被取消——三处 `tokio::select!` 分支(`reasoning.rs:162-167, 171-176, 202-209, 212-218, 222-227, 250-256`)一条都没被测试触达。这是 B7 handoff §5 G2 明确要求验证\"barge-in 有效\"的核心路径,当前是 0 覆盖。 | **Critical** |\n| `runtime.rs:104-177` `Runtime::run()` 主循环里 voice 抢占正在运行的回合 | 没有端到端测试验证:第二条 voice 输入到达时,`replace_turn` 取消了第一条的 token,`abort_active_tasks` 真的中止了第一条的 `run`/`action_drain` task,且第一条尚未发出的 TTS 不会播放。当前只在 `trigger.rs` 层测了\"队列里 voice 抢占 event\"(纯排队语义),没有测\"运行中的回合被新 voice 打断\"(真正的 barge-in)。 | **Critical** |\n| `runtime.rs:232-275` `drain_actions` 取消/receiver-drop | 没有测试覆盖 TTS 合成中途 `rx` 被 drop(B7 handoff §4.2 提到\"适配器内长合成应可被 receiver drop 中断\")的场景;也没测 `synth`/`drain` 两个 future 中一个 panic/error 时 `tokio::join!` 的行为。 | High |\n| `runtime_bridge.rs:383-425` `next_speech_segment` 边界 | 已列在 §1,补充:`min_speech_ms` 刚好不够、`max_segment_ms` 强制截断、`bail!`混合采样率错误路径——3 个分支均未测。 | High |\n| `runtime_bridge.rs:154-178` `build_llm_backend_from_env` | 完全没有单测,env 分支(`kimi` vs 默认 `ollama`,`KIMI_API_KEY`缺失,`KIMI_BASE_URL`/`OLLAMA_BASE_URL`默认值)0 覆盖。属于纯函数、易测、却被跳过。 | Medium |\n| `runtime.rs:180-191` `Runtime::check()` | 只有逻辑本身(if/bail),没有单测验证 4 种组合(`events.is_some()!=reasoning.is_some()`等)中哪些会触发 `bail!`。这是 `--check` OTA 预验证的核心函数,CLAUDE.md 强调\"失败则由 OTA 框架回滚\"——这种安全网函数完全没测意味着回滚触发条件本身没被验证过。 | High |\n| `runtime_bridge.rs:441-457` `DeviceCaptureAdapter::start_video` 的 feature-gate 分支 | `#[cfg(not(feature = \"video\"))]` 分支(返回 `Ok(())` 不发任何帧)没有测试;`Runtime::check()` 的\"capture 存在但 asr 不存在\"组合若被触发,实际运行时会在 `start_perception_lanes` 里 `bail!`,但这条路径与 `check()` 的逻辑是否一致从未交叉验证。 | Medium |\n| `agent_core/skill.rs`, `tools.rs`, `prompt.rs`, `guard.rs` | 这几个模块各只有 1 个测试(`skill.rs:124`、`tools.rs:108`、`prompt.rs:111`、`guard.rs:148`),均为\"happy path 单元测试\",错误路径(配置文件格式错误、工具名冲突、prompt key 缺失时的 fallback)基本没测。`PromptStore::get` 在 `reasoning.rs` 里多处用 `.unwrap_or_else` 兜底,说明设计者预期 key 会缺失,但这个兜底行为本身没有专门测试。 | Medium |\n\n---\n\n## 3. 集成测试形同虚设\n\n`tests/e2e/` 共 2 个文件、6 个测试函数:\n\n| 文件 | 测试 | 标记 | 默认运行? |\n|---|---|---|---|\n| `test_middleware_service_http.py:13` `test_tts_synthesize` | `@pytest.mark.e2e` | 需要 middleware-service 进程跑着,CI 没起进程则会连接失败/被手动跳过运行 |\n| `test_middleware_service_http.py:24` `test_asr_transcribe` | 同上 | 同上 |\n| `test_middleware_service_http.py:38` `test_vision_detects_expected_object` | 同上 | 同上 |\n| `test_llm_gateway_http.py:12` `test_create_session` | `@pytest.mark.e2e` | 同上 |\n| `test_llm_gateway_http.py:20` `test_chat_completions_optional` | `@pytest.mark.e2e` + `@pytest.mark.optional_e2e`,内部 `pytest.skip()` 除非传 `--run-gateway-chat` | **默认 skip** |\n| `test_llm_gateway_http.py:47` `test_direct_mlx_optional` | 同上,需 `--run-direct-mlx` | **默认 skip** |\n\n**结论**:\n- 全部 6 个测试都要求一个**已经手动启动的** middleware-service 进程(`127.0.0.1:8765`),没有任何 fixture 自动拉起被测进程——这意味着即便不加 `--run-*` flag,`pytest tests/e2e` 在没有手动起服务的环境下(包括任何 CI)会直接连接失败而不是\"跳过\"。这比\"optional_e2e 默认跳过\"更糟:**没有进程在跑时,test_tts_synthesize / test_asr_transcribe / test_create_session 这三个\"必跑\"测试也会失败**,所以这层测试事实上从未在自动化环境下被验证过,纯粹依赖工程师手动跑。\n- 真正覆盖\"LLM 推理产出正确内容\"的 `test_chat_completions_optional` 和 `test_direct_mlx_optional` 默认 skip,等于**全链路 LLM 推理正确性从未被自动验证**,只验证了\"session 创建成功\"这种契约外壳。\n- 本次审计重点的两块代码(`capabilities::llm` 真流式、`agent_core` Runtime)**完全没有 e2e/集成覆盖**——`tests/e2e` 测的是 middleware-service(C1 HTTP 网关),不是 agent-core 的 in-process Rust 编排路径或 stdin/voice 全链路。B7 handoff §6 要求的\"手动 MVP 验证(stdin 输入一行确认全链路 + TTS 出声)\"和\"voice 模式 mic 说话验证 barge-in\"都标注为手动步骤,没有任何自动化测试固化这些验证。\n\n---\n\n## 4. 缺失的高级手段\n\n| 目标模块/函数 | 推荐手段 | 理由 |\n|---|---|---|\n| `llm.rs::LineBuffer::push` + `parse_openai_sse_line`/`parse_ollama_ndjson_line` | **Property-based (proptest)** | 这三个是纯函数、无 I/O、契约清晰(\"任意切分方式喂入字节流,重组后行内容不变\")。当前测试只手写了 2-3 种切分方式,proptest 能自动发现\"切在多字节 UTF-8 字符中间\"\"空字节块\"\"超长行\"等边界。这是本次新增代码里 ROI 最高的一项。 |\n| `llm.rs::ToolCallAccumulator` | **Property-based** | \"任意把一个完整 JSON 字符串切成 N 段、任意打乱到达顺序(同 index 内保持顺序)、reassemble 后必须等于原串\"——这是可形式化验证的不变量，比手写 2 个 case 更能保证正确性。 |\n| `runtime_bridge.rs::next_speech_segment` (VAD 端点) | **Property-based + 边界枚举测试** | 输入是任意长度的语音/静音帧序列,输出契约(\"speech_ms &gt;= min_speech_ms 且 (trailing_silence 达标 或 总长触顶) 才切句\")是可以用 proptest 生成随机帧序列后断言不变量的典型场景。当前只有 1 个 happy-path case。 |\n| `OpenAiCompatibleLlm` / `OllamaLlm` 的真实响应 vs 文档样例 | **Golden/snapshot 对拍** | Handoff 文档里给的 JSON 样例(行 66-74)应该固化成 golden fixture 文件,而不是散落在测试代码里的字符串字面量——这样未来上游 API 格式变化时,直接对比 golden 文件能更快定位是\"契约变了\"还是\"代码错了\"。 |\n| ASR/TTS Rust↔Python 边界(`reasoning::py::asr_transcribe`, `action::tts::synthesize`) | **Differential 对拍** | CLAUDE.md 明确这是 PyO3 in-process 边界,且 ASR/TTS 推理仍在 Python。当前 Rust 侧测试(`wav_to_audio_chunk_reads_mono_i16_pcm` 等)只测了\"Rust 写的 wav→Rust 读回来一致\",没有验证\"真实 Python TTS 输出的 wav 格式是否始终满足 Rust 侧的硬编码假设(mono/16bit,见 `runtime_bridge.rs:267-273` 的 `bail!`)\"。建议:用真实 Python TTS 跑一批样本,固化输出 wav 头部格式做 golden 对拍,一旦 Python 侧模型升级改变了声道数/位深,Rust 测试能立刻发现而不是运行时 `bail!`。 |\n| `agent_core::reasoning::run_turn` 的取消语义 | **Fuzz/property(以 cancel 时点为参数)** | 这是 §2 标 Critical 的缺口。可以构造一个\"可在任意 chunk 之间插入随机延迟的 StubLlm\",再用随机时间点触发 `cancel.cancel()`,断言不变量:取消后 `tx` 上不会再收到任何新 `Action`、且 `run_turn` 总是在有限时间内返回 `Ok(())`。这是把当前\"完全没测的 Critical 缺口\"转成可重复验证的纪律。 |\n| `tests/e2e` | **解禁 e2e** | 给 middleware-service 启动加自动 fixture(在 conftest 里 spawn 子进程、等待健康检查、teardown 时杀掉),让\"必跑\"的 3 个测试在 CI 里真的能跑;同时考虑把 `--run-gateway-chat` 默认打开但用便宜的 mock/录制响应(VCR 式),而不是依赖真实 LLM 调用。 |\n\n---\n\n## 5. 测试基础设施\n\n| 项目 | 状态 | 严重度 |\n|---|---|---|\n| 覆盖率工具 | `cargo-llvm-cov` 未安装,仓库也没有任何覆盖率配置/badge/门禁。 | High(缺失本身) |\n| CI 门禁 | 仓库根目录**没有 `.github/`**(只有 vendored `CosyVoice/` 子模块自带的 CI,与本项目无关)。`git log` 显示这是一个高频 commit(`feat`/`test`/`fix` 交替)的活跃项目,却没有任何自动化跑测试的机制——意味着 `cargo test --workspace` 是否保持绿色完全靠开发者本地自律。 | **Critical** |\n| Flaky 风险 — `thread::sleep` | `llm.rs:1602` mock server 用 `thread::sleep(Duration::from_millis(20))` 模拟分片到达延迟。这是已知的\"用 sleep 制造时序\"反模式——在 CI 资源紧张/并行跑测试时,如果消费端处理变慢,理论上仍可能因 socket buffer 行为差异产生 flaky(目前用真实 TCP loopback,问题概率低,但比起用 channel-based 受控注入仍多了一层不确定性)。 | Low-Medium |\n| Flaky 风险 — `runtime_bridge.rs:487` `std::thread::sleep(Duration::from_millis(FRAME_MS as u64))` | 这是生产代码(音频采集主循环)里的 sleep,不是测试,但说明实时音频路径靠\"sleep 30ms\"做帧节奏控制——不算测试问题,但如果将来给它写时序敏感的单测,会天然带 flaky 风险。 | N/A(标注供参考) |\n| 测试运行本身 | 本次审计实测 `cargo test --workspace`(`export PYO3_PYTHON=...`)全绿:capabilities 30 passed,edge-agent lib 56 passed,middleware-service 25 passed。没有发现当前会失败或挂起的测试。 | 健康 |\n| 网络依赖 | `llm.rs` 的 mock-server 测试绑定 `127.0.0.1:0`(系统分配端口),不依赖外部网络,这点处理得对,不会因端口冲突或外网不可达而 flaky。 | 健康 |\n\n---\n\n## 6. Test Double 质量\n\n| Double | 位置 | 评价 |\n|---|---|---|\n| `StubLlm` | `reasoning.rs:294-319` | **过度简化**:`chat()` 永远返回空响应且从未被调用验证,纯粹是为了满足 trait bound 而存在的死代码式实现。建议要么删掉这个 trait 方法的实现细节断言,要么在测试里显式断言\"chat() 不应被 run_turn 调用\"(目前隐含假设,没写出来)。 |\n| `ScriptedLlm` | `reasoning.rs:321-356` | **设计合理**:用 `Mutex&lt;Vec&lt;Vec&lt;Chunk&gt;&gt;&gt;` 按调用顺序播放脚本、并记录 `calls` 供断言\"工具结果是否被正确回灌进下一轮消息\"——这是测试多轮 ReAct 循环的恰当方式,不是过度 mock。`run_turn_feeds_tool_result_back_into_next_round`(`reasoning.rs:501`)这条测试质量好,验证的是契约(工具结果出现在下一轮 messages 里)而非实现细节。 |\n| `StaticLlm`/`FailingLlm`(`capabilities::llm`) | `llm.rs:939-1126` | **合理**,且是生产代码的一部分(不是仅供测试的 mock),被 `LlmRouter` failover 测试复用得当。`StreamThenFailLlm`(`llm.rs:1616`)专门构造\"先发一个 chunk 再失败\"来验证 `forwarded_any` 状态机,这是**针对契约设计的 double**,质量高。 |\n| `StaticAsr`/`StaticCapture`/`SilentTts`(`capabilities` crate) | `runtime.rs` tests | 合理,但如 §1 所述,这些 double 把\"感知/合成正确\"这件事抽掉了,导致 `runtime.rs` 层的测试只验证编排是否正确转发,**没有任何测试同时验证编排 + 真实 VAD/ASR 适配器的组合行为**——这中间空了一层(`runtime_bridge.rs` 单测 VAD,`runtime.rs` 单测编排,两者从未被同一条测试串起来)。 |\n| HTTP mock server(`serve_streaming_response`,`llm.rs:1577`) | 手写 raw TCP server,不用 `wiremock`(handoff §7 提到的备选方案) | 合理且更轻量,正确模拟了分片到达延迟(`thread::sleep`)以触发\"边收边发\"的真实断言(`first_token_ms.is_some()`、连续 2 个 chunk 各自到达)。这是**不依赖 mock 的真实网络行为验证**,质量在所有 double 里最高。 |\n\n---\n\n## 优先级行动清单(按性价比排序)\n\n1. **【Critical / 立即做】给 `ReasoningStage::run_turn` 补取消竞态测试** — 手段:构造一个\"可在 chunk 间插入受控延迟/在第 N 个 chunk 后触发外部取消\"的测试用 LLM,断言取消后 `tx` 上无新增 Action、task 在限定时间内返回。这是 B7 handoff 明确要求但完全未覆盖的 barge-in 核心契约,且 reasoning.rs 有 4 处 `tokio::select!` 取消点,目前一处都没测到。\n\n2. **【Critical / 立即做】解禁 `tests/e2e` 真集成路径** — 手段:给 middleware-service 加自动 spawn+健康检查 fixture,让\"必跑\"3 个测试能在无人值守环境下真正执行;评估给 `test_chat_completions_optional` 录制响应(VCR 式)代替依赖真实在线 LLM,使其能默认开启而不靠 `--run-gateway-chat`。当前这层测试在自动化环境下 100% 不会被执行。\n\n3. **【High / 本迭代做】给 `Runtime::run()` 补\"运行中回合被新 voice 打断\"的端到端测试** — 手段:spec-first 集成测试,用一个\"发送慢速 chunk 流\"的 StubLlm 起一个回合,在它运行中发第二条 voice 输入,断言第一个 `RunningTurn` 被 abort、第二个回合正常跑起来。目前只测了\"队列层面 voice 优先\"和\"replace_turn 取消 token\"两个局部,从未端到端验证。\n\n4. **【High / 本迭代做】补 `next_speech_segment` 的边界枚举测试 + property test** — 手段:枚举 `min_speech_ms` 不足、`max_segment_ms` 触顶、混合采样率三个分支(目前 `bail!` 分支 0 覆盖),再用 proptest 生成随机帧序列验证\"切句不变量\"。这是 voice 模式(B7 G2)主要新增量,目前只有 1 个 happy-path case。\n\n5. **【High / 本迭代做】为 `Runtime::check()` 写覆盖 4 种组合的单测** — 手段:纯单元测试,直接构造不同 builder 链验证每个 `bail!` 分支被正确触发。这是 OTA 回滚触发条件,函数本身极易测但目前 0 覆盖。\n\n6. **【Medium / 排期】给 `LineBuffer`/SSE-NDJSON 解析加 proptest** — 手段:property-based,验证\"任意切分字节序列重组后行内容不变\",顺带覆盖跨多字节 UTF-8 字符切分(当前真实风险点,CJK 文本场景高发)。\n\n7. **【Medium / 排期】Rust↔Python TTS wav 格式 differential 对拍** — 手段:用真实 Python TTS 跑样本,固化输出格式为 golden fixture,检测 Python 侧模型升级是否破坏了 Rust 侧硬编码的 mono/16bit 假设(`runtime_bridge.rs:267-273`)。\n\n8. **【Medium / 排期】移除/重写 `StubLlm` 的死代码式 `chat()` 实现,删除测试里的\"魔法数\"阈值断言** — 手段:清理循环验证根源。`model_router_uses_fast_for_short_text_without_image` 改为基于 spec 注释（若无 spec，标注为\"实现细节，待产品定义后修正\"）。\n\n9. **【Low / 长期】CI 门禁 + 覆盖率基线** — 手段:补 `.github/workflows`(或等价 CI),跑 `cargo test --workspace` + `python -m pytest python/agent/tests`;安装 `cargo-llvm-cov` 建立覆盖率基线(即使不卡门禁,先有数字)。这是地基级缺失，但短期 ROI 低于上述具体测试缺口。\n\n---\n\n**关键文件路径汇总**(供后续动作引用):\n- `/Users/youxun/project/edgeAgent/rust/crates/capabilities/src/llm.rs`\n- `/Users/youxun/project/edgeAgent/rust/apps/edge-agent/src/agent_core/reasoning.rs`\n- `/Users/youxun/project/edgeAgent/rust/apps/edge-agent/src/agent_core/runtime.rs`\n- `/Users/youxun/project/edgeAgent/rust/apps/edge-agent/src/agent_core/trigger.rs`\n- `/Users/youxun/project/edgeAgent/rust/apps/edge-agent/src/agent_core/guard.rs`\n- `/Users/youxun/project/edgeAgent/rust/apps/edge-agent/src/runtime_bridge.rs`\n- `/Users/youxun/project/edgeAgent/tests/e2e/conftest.py`\n- `/Users/youxun/project/edgeAgent/tests/e2e/test_llm_gateway_http.py`\n- `/Users/youxun/project/edgeAgent/tests/e2e/test_middleware_service_http.py`\n- `/Users/youxun/project/edgeAgent/docs/llm-stream-passthrough-handoff.md`\n- `/Users/youxun/project/edgeAgent/docs/agent-core-b7-integration-handoff.md`</result>\n<usage><subagent_tokens>119382</subagent_tokens><tool_uses>21</tool_uses><duration_ms>217535</duration_ms></usage>\n</task-notification>"]