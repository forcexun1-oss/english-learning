# English Learning Log — 2026-08-12

- 00:01 [ZH]  为什么B还快呢,这本身就说明问题把
- 00:05 [EN] STOP the attribution line of work. A likely measurement artifact has been found that may invalidate the entire A/B gap, including the negative result committed in 0647c77. Test it first.

THE FINDING. mlx_lm/models/switch_layers.py:1330-1341, inside the paged decode path:

    if os.environ.get("MLX_QWEN35_PAGED_QMM_DIAGNOSTICS", "").lower() in {...}:
        # Benchmark-only: inspect route ids after native dispatch inputs are
        # fixed. The normal paged path never reads routes back on the host.
        mx.eval(indices, expert_to_slot)
        route_ids = np.asarray(indices).reshape(-1)
        slots = np.asarray(expert_to_slot)

That is an mx.eval plus two host readbacks per layer -- 40 GPU-to-CPU fences per forward. The comment says outright that the normal paged path never does this. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1 is in the canonical B environment from run_qwen36_paged_b_allhit.sh, so it was set in EVERY B measurement in this investigation: Gate 1's 49 tok/s, the budget sweep, the 200-token confirmation, the ballast ladders, and the attribution run. A never takes this path and never paid it.

WHY THIS EXPLAINS EVERYTHING WE MEASURED. From your own attribution rows, recomputed by layer and percentile:
  - B steady-state per call, forced: median 0.210 ms. A: median 2.169 ms. B's kernel is ~10x FASTER, matching the isolated 0.3912 vs 0.5042 ms result.
  - B's mean is dragged to 2.434 ms by a tail of ~113 calls at 47-57 ms -- about one per (layer, projection), i.e. first-touch warmup. Top 5% of B's calls are 83.4% of its total time. Your headline 'A 7.3421 s vs B 5.5502 s forced sum' therefore compared A's steady state against B's one-time warmup.
  - A runs ~12 ms/token against 120 x 2.17 ms serialized, so A overlaps roughly 18x. B runs ~25 ms/token against 120 x 0.21 ms serialized, so B overlaps 1x -- it is already fully serialized. A per-layer host fence is exactly what would cause that.
  - It also explains why adding your instrumentation cost A 61% (83 -> 32.63 tok/s) and cost B nothing (37-40 -> 41.18): A had overlap to lose, B had none left.

Also note your reported numbers were wrong on units: 'B unforced decode 6.2540 s' is a sum of per-call intervals, while 'A unforced decode 0.4903 s' is wall time. B's actual unforced decode wall was 0.388 s -- B was FASTER than A in that run. Correct that wherever you wrote it.

THE EXPERIMENT. Rerun B with MLX_QWEN35_PAGED_QMM_DIAGNOSTICS UNSET, everything else identical, and compare against A. Residency does not depend on the run: the placement file is complete 40x256, so _paged_qmm_complete is True and resident_only is selected deterministically. Verify residency ONCE with diagnostics on, then measure throughput with it off -- and say plainly in the report that hit-rate/backing-route telemetry is unavailable in the measured runs because it is produced only by the block being removed. That coupling is itself a defect worth recording: the only way to verify full residency is to enable the fence that destroys the thing being measured.

Run: A and B back to back in one session, same memory conditions, 200 tokens, agent prompt. Report tok/s for A, B-with-diagnostics, and B-without-diagnostics, plus token SHAs, which must match.

Three outcomes, all publishable:
  - B-without jumps to or past A -> the entire 83 vs 37 gap was a diagnostics artifact. Then 0647c77's negative result is WRONG and must be corrected, and the mixed-cache conclusion about backing routes needs re-testing too, since those sweeps carried the same fence.
  - B-without improves but stays below A -> the fence was part of it; report the split.
  - B-without is unchanged -> the fence is not the cause and the negative result stands. Say so plainly.

Do NOT skip straight to rewriting conclusions. Measure first. Interpreter is the release build; verify hasattr(mx,'get_external_memory')==True. Memory gate available = free+inactive+purgeable >= ~8 GB, stored compressor flat over 60s, never bare 'Pages free'. One model at a time, TERM never -9, do not push. Worktree is clean at 700e637.
- 00:12 [EN] <task-notification>
<task-id>bjirakvg1</task-id>
<tool-use-id>toolu_01Lh1y4TNXWt5cajsSpLQSvi</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bjirakvg1.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex to test the diagnostics-fence artifact" completed (exit code 0)</summary>
</task-notification>
- 00:14 [ZH] 去做吧
- 00:15 [EN] Three tasks, in order. Your diagnostics-fence measurement was good work and the caution about SHA parity was right -- now close it out.

ESTABLISHED FROM YOUR RUN, do not re-derive: A 78.516 tok/s (12.74 ms/token), B diagnostics-on 36.805 (27.17 ms), B diagnostics-off 52.169 (19.17 ms). The fence costs 8.00 ms/token, which is 55.4% of the 14.43 ms A-vs-B gap. A real 6.43 ms/token gap remains and A is still 1.50x faster, down from 2.13x. Artifacts in .codex_logs/qwen36_diag_artifact_20260812/.

TASK 1 -- get the clean gap number at matched SHAs. Your parity objection is valid but narrower than you framed it: A and B already agree at 4 tokens (bdabf4a9...) and at 16 tokens (72cfec28...); they only diverge by 200 (ef07c59a vs 16081219), which is late accumulation of tiny numerical differences, already known as the unfinished 'matched-path audit' in the Gate 1 report. So rerun A vs B-diagnostics-off at 16 tokens, where the SHAs match, back to back in one session. Assert SHA equality; if it now fails at 16 tokens too, that is a correctness finding and outranks the throughput question -- stop and report it. Otherwise report the clean gap in both tok/s and ms/token.

TASK 2 -- re-test the backing-route conclusion, which is now in doubt. The budget sweep (25/20/16/12 GiB -> 40.10/12.68/8.21/5.77 tok/s) carried the fence at every point, and paths with backing routes read back more per layer, so the fence cost may scale with declining hit rate. That means the published claim -- '124k backing routes cost 68% of throughput' and 'mixed cache only viable at hit rate ~1.0' -- may be substantially fence, not backing. Rerun the sweep with MLX_QWEN35_PAGED_QMM_DIAGNOSTICS UNSET. Verify residency structurally from the placement file rather than from telemetry, since the counters only exist inside the removed block, and say so. Report the with-fence and without-fence curves side by side so the fence's contribution at each hit rate is visible.

TASK 3 -- correct the record. Commit 0647c77 states B at 37-40 tok/s and asserts the backing-route constraint; both came from fence-contaminated runs. It is already committed, so add a corrective commit that states the measured fence contribution, the revised B figure, the clean gap from Task 1, and the revised or confirmed backing-route curve from Task 2. Do not rewrite history. Also record the design defect explicitly: resident_hit_rate and backing_routes are produced ONLY inside the diagnostics block at switch_layers.py:1330-1341, so the only way to verify full residency is to enable the fence that destroys the performance being measured. That coupling should be fixed -- residency verification should not require a per-layer host readback -- but propose it, do not implement it in this pass.

If Task 1 or 2 changes the sign of a published conclusion, say so in plain terms rather than softening it. A wrong committed claim corrected quickly costs far less than one left standing.

Unchanged: release interpreter, verify hasattr(mx,'get_external_memory')==True; canonical A and B environments otherwise verbatim, never MLX_EXPERT_MMAP or MLX_EXPERT_MMAP_PACKED; agent prompt; one model at a time; composite watchdog TERM only; memory gate available = free+inactive+purgeable >= ~8 GB with stored compressor flat over 60s, never bare 'Pages free'; record starting memory with every measurement; do not push. Worktree clean at 700e637.
- 00:32 [EN] what's the stutas
- 00:34 [ZH] 跑完结论写一下,然后,让电脑休眠把
- 00:35 [EN] <task-notification>
<task-id>b0fnkdii4</task-id>
<tool-use-id>toolu_01TBAhoJDVevvyshYYPksWAQ</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b0fnkdii4.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex to close out the fence finding" completed (exit code 0)</summary>
</task-notification>
- 09:01 [ZH] 所以现在有方案解决了吗
- 09:13 [EN] go ahead
- 09:15 [EN] Execute Task 1 of docs/plans/2026-08-12-command-buffer-submission-cadence.md in full. Read that plan first, and read docs/experiments/2026-08-12-paged-qmm-investigation-conclusions.md before it for context on what A and B are, what the 6.15 ms/token residual is, and what has already been refuted.

The short version: MLX's CommandEncoder::needs_commit() (mlx/backend/metal/device.cpp:513-516) commits a command buffer when buffer_sizes_ exceeds max_mb_per_buffer_, which is 40-50 MB on this device. buffer_sizes_ accumulates in set_input_array (device.cpp:343-349), counting each distinct buffer once. B binds a whole ~220 MB per-layer resident slab on every ResidentGatherQMM, so B may be committing one command buffer per QMM -- roughly 120 GPU submissions per decode forward -- while A batches. Both limits are runtime env vars read via get_var in mlx/utils.h:178-188, so this needs NO MLX rebuild for Task 1.

Do Task 1 only: sweep MLX_MAX_MB_PER_BUFFER over 40, 256, 1024, 4096 for both A and B, 16 tokens, everything else fixed, using the existing drivers .codex_logs/qwen36_control_a_stream_driver.py and .codex_logs/qwen36_paged_b_allhit_driver.py. Do NOT start Task 2; it needs a rebuild and only runs if Task 1 moves.

Hard constraints, all of them in the plan, restating the ones that have been violated before in this arc:

1. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS must be unset or 0 at every single point. That flag is the per-layer mx.eval fence that contaminated every previously published B number in this investigation. If a point seems to need it for residency telemetry, that point is invalid -- report that, do not enable it.
2. Token-id SHA must stay 72cfec2812ddc4f65f83dc847a047b8e0b6db4da at every point. If it changes, the comparison is broken: flag it and stop.
3. Only the 25 GiB full-resident B point. Do not run 20/16/12 GiB -- those hang after the first token with diagnostics off and that is a separate open bug.
4. One large model at a time. Memory gate before each real-model run: available = free + inactive + purgeable >= ~8 GB and stored compressor flat over 60s. Use 'stored', not 'occupied'. Do NOT gate on bare 'Pages free' -- it excludes reclaimable pages and has produced two false stops already in this project. Use the existing composite watchdog benchmarks/qwen35_paged_probe_watchdog.py, TERM only. Raising MLX_MAX_MB_PER_BUFFER makes each command buffer hold more memory and this machine has a Metal OOM history, so abort a point rather than allowing swap.
5. Record per point: tok/s, token SHA, minimum decode-time available memory, stored compressor before/after, mlx_active and mlx_external.

The prediction and the falsifier are both stated in the plan before running, and you must honour the falsifier: if B is FLAT across 40 -> 4096, the MB cap is not the binding constraint and the hypothesis is dead -- record that plainly and stop. If A moves as much as B, the mechanism is not B-specific and the hypothesis also fails -- same outcome. A refutation is a valid and valuable result here; three hypotheses have already been killed in this arc. Do not adjust the sweep to rescue the hypothesis, and do not report only the axis that looks better.

Write the results as a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. Do not push; origin is an unplugged external drive.
- 09:27 [EN] <task-notification>
<task-id>bz0cpentd</task-id>
<tool-use-id>toolu_012bpjeos4bYrKYdPRzfwZcr</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bz0cpentd.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex to run Task 1 env sweep" completed (exit code 0)</summary>
</task-notification>
- 09:35 [EN] Rerun Task 1 of docs/plans/2026-08-12-command-buffer-submission-cadence.md. Read the plan again -- it now has a 'Run-1 correction (2026-08-12)' section that you must read before doing anything. The A side is done and valid; do NOT rerun A. Only the B ladder needs running.

What went wrong in run 1, stated plainly so it is not repeated:

Your B/40 point never ran the resident path at all. Its driver.json reported expert_cache.entries=0, resident_bytes=0, resident_debug.calls=0, resident_only_dispatches=0. Compare the known-good diagnostics-off 25 GiB point at .codex_logs/qwen36_closeout_20260812/sweep_off/25g/probe.stdout, which reports entries=40, bytes=26172497920, resident_debug.calls=2274, resident_only_dispatches=2280 -- also with diagnostics off. So those counters are NOT diagnostics-gated, and all-zero means the path genuinely did not execute.

The cause is a missing placement manifest. The loader reads it from MLX_QWEN35_PAGED_QMM_PLACEMENT (mlx_lm/utils.py:467). The prior valid run generated one first; its placement.log records 'full_placement.json: 40 layers x 256 experts = 10240 entries'. You set no placement, so the loader installed nothing and B silently fell back to a third configuration that is neither A nor B. Its 58.344 tok/s was therefore not a B measurement at all, and the token-SHA mismatch you stopped on was a symptom of that fallback rather than the finding. benchmarks/run_paged_qmm_attribution.sh lines 67 and 81 already generate the placement and set the env var correctly -- use that as your template.

Two corrected rules for this rerun:

1. RESIDENCY LANDING ASSERTION. Before you accept any timing number from a B point, assert expert_cache.entries == 40, resident_bytes within a few percent of the 25 GiB budget, and resident_only_dispatches > 0. If the assertion fails, abort that point loudly as a setup failure -- never report a throughput number from a silently-empty cache. This is the same landing-assertion lesson the ballast ladder already produced in this project.

2. THE COMPRESSOR GATE MEANS 'NOT RISING', NOT 'FLAT'. You rejected a gate window where stored compressor FELL from 4.693 to 4.132 GiB at about 20 GiB available. That is a machine getting cleaner and it passes. The prior valid run proceeded on a window that also fell, 7.264 to 7.211. Only a sustained RISE blocks a point. The plan's original 'flat over 60s' wording caused this and has now been fixed in the plan.

Now run the B ladder: MLX_MAX_MB_PER_BUFFER at 40, 256, 1024, 4096, 25 GiB resident budget, QWEN_GATE1_MODE=resident_only, QWEN_GATE1_MAX_TOKENS=16, diagnostics flag 0 at every point, placement manifest generated and passed at every point. Everything else exactly as in the plan.

Unchanged hard constraints: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS must be 0 or unset at every point -- never enable it. Token-id SHA must be 72cfec2812ddc4f65f83dc847a047b8e0b6db4da at every point once the residency assertion passes; if residency lands and the SHA still differs, that is a real finding and you should stop and report it, because A and the known-good B share text_sha1 c9248616e37bd63654c95c37aa390b55faf372bd. Only the 25 GiB budget -- no 20/16/12 GiB. One model at a time, TERM-only composite watchdog, abort a point rather than allowing swap since raising MLX_MAX_MB_PER_BUFFER makes each command buffer hold more memory and this machine has a Metal OOM history.

For reference, A's completed and valid results were 82.957, 85.340, 85.302, 84.929 tok/s at 40/256/1024/4096, all with the correct SHA -- flat, as predicted.

The falsifier is unchanged and you must honour it: if B is FLAT across 40 to 4096 with residency landing at every point, the MB cap is not the binding constraint, the hypothesis is dead, record it plainly and stop. Do not adjust the sweep to rescue it.

Update docs/experiments/2026-08-12-command-buffer-submission-cadence.md in place with the corrected run -- keep the A table, replace the B section, and keep an honest record of the run-1 setup failure rather than deleting it. Commit. Do not push; origin is an unplugged external drive.
- 09:43 [EN] <task-notification>
<task-id>bchnssnlf</task-id>
<tool-use-id>toolu_019qkh8oE5RiGPtkkcNGser5</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bchnssnlf.output</output-file>
<status>completed</status>
<summary>Background command "Re-dispatch codex with corrected B setup" completed (exit code 0)</summary>
</task-notification>
- 09:47 [EN] Run the positive control now specified in the 'Follow-up' section of docs/experiments/2026-08-12-command-buffer-submission-cadence.md. Read that whole file first, including the 'Interpretation' section, which I have rewritten since your last run.

Your ladder executed cleanly -- residency landed at every point (40 entries, 26,172,497,920 bytes, 2280 resident-only dispatches, correct SHA). That part was right. But your conclusion was wrong and I have withdrawn it. You read B's +10.9% from 40 to 256 MB against a single default-cap point. Two further replicates of that same default-cap configuration already exist in this repo: 18.12 ms/token in the conclusions doc and 19.329 ms/token in .codex_logs/qwen36_closeout_20260812/sweep_off/25g. Your ladder's default-cap point was 20.126. So default-cap B has been measured three times spanning 2.0 ms/token, and your raised-cap band (18.150, 18.778, 18.257; mean 18.40) sits inside that spread. The effect is not distinguishable from run-to-run variance. A over the same ladder was flat to 0.36 ms. Task 2 is NOT authorized and no MLX rebuild is to be started.

There is also a structural gap: needs_commit() is an OR of two limits and you only varied one. max_ops_per_buffer_ stayed at 40 while a decode forward issues about 120 QMM primitives plus attention, norms and routing, so once the MB cap clears the slab size B just falls back to committing every ~40 ops, which is A's regime. The gap should then close, and it did not.

Run exactly three points, 16 tokens each, everything else as in the plan:

1. POSITIVE CONTROL, the important one. A packed mmap with MLX_MAX_OPS_PER_BUFFER=1, MB cap at default. This forces a command-buffer commit after every primitive, which is the exact regime B was hypothesised to be stuck in. Report ms/token from step_latency_ms mean, not just tok/s. A's baseline on this ladder is 12.854 ms/token at default caps.

2. B at 25 GiB with MLX_MAX_OPS_PER_BUFFER=4096 AND MLX_MAX_MB_PER_BUFFER=4096. Maximal batching on both limits -- the condition your ladder never reached.

3. One more B replicate at both caps DEFAULT, to add a fourth point to the variance band.

How to read it, fixed before running: if A at ops=1 collapses from 12.85 toward B's ~18 ms/token, submission cadence is a real and large cost on this hardware and the hypothesis is live. If A at ops=1 barely moves, cadence is cheap here, the hypothesis is DEAD, and you should say so plainly and stop. Point 1 alone settles it, so run it first and report its number even if the other two are aborted for memory reasons. Do not treat anything smaller than the 2.0 ms/token variance band as an effect.

All prior hard constraints still apply: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0 at every point, never enabled. Generate and pass the MLX_QWEN35_PAGED_QMM_PLACEMENT manifest for every B point and assert residency lands (entries==40, resident_bytes near the 25 GiB budget, resident_only_dispatches>0) before accepting any timing -- that is the failure that invalidated your first run. Token-id SHA must be 72cfec2812ddc4f65f83dc847a047b8e0b6db4da. Only the 25 GiB B budget. One model at a time, TERM-only composite watchdog. Memory gate: available = free+inactive+purgeable >= ~8 GB and stored compressor NOT RISING over 60s -- falling or steady passes, only a sustained rise blocks.

Update docs/experiments/2026-08-12-command-buffer-submission-cadence.md in place with a new section for this control, keeping the existing tables and the corrected interpretation intact. If the result kills the hypothesis, record that as the outcome -- four hypotheses have already been killed in this arc and a fifth is a fine result. Commit. Do not push; origin is an unplugged external drive.
- 09:58 [EN] <task-notification>
<task-id>bkduidj6f</task-id>
<tool-use-id>toolu_018ZjZ9o7JvQtrGi8QgREYmc</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bkduidj6f.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex positive control" completed (exit code 0)</summary>
</task-notification>
- 10:00 [EN] Run the three checks in the 'The discriminating test' section of docs/experiments/2026-08-12-command-buffer-submission-cadence.md. Read that file first, especially the new 'Reading of point 2' section, which I added after your last run.

Your point 1 result stands and was well executed: A at ops=1 went 12.854 -> 15.9462 ms/token, a real 3.09 ms cost that exceeds the 2.0 ms variance band. Cadence is a real cost on this hardware. You were also right to refuse to compare point 2's timing while its SHA was wrong, and right to abort point 3.

But you filed point 2 as merely 'invalid'. It is the most informative point in the whole investigation, and the ladder you already ran isolates the variable:

- Your earlier ladder ran MB=4096 with ops=40 and got the CORRECT SHA 72cfec2812ddc4f65f83dc847a047b8e0b6db4da at 18.257 ms/token. So MLX_MAX_MB_PER_BUFFER does not alter output.
- Point 2 changed only MLX_MAX_OPS_PER_BUFFER from 40 to 4096, and the SHA changed to 4fddfb8029badcec3b3eb955884118f7dd6f01b6.

So batching more primitives into one command buffer changes B's numerical output, and B at that setting measured 13.6855 ms/token against its 18.1-20.1 band and A's 12.49-12.85. That is roughly 90% of the residual gap, with residency landed. If the output can be made correct at that setting, B reaches near parity with A.

Run these three, in this order:

1. THE DISCRIMINATOR, run it first. A packed mmap at MLX_MAX_OPS_PER_BUFFER=4096 and MLX_MAX_MB_PER_BUFFER=4096, 16 tokens. Report the token-id SHA and step_latency_ms.mean. If A stays on 72cfec28... while B diverges under the identical setting, the defect is in ResidentGatherQMM specifically, not in MLX batching generally. Report this number even if the later points abort.

2. B at 25 GiB, ops=4096, MB=4096, run TWICE more (two independent processes). Report both token-id SHAs. If the SHA varies between identical runs, that proves a race. If it is stably 4fddfb8029badcec3b3eb955884118f7dd6f01b6 across all three runs including your earlier one, it is a deterministic ordering difference instead. Report exactly what you observe -- both outcomes are informative and neither is a failure.

3. B ops threshold at MB=4096 fixed: ops = 160, then 640. You already have ops=40 correct and ops=4096 wrong at this MB setting, so these two points bracket where correctness breaks. Report SHA and ms/token for each.

For points 2 and 3 the token-SHA constraint is SUSPENDED as a stop condition -- recording which SHA appears IS the measurement. Do not abort a point because the SHA is wrong; abort only on memory-gate or watchdog grounds. But you must still assert residency landed (entries==40, resident_bytes near the 25 GiB budget, resident_only_dispatches>0) before recording any timing, and you must still generate and pass the MLX_QWEN35_PAGED_QMM_PLACEMENT manifest for every B point. A point whose residency did not land is a setup failure and must be discarded, not recorded.

Unchanged: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0 everywhere, never enabled. Only the 25 GiB B budget. 16 tokens. One model process at a time, TERM-only composite watchdog. Memory gate: available = free+inactive+purgeable >= ~8 GB, stored compressor NOT RISING over 60s -- falling or steady passes. Do not start an MLX rebuild and do not begin Task 2 instrumentation; these are all runtime env-var checks.

Also note for your own bookkeeping: your point 1 wrapper hit a shell reserved-variable error in post-run bookkeeping. Fix that in the harness so it does not recur, since it makes a clean child run look like a failed one.

Update docs/experiments/2026-08-12-command-buffer-submission-cadence.md in place with a new section for these three checks, keeping all existing tables and interpretation intact. Commit. Do not push; origin is an unplugged external drive.
- 10:19 [EN] <task-notification>
<task-id>b5sh28fw3</task-id>
<tool-use-id>toolu_01Lxy4F6ruYQrFmWrxe51xqn</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b5sh28fw3.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex discriminating tests" completed (exit code 0)</summary>
</task-notification>
- 10:34 [ZH] 让codex去做
- 10:36 [EN] Execute docs/plans/2026-08-12-submission-count-and-ops-corruption.md. Read the plan first, and read docs/experiments/2026-08-12-command-buffer-submission-cadence.md before it -- especially the 'What the discriminator settled' section, which I wrote after your last run and which revises your conclusion.

Your last run was well executed and you were right to record what you saw rather than force a verdict. Two corrections to how it was filed:

First, your point 1 discriminator came back NEGATIVE and that is the important part. A also diverges at ops=4096 (SHA 759f124f instead of 72cfec28), so the output corruption is NOT specific to ResidentGatherQMM -- it is general to this MLX build. The unifying 'one defect explains everything' framing I had proposed is withdrawn; it required the defect to be B-specific and it is not.

Second, you filed B's wrong-SHA points as unusable. Compared against A they are, but compared against B ITSELF at constant work they are the most valuable measurement in the arc. At MB=4096 with identical residency and identical 2280 resident-only dispatches, B is 18.257 ms/token at ops=40 with correct output and 13.69 to 14.18 at ops=640 and 4096 with wrong output. That is ~4.6 ms/token from batching alone, 80% of B's 5.7 ms deficit against A, with the work held constant. The work-constant check is what makes it valid, and it matters because your ops=160 point proves wrong output CAN skip work -- it emitted one token on only 480 dispatches. 640 and 4096 carried the full 2280.

So the prize is now measured and the rebuild is authorized. Three parts, in order:

PART A, do this FIRST and do not touch the 35B until it is done. Reproduce the ops-threshold corruption on a small MLX model already on disk: fixed token count, greedy, MLX_MAX_MB_PER_BUFFER=4096, sweep MLX_MAX_OPS_PER_BUFFER over 40/160/640/4096, compare token-id SHAs. If it reproduces there, every subsequent debug cycle costs seconds instead of a memory-gated 35B run and all of Part C happens on the small model. If it does not reproduce, say so and record which model and token counts you tried. No memory gate needed for small models, but never run one concurrently with a 35B process.

PART B, count submissions. Add CPU-side atomic counters to the MLX fork following the existing pattern at mlx/ops.cpp:27-45, gated behind an env flag that is OFF by default so the release path is untouched when unset. Count command-buffer commits (CommandEncoder::commit, device.cpp:518), end_encoding calls (device.cpp:426), total dispatches, and -- this has never been distinguished -- how many commits were triggered by the ops arm versus the MB arm of the needs_commit OR at device.cpp:513-516. Then run A and B at ops=40 and ops=4096 with MB=4096, 16 tokens each, and report commits per decode forward. 2280 resident dispatches equals 19 forwards times 120, so per-forward figures are derivable without token-boundary signposts.

THE HARD CONSTRAINT ON PART B: this must not fence. No mx.eval, no host readback of any MLX array, no per-call synchronization, nothing that drains the GPU. Per-call instrumentation is proven to destroy A's overlap -- it cost A 61% of its throughput in an earlier run in this arc, and a diagnostics fence exactly like it contaminated every published B number before it was caught. A design that serialises A is invalid and must be discarded, not reported. Increment atomics, dump totals at process exit.

Part B has a stated falsifier you must honour: if A and B commit at similar rates at ops=40, then 'fewer submissions' is wrong, the 4.6 ms comes from something else, and the whole investigation redirects. Record that plainly if you see it.

PART C, why high ops-per-buffer changes output. Before any deeper theory, check whether Metal is reporting an error that MLX discards: inspect MTL::CommandBuffer status() and error() after completion and log any non-Completed status. CommandEncoder::commit already installs a completion handler that reads event->error() -- extend that one rather than adding a second. Only if Metal reports nothing should you look at the barrier bookkeeping in CommandEncoder::maybeInsertBarrier (device.cpp:391-404), which is a two-epoch prev/next scheme that swaps rather than merges when a barrier fires. Note that all seven ResidentGatherQMM inputs already go through set_input_array (quantized.cpp:1188-1194), so a naively missing binding is excluded. Your explanation has to fit all four facts: deterministic (4fddfb80 across three independent processes), affects the ordinary packed path too, silent (clean exit, no watchdog kill, no Metal error printed), and appears between 40 and 640 ops with a degenerate single-token case at 160.

BUILD: use CMAKE_ARGS with -DUSE_SYSTEM_FMT=ON and -DFETCHCONTENT_SOURCE_DIR_METAL_CPP pointed at a local checkout. fmt lacks GIT_SHALLOW TRUE and stalls cloning full history; metal-cpp fails CMake's own download on this box even though curl fetches it in under a second. The venv_build interpreter imports the editable extension, so your rebuild replaces what every other experiment in this arc uses -- RESTORE THE RELEASE BUILD when finished and verify hasattr(mx, 'get_external_memory') == True before declaring done. Do NOT enable MLX_METAL_DEBUG; a prior attempt with it produced empty command-buffer and encoder tables and resolved nothing, which is exactly why Part B's counters exist.

Unchanged ground rules: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS stays 0, never enabled. Every B point generates and passes MLX_QWEN35_PAGED_QMM_PLACEMENT and asserts residency landed (entries==40, resident_bytes near the 25 GiB budget, resident_only_dispatches>0) before any timing is recorded -- a point whose residency did not land is a setup failure, not a measurement. Only the 25 GiB B budget. One large model at a time, TERM-only composite watchdog. Memory gate: available = free+inactive+purgeable >= ~8 GB and stored compressor NOT RISING over 60s; falling or steady passes, only a sustained rise blocks. Anything smaller than 2.0 ms/token is inside B's known variance and is not an effect.

Write results into a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. Commit the MLX fork changes separately in that repo with the counters gated off by default. Do not push; origin is an unplugged external drive.
- 10:38 [ZH] mmap不能做按需加载吗,必须全量缓存?
- 10:41 [EN] Both of your blockers are resolved. Resume docs/plans/2026-08-12-submission-count-and-ops-corruption.md.

BLOCKER 1, small model: you searched /Users/youxun/models, where the small entries are 0-byte placeholder dirs. The real ones are in the HuggingFace cache. Use:

  mlx-community/Qwen1.5-0.5B-Chat-4bit
  /Users/youxun/.cache/huggingface/hub/models--mlx-community--Qwen1.5-0.5B-Chat-4bit/snapshots/659d8dafc39202a6688bb46242d60440702489b1/

That snapshot is 260 MB with a real model.safetensors. Qwen1.5-0.5B is dense, not MoE, which is fine and in fact better for Part A: the ops-threshold corruption hits the ordinary packed path too (A diverged at ops=4096), so if it reproduces on a plain dense model the defect is confirmed as unrelated to MoE and to paged-QMM entirely, and every Part C debug cycle costs seconds. Also note /Users/youxun/.cache/huggingface/hub/models--mlx-community--Qwen3-4B-4bit and Llama-3.2-1B-Instruct-4bit exist but are 15-17 MB metadata-only, so their weights are not downloaded -- do not try those.

If the corruption does NOT reproduce on the 0.5B dense model, that is itself informative: record it, and say whether you think the next cheapest probe is a small MoE or going straight to the 35B.

BLOCKER 2, the MLX fork's uncommitted changes: your option 2 does not exist. Those 1342 insertions across 16 files ARE the ResidentGatherQMM implementation and the external-buffer accounting -- they are the subject under test. A clean release tree would not contain B at all, so there is nothing to point you at.

Take option 1, with one addition. First commit the pre-existing work as its own commit on branch feat/array-from-ptr-zerocopy, with a message stating plainly that it is pre-existing work from the paged-QMM arc that you did not author and are committing only to establish a clean base. Do not mix it with anything of yours. Then add your counters as a separate commit on top. This matters because your counters need mlx/ops.cpp, which is already dirty, so a path-scoped commit would otherwise sweep that pre-existing work into your commit silently. Note mlx/backend/metal/device.cpp is currently clean, so most of the counter work lands there without collision.

Everything else in the plan stands unchanged, in particular:

- Part B must not fence. No mx.eval, no host readback of any MLX array, no per-call synchronization. Per-call instrumentation cost A 61% of its throughput earlier in this arc and a diagnostics fence exactly like it contaminated every published B number before it was caught. A design that serialises A is invalid and must be discarded, not reported. Atomics incremented in C++, totals dumped at process exit, behind an env flag that is OFF by default.
- Part B's falsifier: if A and B commit at similar rates at ops=40, then 'fewer submissions' is wrong, the 4.6 ms/token comes from something else, and the investigation redirects. Record that plainly if you see it.
- Part C starts by checking whether Metal reports an error MLX discards -- MTL::CommandBuffer status() and error() after completion -- before any barrier theory. Extend the completion handler already in CommandEncoder::commit rather than adding a second one.
- Do not enable MLX_METAL_DEBUG. It produced empty tables in a prior attempt and is exactly what the counters replace.
- Restore the release MLX build when finished and verify hasattr(mx, 'get_external_memory') == True before declaring done. The venv_build interpreter imports the editable extension that every experiment in this arc depends on.
- 35B rules if you get there: diagnostics flag 0, placement manifest generated and residency asserted (entries==40, resident_bytes near the 25 GiB budget, resident_only_dispatches>0) before any timing, 25 GiB budget only, one large model at a time, TERM-only watchdog, memory gate on available >= ~8 GB with stored compressor not rising. Nothing under 2.0 ms/token counts as an effect.

Write results into a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, commit in mlx-lm, and commit the MLX fork changes separately as described. Do not push; origin is an unplugged external drive.
- 10:46 [ZH] 那我们做好A路线不就好了,我只要按全量内存逐步减少内存,找出一个最佳位置就好了,A路径是否真的是按需,还是其实也加载缓存到内存了,这个要说清楚
- 10:51 [ZH] 停掉Part B,留Part C,A路径驻留sweep顶上来
- 10:55 [EN] Scope change. Stop work on Part B of docs/plans/2026-08-12-submission-count-and-ops-corruption.md -- it is cancelled. Read the scope-change block now at the top of that plan.

Your work so far is kept and was correct: commit 565155e in the MLX fork established the clean base as instructed, and your submission counters in mlx/backend/metal/device.cpp are gated behind MLX_METAL_SUBMISSION_COUNTERS which is unset by default. Keep them -- commit counts are useful evidence for Part C. Just do not run Part B's four 35B measurement points. I verified the extension still imports, gpu math works, and hasattr(mx, 'get_external_memory') is True, so the build is intact.

The reason for the change: A -- packed mmap streaming -- is the production path and it wins on both axes. 12.5 ms/token against B's 18.3, 1.86 GiB MLX-owned against 26.235, 2.37 GiB peak RSS. And it is genuinely on-demand, confirmed three ways from source: wired_limit_ defaults to 0 (allocator.h:75) and capacity_ to 0 (resident.h:31), so ResidencySet::insert sends every buffer to unwired_set_ which is bookkeeping only, and A never calls set_wired_limit; _stream at switch_layers.py:1635-1637 returns from _packed_decode before the cache path, with hits=0 AND misses=0 against a 12 GiB budget; and _packed_decode hands the whole mapped tensor to gather_qmm with rhs_indices so only routed pages fault in. Nothing wired, nothing cached, nothing copied. So explaining B's submission deficit is off the critical path.

Part C keeps its priority and is NOT cancelled, because the ops-threshold corruption is not a paged-QMM defect -- the ordinary packed path A diverges too, at ops=4096, SHA 759f124f instead of 72cfec28. That is a silent deterministic correctness bug on the path we are shipping. Part A, the small-model reproduction, stays as Part C's prerequisite; use mlx-community/Qwen1.5-0.5B-Chat-4bit at /Users/youxun/.cache/huggingface/hub/models--mlx-community--Qwen1.5-0.5B-Chat-4bit/snapshots/659d8dafc39202a6688bb46242d60440702489b1/ (260 MB, real weights, dense not MoE, which is fine and in fact better).

Now execute the NEW primary plan first: docs/plans/2026-08-12-a-path-residency-sweep.md. Read it in full. Summary of what it asks:

Sweep MLX_EXPERT_RESIDENT_GB over 0, 4, 8, 12, 16, 20 on the A path, 16 tokens, everything else fixed. Watch the validation at switch_layers.py:55-62 -- MLX_EXPERT_RESIDENT_GB must be <= MLX_EXPERT_STREAM_CACHE_GB which defaults to 12, so the 16 and 20 GiB points need STREAM_CACHE_GB raised to match or they raise ValueError before generating. Lock the two knobs to the same value at every point and say so in the record.

Run THREE replicates at the 0 GiB baseline FIRST to establish A's variance band before any comparison. A's spread across the earlier MB ladder was 0.36 ms/token so expect roughly 0.4 ms, much tighter than B's 2.0 -- but confirm it rather than assuming. Then two replicates at each remaining point.

LANDING ASSERTION, required before any timing is recorded. This is the exact failure that invalidated an entire earlier run of yours, so it is not optional: assert expert_cache.resident_bytes is within a few percent of the requested budget and exactly 0 at the 0 GiB point, resident_entries > 0 for every non-zero budget, and mlx_active rises while mlx_external falls as the budget rises. If mlx_active is still around 1.86 GiB at a 16 GiB budget then nothing landed. A point whose residency did not land is a setup failure, not a measurement -- abort it loudly and never report its throughput.

Do NOT set MLX_MAX_OPS_PER_BUFFER or MLX_MAX_MB_PER_BUFFER anywhere in this sweep. Leave the command-buffer caps at default. High ops values silently corrupt output on this build including on the A path, which is the subject of Part C.

Record per point: ms/token from step_latency_ms.mean (not the reciprocal of tok/s), tok/s, token SHA, mlx_active and mlx_external, peak RSS, pageins_delta, minimum decode-time available memory, and stored compressor before and after. pageins_delta matters on its own -- A reads 7.9 GB per 16-token run today, and if residency cuts that without moving tok/s it still matters for sustained serving.

The decision rule is fixed before running: nothing smaller than the measured 0 GiB variance band counts as an effect. If every point sits inside that band, the conclusion is 'residency does not matter on the A path, ship A at 0 GiB' -- that is a clean shippable answer, record it as the result rather than hunting for a better-looking axis. Task 2 of that plan, the 200-token sustained-decode check, runs ONLY if Task 1 produces a point outside the band.

Unchanged: MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0 and never enabled, token SHA must stay 72cfec2812ddc4f65f83dc847a047b8e0b6db4da, one large model at a time, TERM-only composite watchdog, memory gate on available = free+inactive+purgeable >= ~8 GB with stored compressor not rising over 60s (falling or steady passes).

Write results as a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. Then, and only then, proceed to Part A and Part C of the older plan. Do not push; origin is an unplugged external drive.
- 11:05 [EN] <task-notification>
<task-id>b39a8xbek</task-id>
<tool-use-id>toolu_01GJme6PkkuaVsK2ztGBT1Jx</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b39a8xbek.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex A-path residency sweep" completed (exit code 0)</summary>
</task-notification>
- 11:28 [ZH] 79是全量内存的效果吗
- 11:42 [ZH] 所以映射24.375 GiB这部分其实是全量在内存的?
- 11:46 [ZH] 那A这就够了啊,那我想知道内存是否真的能挤出去,挤出去之后的decode下降取消是多少
- 11:50 [EN] Execute docs/plans/2026-08-12-a-path-cache-eviction-cost.md. Read it in full first, and read docs/experiments/2026-08-12-a-path-residency-sweep.md before it, especially the section 'Is the mapped 24.375 GiB actually in RAM?'.

Context. The A-path residency sweep you ran is complete and I wrote it up -- the optimum is 0 GiB and I extracted your artifacts myself since you ran out before writing the doc. Good data, all 13 points landed. Two things I found while writing it up that you should know: the ladder never actually varied memory (mlx_active was 1.86 GiB at every point, RSS flat at 2.1-2.4, the resident bundles are views not copies), and rss_max does not discriminate A from B at all (B reports 2.30 GiB while holding 26.24 GiB owned), so that field is not evidence of anything here.

What remains untested is the question this plan answers. A's whole memory story is 'the 26 GiB of file-backed page cache is free to evict, so A needs only ~2 GiB'. The only support for that is the cold-start replicate: 28.0 GB faulted in at a cost of 0.029 ms/token. But that is bulk SEQUENTIAL fault-in during prefill, which overlaps perfectly. Nobody has measured sustained decode while the kernel is actively evicting, where faults are random-access and land on the critical path. This experiment determines A's real memory requirement.

THE THING THAT KILLED YOUR TWO PREVIOUS BALLAST LADDERS, and how this one differs. Both earlier attempts produced NULL because the treatment never landed -- available memory moved only 0.08 to 0.14 GiB. The ballast helper itself is fine: .codex_logs/ballast_anon.py already uses incompressible random bytes, a 16 KiB stride, continuous re-touching, and a TERM trap. Reuse it as-is. The failure was the LANDING METRIC. Those runs asserted on available memory while B held 26 GiB of anonymous memory, so there was nothing reclaimable to give. Here the target is different and much easier: the kernel evicts clean file-backed pages before it compresses anonymous ones, so anonymous ballast should displace A's cache readily.

So the landing assertion is on 'File-backed pages' from vm_stat, NOT on available memory. File-backed must fall by roughly the ballast size versus the 0 GiB point. If it has not fallen, that point is a setup failure -- record it as such and do not report its timing. A flat dependent variable against a flat independent variable is not evidence; this arc has already produced two NULL ladders exactly that way.

Ladder: 0, 16, 24, 32 GiB of ballast. DO NOT EXCEED 32. Beyond that the ballast starts forcing A's own anonymous memory into the compressor, which is a different and dangerous failure mode, not the one under test. Per point: warm the cache with a discarded A run, start the ballast, let it settle, assert the landing, run A, then kill the ballast and confirm it is gone before the next point.

TWO REQUIRED CHANGES to the measurement:

1. 200 tokens, not 16, two replicates per point. 16 tokens is about 0.3 s of decode, far too short for sustained eviction to show. The token SHA will therefore differ from the 16-token 72cfec28 -- establish it at the 0 GiB point and require every later point to match THAT, not the 16-token one.

2. Driver change: the driver reports a single pageins_delta spanning load, prefill and decode. The number that answers this question is the decode-phase rate. Record pageins at decode_start_timestamp and decode_end_timestamp and report decode_pageins and decode_pageins_per_token separately. Also extend decode_memory_samples to include File-backed pages, so eviction can be seen DURING decode rather than inferred from before/after. The ballast re-touches continuously and should hold its ground, but verify -- if the model's working set simply pushes the ballast back out mid-run, the treatment did not hold for the duration and the point is invalid.

Record per point: ms/token from step_latency_ms.mean, last-50, tok/s, token SHA, decode_pageins and per token, File-backed pages before / minimum during decode / after, Anonymous pages, stored compressor before and after, pageouts_delta, mlx_active and mlx_external.

The prediction is genuinely open and you should record it as open rather than guessing. Cold bulk fault-in was free; random mid-decode faults are a different mechanism. Both a small and a large degradation are publishable results. A's within-session variance band is 0.029 ms/token from the residency sweep's three replicates, which is extraordinarily tight, so almost any real degradation will be visible -- but re-establish the band at the 0 GiB point in this session first, because cross-session A baselines differ by about 0.7 ms while within-session spread is 0.03.

Task 2, only if Task 1 shows real degradation: bisect between the last good and first bad ballast point to find the knee. That cache size plus A's ~2 GiB anonymous IS the service's real memory requirement and should be stated plainly as such. If Task 1 is flat through 32 GiB, skip Task 2 and record that A's requirement is its anonymous footprint alone.

SAFETY, this deliberately creates memory pressure on a machine with a Metal OOM history and a compressor that ratchets 1-2 GiB per 35B load without draining. All mandatory: abort a point immediately if pageouts_delta goes nonzero beyond a small tolerance -- swapping ends the ladder, do not push through it. Abort if stored compressor rises sharply during a point. The ballast must be cleaned up even when a point aborts; never leave one running between points. One large model at a time, TERM-only composite watchdog. Stop the ladder early rather than reaching for a bigger number.

Unchanged: A path env is MLX_EXPERT_STREAM=1 MLX_EXPERT_PACKED_DECODE=1 MLX_EXPERT_PACKED_PREFILL=1 MLX_EXPERT_RESIDENT_GB=0 MLX_QWEN35_PAGED_QMM_DECODE=0. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0, never enabled. Leave MLX_MAX_OPS_PER_BUFFER and MLX_MAX_MB_PER_BUFFER at default -- high ops values silently corrupt output on this build including on the A path. Gate before each point: available = free+inactive+purgeable >= ~8 GB and stored compressor not rising over 60s, falling or steady passes.

Write results as a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. If you are running low on budget, WRITE AND COMMIT THE DOC BEFORE running the last points -- last time you completed all 13 runs but never wrote the doc, and I had to reconstruct it from artifacts. Do not push; origin is an unplugged external drive.
- 12:05 [EN] <task-notification>
<task-id>b7yeru581</task-id>
<tool-use-id>toolu_01QbsUZkNjn4jT8JxZcy2Vob</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b7yeru581.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex cache eviction experiment" completed (exit code 0)</summary>
</task-notification>
- 12:10 [EN] One short confirmation run, then a redesign. Read docs/experiments/2026-08-12-a-path-cache-eviction-cost.md first -- I added a 'Re-analysis' section after your run that changes the conclusion.

Your execution was correct and the stop was right. But the interesting result is not the stopped ladder, it is your 0 GiB baseline, and it inverts what this whole arc had been assuming.

Your memory_monitor.jsonl for 0gb/rep1 shows File-backed pages collapsing from 29.72 to 5.34 GiB the moment the model loads, then sitting flat at 5.34 for the entire 200-token generation -- while A ran at full speed, 13.404 ms/token, with only 424 decode page-ins total, 2.13 per token, against a 24.375 GiB mapping. So the model's pages are NOT in the File-backed category while the process holds them, which means File-backed was the wrong landing metric and my plan was wrong to specify it.

Arithmetic on your sample 7 (fb 5.34, anon 6.69, avail 8.03, stored 5.08 so occupied about 2) against 48 GiB total gives free + wired = 33.97, and free <= available = 8.03, so wired >= 25.9 GiB. I measured the idle control myself with nothing loaded: Pages wired down = 3.15 GiB.

So roughly 26 GiB appears to be WIRED while A runs, even though wired_limit_ defaults to 0. ResidencySet is not the wiring mechanism -- newBuffer(ptr, size, resource_options, nullptr) on a shared-storage buffer pins the pages itself, independently of the residency set. If that holds, then A's memory is not evictable at all while it runs, A's real requirement is ~26 GiB rather than the ~2 GiB this arc had been claiming, and there is no eviction cost to measure because eviction cannot happen. Your ballast failure corroborates it: compressor went 5.69 to 19.92 GiB, anonymous 21 to 5.76, file-backed 20.45 to 2.02 -- the kernel destroyed everything else rather than reclaim the model, because it could not.

But that number is INFERRED, not measured. Pages wired down was never captured -- that is the variable my plan should have had and did not.

TASK 1, the only thing I want run right now. One plain A run, 200 tokens, NO ballast, with 'Pages wired down' sampled from vm_stat throughout: before load, after load, during decode at the existing sample interval, and after exit. Report the wired timeline alongside file-backed, anonymous, free, and available. Add wired to the monitor permanently since it is now clearly a first-class variable.

What it settles: if wired sits near 26 GiB during the run and drops back toward 3 GiB after exit, the model is pinned, the eviction question is answered negatively, and A's memory requirement is ~26 GiB. If wired stays small, then my arithmetic is wrong, the ~26 GiB is somewhere else, and you should find which vm_stat category holds it before anything else is designed.

This is one short run with no memory pressure induced, so it needs only the normal gate.

TASK 2, only if Task 1 shows the model is NOT pinned. Redesign the ballast with two fixes:

First, use FILE-CACHE ballast instead of anonymous. A second process that repeatedly reads a large file already on disk -- /Users/youxun/models/qwen3.6-35b-mlx-q8-aligned is 36 GB and needs no writing -- creates page-cache pressure with zero anonymous growth, therefore zero compressor and zero swap risk. That completely sidesteps the failure mode that stopped your run.

Second, fix the guards. My pageout rule was too narrow: 5,874 to 5,917 is 43 pages, 704 KB, which is not swapping, and it is the fourth time in this arc I have specified a guard too tightly. Make the PRIMARY guard stored compressor rise -- abort on roughly +2 GiB -- since that is what actually signalled danger in your run. Keep a pageout guard but make it magnitude-based, something like 64 MB, not nonzero.

Do not run Task 2 without reporting Task 1 first. If Task 1 confirms pinning, Task 2 is moot and the right output is a short conclusion saying the eviction question is closed and A's requirement is ~26 GiB.

Unchanged: A path env is MLX_EXPERT_STREAM=1 MLX_EXPERT_PACKED_DECODE=1 MLX_EXPERT_PACKED_PREFILL=1 MLX_EXPERT_RESIDENT_GB=0 MLX_QWEN35_PAGED_QMM_DECODE=0. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0, never enabled. Leave MLX_MAX_OPS_PER_BUFFER and MLX_MAX_MB_PER_BUFFER at default. 200-token SHA should match ef07c59a3adc5a0a37313eda5d36c3c140dc13b4 from your baseline. One large model at a time, TERM-only watchdog, gate on available >= ~8 GB with stored compressor not rising.

Update docs/experiments/2026-08-12-a-path-cache-eviction-cost.md in place with a Task 1 section, keeping the existing tables and my re-analysis intact, and commit. Write the doc before you run low on budget. Do not push; origin is an unplugged external drive.
- 12:18 [EN] <task-notification>
<task-id>b6twrwyh2</task-id>
<tool-use-id>toolu_01YEp4H971FH7ppLKEgPaWjM</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/b6twrwyh2.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch wired-memory confirmation run" completed (exit code 0)</summary>
</task-notification>
- 12:35 [ZH] 那接下来做什么
- 13:05 [ZH] 快推
- 13:09 [ZH] 推到盘上了把,没有推到远程吧
- 13:10 [ZH] 接下来如何开展,A路线其实毫无意义了
- 13:18 [ZH] 目标仍然是运行大于设备 RAM 的 MoE, 当然可以是iPhone也可以是其他尺寸的mac,反正就是内存不够也能跑,另外,还是要先把流式全量的内存这条线为什么比A差那么多,回答清楚,你说的masked基线是什么意思,按现在代码跑个数据出来?
- 13:21 [EN] Execute docs/plans/2026-08-12-masked-streaming-baseline.md. Read it in full first, then read docs/reference/qwen35-serving-configuration.md and sections 1 and 4 of docs/expert-streaming-decode-optimization.md.

Framing change you need to absorb before starting. The goal is what the original design doc says: run a MoE LARGER than device RAM -- iPhone 8 GB, or any Mac whose RAM is smaller than the model. Against that goal, most of the last four days measured the wrong thing. What we called 'A' is MLX_EXPERT_PACKED_DECODE=1, which binds the whole packed [256, out, in] tensor per projection and therefore wires about 26 GiB, which you measured directly. 'B' was all-resident. So A and B were two ways of being FULLY RESIDENT, not streaming versus resident. A wins only because it skips the copy and uses the stock gather_qmm kernel. The actually memory-bounded path -- the masked streaming loop at mlx_lm/models/switch_layers.py:1676-1712, which indexes per-expert handles and binds only the roughly 8 routed experts per layer -- has never been measured on qwen3.6-35B.

One warning that matters: qwen3.6 already REVERSED a Gemma-era finding. The design doc records packed decode as 4x SLOWER on Gemma-4-26B (4.3 vs 22 tok/s) while on qwen3.6 it is the fastest path we have. So none of the Gemma masked-loop numbers, including its ~22 tok/s ceiling, can be assumed to carry over. Measure, do not extrapolate.

TASK 1 first, because it closes the A/B question that was explicitly asked to be answered before the streaming work.

8.00 ms/token of the original 14.43 ms gap was the diagnostics fence and is settled. The remaining ~5.7 ms is unexplained. Already killed: memory starvation, kernel speed (B's kernel is ~10x faster per call), dispatch count (both issue exactly 120 QMM per decode forward), storage type, and the MB command-buffer cap. The standing hypothesis is that B does not get the dispatch batching A gets, supported by B going 18.3 to 13.7 ms/token at maximal MLX_MAX_OPS_PER_BUFFER with IDENTICAL work -- same 2280 resident dispatches -- but that setting corrupts output on both paths so it cannot be proof.

Nobody has ever counted a command-buffer submission. Your counters from MLX fork commit 3df32c6 are exactly what is needed: gated atomics for commits, end_encoding calls, dispatches, and which arm of the needs_commit() OR fired, behind MLX_METAL_SUBMISSION_COUNTERS which is unset by default. Rebuild the fork, then run A and B at DEFAULT command-buffer caps, 16 tokens, counters on, and report commits per decode forward for each. 2280 resident dispatches equals 19 forwards times 120, so per-forward figures are derivable.

Prediction: B commits far more often per forward than A. Falsifier: if A and B commit at similar rates, the batching hypothesis is dead and the ~5.7 ms comes from something else -- record that plainly. 'Unexplained, and here is the list of what was eliminated' is an acceptable and honest close to this question; do not manufacture an explanation.

The counters must not fence. No mx.eval, no host readback, no per-call synchronization -- per-call instrumentation cost A 61% of its throughput earlier in this arc. Atomics only, dumped at exit. Build with CMAKE_ARGS using -DUSE_SYSTEM_FMT=ON and -DFETCHCONTENT_SOURCE_DIR_METAL_CPP pointed at a local checkout; fmt lacks GIT_SHALLOW TRUE and stalls, metal-cpp fails CMake's own download here. Restore the release build afterwards and verify hasattr(mx, 'get_external_memory'). Do NOT enable MLX_METAL_DEBUG -- it produced empty tables before and is precisely what these counters replace.

TASK 2, and CHECK THIS BEFORE RUNNING ANY MASKED MEASUREMENT. The masked loop requires the per-expert list representation: it indexes self.gate_proj['weight'][expert_id] and its quantized branch requires isinstance(weight, list) for weight, scales and biases across all three projections. If qwen3.6-35b-mlx-q6-aligned loads only as a single packed tensor, the masked path is UNREACHABLE and every run you label 'masked' would silently fall through to something else. That is the same failure mode that invalidated your earlier ladder -- a configuration that did not land, reported as a measurement. Report which representation the model actually loads, per projection. If the list form is absent, STOP and report what would be needed to produce it -- loader flag, repack, or both -- rather than running.

TASK 3, only if Task 2 confirms the path is reachable. Two configurations, since prefill and decode have separate switches with opposite tradeoffs: M1 is PACKED_PREFILL=1 with PACKED_DECODE=0, M2 is both 0. 200 tokens, agent prompt, two replicates each.

THE HEADLINE METRIC IS 'Pages wired down' sampled across load, prefill, decode, and after-exit -- not throughput. This is a memory experiment first. Also record file-backed, anonymous, mlx_active, mlx_external, ms/token from step_latency_ms.mean, decode page-ins per token, token SHA, and specifically whether M1's prefill wiring is RELEASED before decode starts.

What M1 versus M2 answers: can packed prefill keep its 74s-to-3s win without permanently wiring the model? If M1's wiring drops back after prefill, M1 is the shipping configuration for the >RAM target. If it does not, M2 is the only honest low-memory path and its prefill cost becomes the real problem.

How to judge the result, fixed before running: a masked configuration that wires MATERIALLY LESS than packed's 26.5 GiB is the finding, even if it is much slower. The goal is running a model that does not fit, so slow-but-bounded is a success and fast-but-26-GiB is not. Report memory first, throughput second. And if masked wires ~26 GiB anyway, say so plainly -- that would mean this fork has no low-memory path at all on this model, which closes the >RAM direction for qwen3.6 and is the single most important thing to know.

Standard rules: interpreter /Users/youxun/project/mlx/.venv_build/bin/python, model /Users/youxun/models/qwen3.6-35b-mlx-q6-aligned, prompt .codex_logs/qwen36_q6_baseline_prompts/agent.txt. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0 never enabled. Leave the command-buffer caps at default except where Task 1 needs them. One large model at a time, TERM-only composite watchdog. Memory gate: available = free+inactive+purgeable >= ~8 GB and stored compressor NOT RISING over 60s -- falling or steady passes. A's within-session variance is ~0.03 ms/token against ~0.7 across sessions, so re-baseline in-session before comparing. Assert the configuration landed before reporting any timing.

Write results as a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. WRITE AND COMMIT THE DOC BEFORE you run low on budget -- twice now you have completed runs and left me to reconstruct the writeup from artifacts. Do not push; origin is an unplugged external drive.
- 13:23 [ZH] 应该把全部专家都定死在内存的这个做了把,
- 13:25 [ZH] 每层只绑约 8 个被路由到的专家,每个约 0.86 MB,一层约 21 MB,40 层约 830 MB —— 而 packed 路径一次绑整块 220 MB × 120 = 全部 24 GiB 按这个不是只有8个吗
- 13:28 [ZH] packed(现状) │ 绑整块 mmap 张量 │ wired 文件页,退出即还 │ 12.5–13.5 不是70多80吗
- 13:32 [ZH] 当前的目标是什么,还是把全量内存情况下要接近70tok/s吧
- 13:38 [ZH] 先全量定在内存和带宽有什么关系
- 13:39 [ZH] 我说的是当前目标,先把差异对齐了
- 13:42 [ZH] B就是masked?
- 13:54 [EN] what's the status
- 14:01 [ZH] 你把目标搞错了把,你测的是长期目标,当前目标先把全量内存的B路线这个结果拿到
- 14:02 [EN] <task-notification>
<task-id>byttr1i4p</task-id>
<tool-use-id>toolu_018nPgaCqTgyWAxzRVmSsuvQ</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/byttr1i4p.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch A/B close-out and masked baseline" completed (exit code 0)</summary>
</task-notification>
- 14:05 [EN] New task, narrow scope. Execute docs/plans/2026-08-12-b-route-variant-c.md. Read it first.

Scope discipline: this is the B route ONLY. Masked streaming, the bandwidth wall, and routing traces are long-term work and are explicitly out of scope. Do not touch them. Your masked results from the last run are committed and fine; leave them alone.

Your Task 1 counter work was exactly right, including honoring the falsifier and refusing to manufacture an explanation. It also sharpened the problem to one number. Per decode token, A and B issue an IDENTICAL 1,869 dispatches, commits differ by only 1.11x (171.1 vs 189.1), and MB-limit commits match to within one (1830 vs 1831). Of B's 1,869 dispatches, exactly 120 are ResidentGatherQMM. So the entire 4.9 ms/token gap concentrates as about 41 microseconds of extra cost per ResidentGatherQMM call. That is far too large to be the unconditional x_layout string at quantized.cpp:1885, which is worth 1-2 us -- it points at the kernel as executed in context. Note the 'B's kernel is faster' result (0.3912 vs 0.5042 ms) came from an ISOLATED measurement at a different shape and does not contradict this.

The experiment is the missing third cell. A is stock gather_qmm on the mmap'd whole tensor at 13.4 ms/token. B is the custom qwen35_resident_gather_qmm on MLX-owned compact slabs at 18.3. C is stock gather_qmm on those same compact slabs, and it has never been run. C near A means the custom kernel is the cost and the 41 us has a definite home. C near B means the slab or its binding is the cost, no kernel swap can save it, and the B route closes. Both outcomes end the question, so report whichever you get without hedging.

Implementation needs no MLX rebuild and no kernel work. B runs at 100% residency, so expert_to_slot is a permutation and the stock kernel is mathematically equivalent on the same slabs: mx.gather_qmm(inputs, *resident, rhs_indices=expert_to_slot[indices], transpose=True, group_size=proj.group_size, bits=proj.bits, mode=proj.mode). Add it as a third branch in the paged_qmm closure at mlx_lm/models/switch_layers.py:1343-1355, behind a new default-off env flag, suggested MLX_QWEN35_PAGED_QMM_STOCK_KERNEL=1, that only applies when resident_only is true. A and B must behave exactly as before when the flag is unset. Guard it: assert resident_only and full residency before taking the branch, because an incomplete placement leaves -1 entries in expert_to_slot and the indexing would silently produce garbage -- fail loudly instead.

PROTOCOL, and this part matters as much as the result. A has drifted 11.98 to 12.5 to 13.4 ms/token across sessions while the gap being explained is only 4.9 ms, and A, B and C have never been run back to back. Run all three IN ONE SESSION, ALTERNATING, two replicates each in the order A B C A B C. 16 tokens, agent prompt, default command-buffer caps, MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=0 throughout.

Every point must produce token SHA 72cfec2812ddc4f65f83dc847a047b8e0b6db4da. If C produces a different SHA that means the index remap is wrong -- that is a bug to fix before measuring, not a result to report. Every B and C point must pass the residency landing assertion before any timing is accepted: entries == 40, resident_bytes near the 25 GiB budget, resident_only_dispatches > 0. A point that did not land is a setup failure, not a measurement. Report step_latency_ms.mean rather than generation_tps, since 16-token tps is 6.7% inflated by having only 15 inter-token intervals. Anything under 2.0 ms/token is inside B's known run-to-run variance and is not a difference.

Only if C comes out near A: as a separate cheap follow-up, remove the unconditional x_layout construction at quantized.cpp:1885 -- the callee already early-returns when recording is disabled at mlx/ops.cpp:58-61 -- and re-measure. It will not be the 41 us but it is free to fix and it quantifies the host-side share. Report it separately, do not fold it into the C number.

Standard rules: interpreter /Users/youxun/project/mlx/.venv_build/bin/python, model /Users/youxun/models/qwen3.6-35b-mlx-q6-aligned, prompt .codex_logs/qwen36_q6_baseline_prompts/agent.txt. Generate and pass MLX_QWEN35_PAGED_QMM_PLACEMENT for every B and C point. Do not set MLX_MAX_OPS_PER_BUFFER or MLX_MAX_MB_PER_BUFFER. One large model at a time, TERM-only composite watchdog. Memory gate: available = free+inactive+purgeable >= ~8 GB and stored compressor NOT RISING over 60s, falling or steady passes.

Write results as a new experiment doc under docs/experiments/ dated 2026-08-12, add it to docs/experiments/README.md, and commit. Write and commit the doc before you run low on budget. Do not push; origin is an unplugged external drive.
