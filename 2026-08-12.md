# English Learning Log — 2026-08-12

- 00:01 [ZH]  为什么B还快呢,这本身就说明问题把
- 00:05 [EN] STOP the attribution line of work. A likely measurement artifact has been found that may invalidate the entire A/B gap, including the negative result committed in 0647c77. Test it first.

THE FINDING. mlx_lm/models/switch_layers.py:1330-1341, inside the paged decode path:

    if os.environ.get("MLX_QWEN35_PAGED_QMM_DIAGNOSTICS", "").lower() in {...}:
        # Benchmark-only: inspect route ids after native dispatch inputs are
        # fixed. The normal paged path never reads routes back on the host.
        mx.eval(indices, expert_to_slot)
        route_ids = np.asarray(indices).reshape(-1)
        slots = np.asarray(expert_to_slot)

That is an mx.eval plus two host readbacks per layer -- 40 GPU-to-CPU fences per forward. The comment says outright that the normal paged path never does this. MLX_QWEN35_PAGED_QMM_DIAGNOSTICS=1 is in the canonical B environment from run_qwen36_paged_b_allhit.sh, so it was set in EVERY B measurement in this investigation: Gate 1's 49 tok/s, the budget sweep, the 200-token confirmation, the ballast ladders, and the attribution run. A never takes this path and never paid it.

WHY THIS EXPLAINS EVERYTHING WE MEASURED. From your own attribution rows, recomputed by layer and percentile:
  - B steady-state per call, forced: median 0.210 ms. A: median 2.169 ms. B's kernel is ~10x FASTER, matching the isolated 0.3912 vs 0.5042 ms result.
  - B's mean is dragged to 2.434 ms by a tail of ~113 calls at 47-57 ms -- about one per (layer, projection), i.e. first-touch warmup. Top 5% of B's calls are 83.4% of its total time. Your headline 'A 7.3421 s vs B 5.5502 s forced sum' therefore compared A's steady state against B's one-time warmup.
  - A runs ~12 ms/token against 120 x 2.17 ms serialized, so A overlaps roughly 18x. B runs ~25 ms/token against 120 x 0.21 ms serialized, so B overlaps 1x -- it is already fully serialized. A per-layer host fence is exactly what would cause that.
  - It also explains why adding your instrumentation cost A 61% (83 -> 32.63 tok/s) and cost B nothing (37-40 -> 41.18): A had overlap to lose, B had none left.

Also note your reported numbers were wrong on units: 'B unforced decode 6.2540 s' is a sum of per-call intervals, while 'A unforced decode 0.4903 s' is wall time. B's actual unforced decode wall was 0.388 s -- B was FASTER than A in that run. Correct that wherever you wrote it.

THE EXPERIMENT. Rerun B with MLX_QWEN35_PAGED_QMM_DIAGNOSTICS UNSET, everything else identical, and compare against A. Residency does not depend on the run: the placement file is complete 40x256, so _paged_qmm_complete is True and resident_only is selected deterministically. Verify residency ONCE with diagnostics on, then measure throughput with it off -- and say plainly in the report that hit-rate/backing-route telemetry is unavailable in the measured runs because it is produced only by the block being removed. That coupling is itself a defect worth recording: the only way to verify full residency is to enable the fence that destroys the thing being measured.

Run: A and B back to back in one session, same memory conditions, 200 tokens, agent prompt. Report tok/s for A, B-with-diagnostics, and B-without-diagnostics, plus token SHAs, which must match.

Three outcomes, all publishable:
  - B-without jumps to or past A -> the entire 83 vs 37 gap was a diagnostics artifact. Then 0647c77's negative result is WRONG and must be corrected, and the mixed-cache conclusion about backing routes needs re-testing too, since those sweeps carried the same fence.
  - B-without improves but stays below A -> the fence was part of it; report the split.
  - B-without is unchanged -> the fence is not the cause and the negative result stands. Say so plainly.

Do NOT skip straight to rewriting conclusions. Measure first. Interpreter is the release build; verify hasattr(mx,'get_external_memory')==True. Memory gate available = free+inactive+purgeable >= ~8 GB, stored compressor flat over 60s, never bare 'Pages free'. One model at a time, TERM never -9, do not push. Worktree is clean at 700e637.
- 00:12 [EN] <task-notification>
<task-id>bjirakvg1</task-id>
<tool-use-id>toolu_01Lh1y4TNXWt5cajsSpLQSvi</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/1982396c-65de-4315-9272-9e27be028c06/tasks/bjirakvg1.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch codex to test the diagnostics-fence artifact" completed (exit code 0)</summary>
</task-notification>
