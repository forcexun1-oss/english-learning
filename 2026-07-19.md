# English Learning Log — 2026-07-19

- 22:15 [ZH] 这个项目改造了一部分:moe架构的大模型运行在内存小于模型的情况下如何运行, 参考一下mlx-lm后面更进一步的改造, 继续改造这个项目, 我要让moe架构的qwen3.6运行在RTX4040ti的显卡上,显存只有8G如何运行大于8G的模型, 写一个方案, 尽量详尽,后面让别人去执行
- 22:15 [ZH] 这个项目改造了一部分:moe架构的大模型运行在内存小于模型的情况下如何运行, 参考一下mlx-lm后面更进一步的改造, 继续改造这个项目, 我要让moe架构的qwen3.6运行在RTX4040ti的显卡上,显存只有8G如何运行大于8G的模型, 写一个方案, 尽量详尽,后面让别人去执行,方案用英文
- 22:24 [EN] let subagent implement it using the sonnet model
- 22:36 [EN] <task-notification>
<task-id>acd1e93f68e5fbdca</task-id>
<tool-use-id>toolu_01Sg3AtcvmwP8ku2gL73xT26</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-llama-cpp/7dc0bdb2-3a74-4899-a5d1-2aa7c4ec77be/tasks/acd1e93f68e5fbdca.output</output-file>
<status>completed</status>
<summary>Agent "Implement CU1 Phase 1 refactor" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All changes are in the working tree as required (nothing committed/staged/stashed). Final summary follows.

## Changes Made

- `common/expert-cache-backend.h` (new): defines `struct common_expert_cache_gpu_backend_ops` — the Phase 1 backend-ops interface. Fields: `buffer_create`, `buffer_create_nocopy`, `buffer_free`, `buffer_contents`, `buffer_gpu_address`, `buffer_backend_buffer`, `buffer_request_residency` (slab lifecycle/addressing), `set_slot_lookup`/`set_addr_lookup`/`set_bytes_lookup`/`set_handles_lookup`/`set_alias_fn` (kernel-side lookup callback registration), `set_bindless_stage_fn`/`set_bindless_timeline_stage_fn`/`set_bindless_timeline_sync_fn` (per-step staging + timeline hooks), `bindless_table_write` (device pointer-table write). Header comment documents the mapping to the proposal's six sketched responsibilities (slab_init/slab_free/slot_device_addr/slot_upload_async/compute_wait/table_write) and explains why `compute_wait` has no member (never called by expert-cache.cpp today — it's internal to ggml-metal-ops.cpp's op dispatch).
- `common/expert-cache-backend.cpp` (new): implements `common_expert_cache_gpu_backend_ops_get()`, returning a static table that forwards every field 1:1 to the existing `ggml_moe_gpu_slab_*`/`ggml_moe_gpu_bindless_*` functions declared in `ggml/include/ggml-cpu.h`. No logic changes — pure routing.
- `common/expert-cache.cpp`: added `#include "expert-cache-backend.h"` and a file-scope `g_gpu_ops` pointer; replaced all ~35 direct call sites of `ggml_moe_gpu_slab_buffer_*`, `ggml_moe_gpu_slab_set_*`, `ggml_moe_gpu_bindless_set_*`, and `ggml_moe_gpu_bindless_lookup` with `g_gpu_ops-&gt;...` equivalents. Left `ggml_moe_gpu_bindless_is_enabled()`, `ggml_moe_gpu_hybrid_*`, and `ggml_backend_dev_by_type`/`ggml_backend_buft_get_max_size` untouched — these are generic ggml-backend/state-flag APIs, not part of the GPU-slab-backend surface.
- `common/CMakeLists.txt`: added the two new source files to the `llama-common` target.
- `tools/cli/cli.cpp`: added a guard right after `expert_cache.init(...)` — if `--expert-cache-gpu-slab`/`--expert-cache-gpu-bindless` was requested but the cache failed to come up (`!expert_cache.enabled()`), print a clear error and `return 1` instead of proceeding. This closes the actual crash gap I found: `ggml_moe_gpu_bindless_set_enabled(true)` was already called unconditionally earlier in `llama_cli()`, so on a hypothetical future CUDA build with no slab backend registered, inference would have proceeded and hit `GGML_ABORT("bindless MUL_MAT_ID missing slab addresses")` deep inside `ggml-metal-ops.cpp`'s (or the future CUDA equivalent's) op dispatch — exactly the crash the task asked to prevent.

## Interface settled on

`common_expert_cache_gpu_backend_ops` in `common/expert-cache-backend.h` (16 function-pointer fields, listed above) plus `const common_expert_cache_gpu_backend_ops * common_expert_cache_gpu_backend_ops_get(void)`. I deviated from the proposal's literal 6-function sketch because the real Metal call surface used by `expert-cache.cpp` doesn't map 1:1 onto it: there's no existing "async upload" or "compute wait" call in this file (Metal's unified memory makes upload a synchronous host memcpy into `buffer_contents()`, and the wait happens inside the Metal op-dispatch file, not here), while the actual surface includes buffer lifecycle *and* five kernel-lookup-callback registrations that the proposal's sketch omitted. The header documents the mapping explicitly.

## Build result

`cmake --build build -j 8 --target llama-cli` — succeeds, zero warnings or errors (verified with a clean rebuild of just the touched object files).

## Test results

`test-llama-archs --check-expert-names` — built and run, exit code 0, no failures (unaffected by this refactor, confirms build-tree consistency).

## Oracle gate result

Ran the locked oracle command from `docs/ds4/models/qwen3.6-35b-a3b/RECIPE.md` via `run_llama_guarded.sh`, `--expert-cache-gpu-bindless --expert-cache-budget 4`, `LLAMA_EXPERT_STREAM_NOMMAP=1`, `--no-repack --no-mmap -ngl 99`, oracle prompt, `--n-predict 32 --temp 0 --seed 1 --single-turn --simple-io`. Guarded run completed exit 0, no compressor kill. Generated text matched RECIPE.md's expected block exactly, and its SHA-256 (trailing newline included) is:

```
21eeb2f783e71f4872d220527f5db05897f1908eb5d47434ea3bb2024c6b741d
```

This matches the locked value in RECIPE.md exactly. **SHA match: yes.** Post-refactor flag-on Metal behavior is byte-identical, confirming the routing through the new ops table introduced no functional change.

## Deviations / notes

- No functional/behavioral deviation. The one genuinely new *behavior* (not present before) is the CLI-level clean-failure guard in `tools/cli/cli.cpp` — this doesn't change any byte-identical flag-on/flag-off path (Metal always registers, so `expert_cache.enabled()` is unaffected here), it only adds a new early-exit for a case that previously would have proceeded into a crash. I judged this in-scope because it's the literal wording of the task's hard requirement ("must fail cleanly, not crash") and the actual bug lived at the call site of `expert_cache.init()`, not inside `expert-cache.cpp`/backend files themselves.
- I did not touch `ggml/src/ggml.c` or `ggml/include/ggml-cpu.h` — the existing `ggml_moe_gpu_slab_*` dispatch there is already null-safe/backend-agnostic (confirmed by reading it), so no changes were needed for the "fail cleanly" requirement beyond the CLI guard.
- No blockers hit.

Relevant paths: `/Users/youxun/project/llama.cpp/common/expert-cache-backend.h`, `/Users/youxun/project/llama.cpp/common/expert-cache-backend.cpp`, `/Users/youxun/project/llama.cpp/common/expert-cache.cpp`, `/Users/youxun/project/llama.cpp/common/CMakeLists.txt`, `/Users/youxun/project/llama.cpp/tools/cli/cli.cpp`, `/Users/youxun/project/llama.cpp/docs/ds4/models/qwen3.6-35b-a3b/RECIPE.md`.</result>
<usage><subagent_tokens>192451</subagent_tokens><tool_uses>77</tool_uses><duration_ms>683023</duration_ms></usage>
</task-notification>
- 23:07 [EN] <task-notification>
<task-id>a30298da1fb842810</task-id>
<tool-use-id>toolu_01CnSvXAFhXThzpw5UoBSk6X</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-llama-cpp/7dc0bdb2-3a74-4899-a5d1-2aa7c4ec77be/tasks/a30298da1fb842810.output</output-file>
<status>completed</status>
<summary>Agent "Implement CU1 Phase 2 CUDA scaffold" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All Metal/shared-file diffs were pre-existing (from earlier Phase 0/1 work, before this session started) — I only ever `Read` those files, never `Edit`/`Write`. My session's diff is scoped entirely to `ggml/src/ggml-cuda/*` (purely additive, 442 insertions, 0 deletions) plus the new results doc. The macOS build (`GGML_CUDA=OFF`) passed cleanly, and by construction it cannot be affected since `ggml-cuda/CMakeLists.txt` only compiles that directory when `-DGGML_CUDA=ON`.

## Summary

**Files added:**
- `ggml/src/ggml-cuda/expert-slab.cu` / `.cuh` — CUDA implementation of the `ggml_moe_gpu_slab_*`/`ggml_moe_gpu_bindless_*` registration surface (buffer lifecycle with a `cudaMalloc` device buffer + `cudaMallocHost` pinned mirror per slab, bindless pointer-table registry, dedicated non-blocking copy stream, slot-data staging helper, one-time registration).
- `docs/ds4/CU1_RESULTS.md` — Phase 1 recorded done (oracle SHA `21eeb2f7...` matched), Phase 2 recorded as code-written/UNBUILT, full rationale, genuinely-unverified list, and a step-by-step bring-up checklist for the RTX 4060 Ti owner.

**Files modified (all purely additive, flag-off path untouched):**
- `ggml-cuda.cu` — registers the CUDA MoE helpers once inside `ggml_backend_cuda_reg()`; adds new `ggml_cuda_mul_mat_id_bindless()`; adds one `if (ggml_moe_gpu_bindless_is_enabled())` branch at the very top of `ggml_cuda_mul_mat_id()`.
- `mmvq.cu`/`.cuh` — new `mul_mat_vec_q_moe_bindless` kernel (a twin of the existing `mul_mat_vec_q_moe`, reading each block's base pointer from a device table instead of `src0+expert*nb02`) + host entry `ggml_cuda_mul_mat_vec_q_id_bindless`.
- `mmq.cu`/`.cuh` — new `ggml_cuda_mul_mat_q_id_bindless`, using a gather-then-call-existing-kernel strategy instead of hand-editing `mmq.cuh`'s ~4000-line MMA pipeline.

**Registration**: one call, `ggml_cuda_register_moe_helpers()`, mirrors Metal's `ggml_backend_metal_register_moe_helpers()` exactly (same four `ggml_moe_gpu_*_set_*` calls); `common/expert-cache-backend.cpp` needed zero changes.

**Kernel-variant mechanism**: literal indirect-pointer-table read for `mmvq.cu` (low risk, small kernel); gather-to-scratch-then-call-unmodified-`ggml_cuda_mul_mat_q` for `mmq.cu` (deliberate divergence from the proposal's literal design — documented in both the code comments and `CU1_RESULTS.md` — because hand-editing the deeply templated MMA tile-loading kernels with no compiler available was judged a worse risk than an extra D2D copy).

**Design surprise vs. the proposal**: traced how expert bytes actually reach the compute node and found `common_expert_cache_cb_eval` (the `ggml_backend_sched` eval callback) already does synchronous, backend-agnostic staging before every `MUL_MAT_ID` node — so CUDA didn't need to port Metal's async `MTLSharedEvent` staging pipeline at all; it only needed a new H2D copy step (host pinned mirror → device slab) that has no Phase-1 interface equivalent, added directly in the dispatch path with `nb02` supplied by the caller.

**Loader/placement** (`llama-model-loader.cpp`, `llama-model.cpp`): traced and found already fully backend-agnostic (keyed on `ggml_moe_gpu_bindless_is_enabled()` and generic `GGML_BACKEND_DEVICE_TYPE_GPU`/`CPU`, no Metal-specific code) — zero changes made.

**Genuinely unverified** (full list with reasoning in `CU1_RESULTS.md`): CUDA graph capture semantics, the `cudaHostRegister`/`cudaHostGetDevicePointer` no-copy path (likely needs `cudaSetDeviceFlags(cudaDeviceMapHost)` earlier in init than this code runs), single-GPU device-resolution assumption, `uint64_t*`→`void**` pointer reinterpretation, mmq gather overhead at real batch sizes, the bindless-alias registry's exact trigger conditions, and `mul_mat_vec_q_moe_bindless`'s perf vs. the small-k-optimized flag-off kernel for `ncols_dst==1`.</result>
<usage><subagent_tokens>368014</subagent_tokens><tool_uses>106</tool_uses><duration_ms>1758228</duration_ms></usage>
</task-notification>
