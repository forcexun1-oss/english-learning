# English Learning Log — 2026-09-17

- 08:57 [EN] continue

<<<<<<< Updated upstream
<<<<<<< Updated upstream
- 09:03 [EN] what's the status?
=======
=======
>>>>>>> Stashed changes
- 09:02 [ZH] 高优先级问题
- [provenance 还没有真正贯通抽取链路 (line 145)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:145)
  §5 只规定了给 MemoryStore.add() 增加可选 provenance 参数，但实际抽取是通过 session_digest 启动独立的 pi 子进程完成的。文档没有说明 session id、turn id、source timestamp、batch id 如何从 harness 传入子进程，再由 Hermes memory 工具传给 MemoryStore.add()。
  目前已有的 DigestProvenance 只记录“这个 digest session 新增了哪些 memory”，并不能让每条 memory 知道具体 turn；交互式 memory 工具也没有来源参数。若不补充这条端到端数据流，阶段 0 的“新写入记忆带完整 provenance”和阶段 2 的覆盖率都无法验收。应明确至少一条完整链路：
  harness metadata
    -> child pi environment / prompt / session metadata
    -> hermes memory tool call
    -> MemoryStore.add(provenance)
    -> SQLite + search/read trace
- [有效性 smoke 会把额外记忆留在被测记忆库中，污染正式查询 (line 218)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:218)
  §7.2 的顺序是 ingest -> smoke -> snapshot -> query，而 §9.3 的 smoke 明确要求“写入一条已知内容的记忆”。文档没有要求在 query 前删除该记忆，也没有要求在独立 store 中执行。这样 smoke 记忆可能被索引注入或 memory_search 返回，直接改变答案和 recall。
  应改成以下任一方式：
  - smoke 使用独立的临时 PI_CODING_AGENT_DIR 或独立 namespace；
  - smoke 结束后删除该 memory，并重新生成索引、重新快照，再开始 query；
  - 将 smoke 放到正式 ingest 前，并验证后销毁整个 smoke store。
  这不是一般的测试洁净性问题，而是会改变最终 benchmark 分数。
- [“覆盖率上界”的统计对象仍然没有闭合 (line 295)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:295)
  source_session_ids ∩ answer_session_ids 非空即算 session 覆盖，source_turn_ids ∩ has_answer:true 非空即算 turn 覆盖，但没有明确这些集合来自：
  - ingest 后的全部 memory；
  - query 时实际返回的 search 结果；
  - 被 read 的 memory；
  - 还是最终回答上下文中的 memory。
  如果使用全部 memory，几乎所有 evidence 都可能因为“曾经被抽取到某条记忆”而算覆盖，无法衡量检索；如果使用 search 结果，又必须定义 top-k、分页、重复结果和索引 headline 的处理。建议拆成两个独立指标：
  - extraction provenance coverage：所有生成 memory 对 evidence 的覆盖；
  - retrieval provenance coverage：query 实际返回的候选 memory 对 evidence 的覆盖。
  两者都应明确分母、候选集合和 cutoff，不能继续共用“覆盖率”这个名称。
- [三态命中仍无法从现有 search 输出可靠映射到 memory id (line 124)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:124)
  §4.4 已确认 memory_search 输出不含 id；它只输出 target、scope、category、content、Created、Last used。可是 §7.3 又要求判定某个 evidence 对应的 memory 是否出现在 search 返回中，以及是否随后被 memory_read。
  新增 provenance 字段存进 SQLite 并不能自动解决这个问题，因为 agent 看到的 search 返回仍然没有稳定 id。需要明确并实现一个可关联字段，例如：
  - search 返回稳定 memory id；
  - search 返回不可见于模型、但由 runner 记录的 result id；
  - 或由 runner 根据完整返回内容与索引建立确定性关联。
  否则 index_only、searched_not_read、read_into_context 只能对“文本片段”做模糊匹配，无法保证互斥或可复现。
中高优先级问题
- [source_timestamp 的类型与时间校验要求互相矛盾 (line 155)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:155)
  §8 要把 haystack_dates 统一解析成 UTC ISO-8601，§5.1 却把 source_timestamp 定义为 DATE。DATE 会丢失时分秒和原始 timezone；同一天内的多个 session 也无法排序。LongMemEval 的 temporal reasoning 可能依赖具体日期顺序，不能只保留日历日期。
  应把字段定义为带时区的完整时间（例如规范化后的 ISO-8601 TEXT），同时另设派生日期字段，或者明确证明题目只依赖日期粒度。验收也不应写成“文件名和 DATE 相等”，而应验证原始时间戳到规范化时间戳的可逆映射。
- [阶段 0 的范围与仓库边界没有解决 (line 145)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:145)
  文档说 eval 代码独立在 agent-eval，但阶段 0 要修改 pi-hermes-memory 的 schema、写入 API、合并逻辑，并迁移生产中的 85 条记忆；§15 还把“上游合并冲突已解”列为验收项。这样一期实际上不再只是 benchmark 接入，而是一个跨仓库生产特性和数据迁移项目。
  需要明确：
  - 哪些改动落在 agent-eval；
  - 哪些改动落在 pi-hermes-memory；
  - 生产库迁移是否真的属于一期；
  - provenance 改动没有合入上游时，benchmark 如何固定依赖；
  - 失败时如何回滚 schema 和已有数据。
  否则“阶段 0 完成”没有清晰的代码归属和交付边界。
- [provenance 的并集规则会造成“来源存在但事实不再存在”的假覆盖 (line 170)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:170)
  文档规定 memory 合并、替换时 provenance 永远取并集，并明确 coverage 只是上界。这是诚实的降级，但如果旧 memory 的正文已经被 memory_replace 改写，继续保留所有旧 turn 会让 provenance 集合越来越大，最终 coverage 只表示“这条 memory 的历史曾经涉及过该 turn”，不表示当前内容仍能回答该 turn。
  建议至少同时保存：
  - historical_source_turn_ids；
  - active_source_turn_ids 或当前正文对应的来源；
  - replace/consolidation 的版本链。
  否则第二期即使使用这些字段，也会系统性高估抽取覆盖。
- [完整上下文 trace 的边界和隐私/体积没有定义 (line 230)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:230)
  “最终送入回答模型的完整上下文”需要明确是 API request 的原始 messages，还是把 provider 内部拼接后的 prompt 也记录下来。两者可能不同，尤其涉及 system prompt、tool schema、搜索结果和隐藏元数据。完整保存 500 题 × 多次运行的上下文也可能远大于 JSONL 结果本身。
  建议记录：
  - 原始 request payload 的 hash；
  - 可审计的 messages 快照；
  - 被截断、压缩或重排的字段；
  - 大 payload 的独立 artifact 路径。
  “body 出现在最终上下文中”的判定必须针对这个明确的 canonical payload，而不是对任意日志文本做字符串搜索。
中优先级问题
- [重复运行与 query 副作用的快照边界不一致 (line 181)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:181)
  §6 说“query 前对记忆库做快照，重复运行从快照恢复”，但 §7.2 把 snapshot 放在 smoke 之后，且没有明确 snapshot 是否包含索引文件、SQLite WAL、lock 文件和生成的 session。应定义一个不可变 case baseline，并规定每次重复运行恢复哪些文件、如何 checkpoint WAL，否则三次运行可能不是同一状态。
- [稳定性验收没有定义具体波动阈值 (line 350)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:350)
  文档要求“声明允许的分数波动区间”，但没有给出区间如何确定，也没有规定按总分、类别分还是每题结果判断。若阈值在看到结果后再设，会把不稳定隐藏掉。应在运行前固定规则，例如总分绝对差、每类分数差和 judge 重复判定差异的上限。
- [阶段 2 的 judge 校准题与正式题的 judge 配置关联不够强 (line 324)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:324)
  题目固定和分层抽样已经比上一版好，但没有规定人工标注者数量、标注独立性、分歧解决方式，以及人工标签如何产生。单一人工标签与 judge 的 kappa 仍可能把人工错误当作标准。至少应要求双人独立标注，第三人仲裁，并将人工 rubric、原始标签和仲裁结果一起存档。
这版可以继续推进，但我会把“是否可以开始实现”分成两个条件：先补齐 provenance 的真实调用链和 smoke 隔离；再把 extraction coverage、retrieval coverage、三态命中的统计对象写成明确 schema。否则即使最终跑出了 QA 分数，覆盖率和 trace 仍然可能无法解释。
<<<<<<< Updated upstream
>>>>>>> Stashed changes
=======
>>>>>>> Stashed changes
- 09:06 [ZH] **还没闭合的一处**
&#x20;   你说「写成明确 schema」，我写的是精确的表格与集合定义，
**不是字面的类型定义**
。
EvalCase&#x20;
/&#x20;
TraceRecord&#x20;
/&#x20;
CoverageResult&#x20;
的 TS 类型我没写进 spec——如果你要的是可直接拿去实现的类型声明，我补；如果那属于实现计划该产出的东西，就留到   writing-plans。这个我不替你定。    另外六类别的每类题数仍然空着，理由同上一轮：
**必须从数据集实测，写进 spec 就是编**
。    Spec 三个 commit 都在&#x20;
agent-eval
，仍是单盘（备份卷没挂）。代码仍未动。 他的回复,然后,你继续review

- 09:10 [ZH] 这两个解释我接受：
- spec 不必提前写 EvalCase / TraceRecord / CoverageResult 的 TS 类型；当前表格和集合定义只要足够让实现者无歧义即可，具体类型留给 writing-plans/实现。
- 六类别题数应由 adapter 对实际数据集统计后写入结果 manifest，不能预先写死在 spec。这里应该补的是“运行时必须记录并校验每类题数”，不是一个猜测数字。
在接受这两个前提后，仍有以下问题。
高优先级问题
- [缺少无记忆 baseline，无法回答“记忆系统带来了什么” (line 9)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:9)
  当前第一期产出 QA 分数、Oracle/S 差异和三态命中，但没有 empty-memory/control run。这样即使拿到一个分数，也不知道：
  - 记忆系统是否比完全不检索更好；
  - Oracle/S 差异是否只是题目难度差异；
  - search/read 工具是否实际带来收益。
    至少应增加一个固定的 NoMemory 配置：同一 instance、同一回答模型、同一 prompt，但不 ingest 记忆且不提供 memory 工具。报告至少列出 NoMemory / Oracle / S 三列。若第一期不想跑完整 no-memory，可以先在 pilot 和每类抽样题上跑，但不能完全没有这个控制。
- [provenance 仍然没有闭合到 child pi 的实际写入调用 (line 145)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:145)
  修订版增加了 schema 和 API，但数据流仍停在 MemoryStore.add(provenance)。实际链路是 harness 启动 pi 子进程，子进程加载 Hermes extension，再由模型调用 memory 工具。文档没有规定 provenance 如何进入这个 tool call。特别是：
  - source_turn_ids 从哪里传给子进程；
  - 一个 digest session 内多个 memory 如何分别绑定来源；
  - source_timestamp 是由 prompt、session metadata 还是环境变量传入；
  - 模型调用 memory_replace 时旧来源如何被读取并并入；
  - 如何证明 child 没有把 provenance 当普通正文保存。
    §15 阶段 0 的验收“新写入记忆带完整 provenance”目前仍无法由现有设计执行。需要在 spec 中至少写出 child-side 的传递协议和失败行为；否则阶段 0 仍是“先改生产代码，再想办法接上 benchmark”。
- [新增 `source_timestamp`，但系统未必会让回答模型看到它 (line 151)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:151)
  把时间写入 SQLite 字段只能保存元数据，不能自动改善 temporal reasoning。当前已验证的 search 输出字段是 Created / Last used，§4.3 又明确 created 是写入当天；文档没有说明 source_timestamp 是否出现在：
  - 索引 headline；
  - memory_search 返回；
  - memory_read body；
  - 最终回答上下文。
    如果四处都没有，时序题仍然无法使用这个字段；如果只在 runner 的 trace 中存在，评分时也不能把它算成被测系统获得的信息。应明确 source timestamp 的展示位置、格式和是否属于生产行为。否则“时序题有可解释的时间来源”只是数据库层面的保存，不是模型可用的输入。
- [coverage 定义仍把 extraction 和 retrieval 混在一起 (line 295)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:295)
  当前定义是：
  source_session_ids ∩ answer_session_ids
  source_turn_ids ∩ has_answer:true
  但 source_session_ids / source_turn_ids 是 ingest 产生的 provenance，还是 query 实际返回的 memory 的 provenance，仍没有写清。两种含义会产生完全不同的结果：
  - 对全部 ingest memory 求交集：测的是抽取覆盖；
  - 对 search 返回 memory 求交集：测的是检索覆盖；
  - 对 read 进入上下文的 memory 求交集：测的是回答前可用证据覆盖。
  既然文档已经决定第一期不做因果归因，最稳妥的做法是把名称明确拆开：extraction_provenance_coverage、search_provenance_coverage、read_provenance_coverage，每个都写清候选集合和 top-k/cutoff。否则报告仍可能把“所有记忆里曾经有来源”误读成“这次查询检索到了”。
中优先级问题
- [smoke 记忆仍处于正式查询路径中 (line 285)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:285)
  §7.2 顺序仍是 ingest -> smoke -> snapshot -> query，而 smoke 会写入一条已知记忆。§9.3 没有要求删除它、隔离 namespace 或重建索引。即使这条内容与 benchmark 问题无关，它也会改变：
  - 索引长度；
  - search 候选集合；
  - memory id 排序；
  - token 成本；
  - 甚至模型的行为。
    smoke 必须在独立 store 运行，或者验证后销毁并从干净 ingest 快照重新开始。当前阶段 1 的“无污染”检查只保护真实生产库，没有保护 benchmark case 自己不被测试夹具污染。
- [三态判定缺少“对应 memory”的确定性算法 (line 239)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:239)
  index_only 的定义依赖“证据对应记忆的 headline”，read_into_context 依赖“对应 memory 的 body”。但同一 evidence 可能被多个 memory 合并、改写或重复抽取，headline 也可能被模型改写。文档还确认 search 输出不包含 id。需要规定：
  - memory-to-evidence 的匹配是通过 provenance 还是文本匹配；
  - 多个 memory 对应同一 evidence 时如何计数；
  - 被 replace 的旧 memory 是否仍算；
  - search 返回没有 id 时如何关联到 SQLite row。
    否则“三态互斥且可机械判定”的结论仍然过强。
- [重复运行的冻结状态还没有定义完整 (line 181)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:181)
  文档说 query 前做快照，但没有说明快照是否包含：
  - SQLite 主库和 WAL；
  - markdown 导出；
  - 索引缓存；
  - session JSONL；
  - settings 和 extension 构建产物；
  - 当前日期/时钟状态。
    如果只复制 SQLite 主文件而 WAL 尚未 checkpoint，恢复出来的记忆库可能与第一次查询不同。应定义一个 canonical baseline artifact，并规定如何 checkpoint、复制、恢复和校验 hash。
- [“固定 seed + temperature=0”不能直接作为稳定性保证 (line 350)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:350)
  本地 provider、工具调用顺序、并行 ingest、KV cache、模型运行时版本都可能仍然非确定。文档已经要求记录 cache 状态，这是好的，但还缺：
  - 不支持 seed 时的明确记录；
  - 三次运行是否复用相同模型进程；
  - 并行度对 memory id 分配和排序的影响；
  - 什么时候判定是模型随机性，什么时候判定是 harness bug。
    目前“超出声明区间则标为不稳定”仍缺少运行前固定的阈值和失败处理。
- [阶段 0 让一期 scope 扩大成生产 schema 迁移项目 (line 374)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:374)
  agent-eval 仍是独立仓库，但阶段 0 要改 pi-hermes-memory、迁移既有 85 条生产记忆，并处理上游合并冲突。这已经不是单纯的 benchmark harness 前置工作。需要明确：
  - provenance 改动在哪个仓库提交；
  - benchmark 使用哪个 fork commit；
  - 85 条既有记忆迁移是否影响真实用户库；
  - 迁移失败如何回滚；
  - 如果生产特性尚未合入，阶段 1 是否允许先用 adapter-side manifest。
    否则“一期代码独立于被测系统”的仓库边界与阶段 0 的实际交付物不一致。
还有一个建议性补充：在 adapter 运行时计算六类别题数，并把 dataset_counts_by_type 写入 manifest.json；同时在 scorer 启动时校验每个类别的分母等于 adapter 统计值。这样既不需要在 spec 中编数字，也能防止数据集版本、过滤条件或 _abs 分类发生漂移。
这版比上一版已经可靠很多。真正还需要闭合的是三条链：provenance 如何穿过 child pi、source timestamp 如何进入模型上下文、coverage 到底针对哪一批 memory 统计。三条链闭合后，剩余工作才主要是实现细节。
