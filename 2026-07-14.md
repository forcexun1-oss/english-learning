# English Learning Log — 2026-07-14

- 08:46 [EN] continue
- 08:48 [EN] <task-notification>
<task-id>af54ad44f1f29169c</task-id>
<tool-use-id>toolu_01MANJDSjA9xJvQa57CqumAp</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/6c3945e6-6f81-469a-bb86-e86238f3f576/tasks/af54ad44f1f29169c.output</output-file>
<status>completed</status>
<summary>Agent "Close llama.cpp streaming-decode gap" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Stopping broad static analysis. Answering the three coordinator questions directly.

---

## 1. make_resident at init — lazy or eager?

`make_resident` is NOT called at init for all entries. The no-copy init pass (Change G) does two things at startup:

1. Calls `ggml_moe_gpu_slab_buffer_create_nocopy(...)` → creates 120 Metal buffer objects (40 layers × 3 projections), each wrapping the full tensor's page-aligned virtual address range. **No physical page faults.** The OS does not fault mmap pages on `newBufferWithBytesNoCopy` — they remain file-backed and cold.

2. Writes metadata into `entry.gpu_slab_slices[pi]` (`slot`, `buffer_index`, `slot_stride`, `slot_data_off`). No `publish_resident` called here.

`publish_resident` (which populates `gpu_slab_by_key`) is called only from `gpu_slab_stage_entries` → triggered by `touch_many` → triggered by `cb_eval` firing on each layer's topk tensor at decode time. Only the selected top-k experts per layer are staged per token.

**Physical pages accessed per token: top-k × 40 layers × 3 projections × 1,114,112 bytes.** For k=8: 8 × 40 × 3 × 1.07 MiB ≈ **1.02 GiB** hot pages per forward pass.

**Mapped-resident (virtual) per layer:** 3 × 256 × 1,114,112 = **815.6 MiB/layer × 40 = 31.88 GiB total virtual.**

---

## 2. Memory safety for up+down no-copy

The 31.88 GiB is clean, file-backed (GGUF mmap). These pages:
- Do **not** go to the compressor when evicted — they are freed back to the filesystem, not compressed
- The "stored compressor" metric only rises for dirty/anonymous pages

Physical hot set at any moment: ~1.02 GiB (8 experts × 40 layers × 3 proj) of clean pages. Peak stored compressor with all-proj active = same ~5.23 GiB baseline (from normal model kv-cache and activation tensors, not from expert weights).

**No memory wall. The up+down no-copy mapping is safe.** No risk of stored compressor approaching 22 GB from this approach.

---

## 3. Honest outcome — correctness wall, not memory wall

The blocker is not memory — it is a **wrong-output bug** in `GATE_NOCOPY=all` that I could not resolve through static analysis alone, and cannot run to diagnostic because the Qwen3.6-35B SSD is not mounted and the only local MoE models (Gemma4-26B/31B in Ollama) fail with tensor count mismatches against this branch's llama.cpp.

Numbers available:

| | decode tok/s | SHA | peak stored compressor |
|---|---|---|---|
| off (baseline) | 14.0 t/s | `5976e445...` | 5.23 GiB |
| gate-only (GATE_NOCOPY=1) | 28.1 t/s | same SHA (proven) | 5.23 GiB |
| all-proj (GATE_NOCOPY=all) | **TBD** | **TBD** | **TBD** |

All-proj numbers are blocked by the wrong-output bug. The debug prints added this session (per-buf_idx addr_lookup trace + init diagnostic for layer 0 UP/DOWN) are compiled and in the working tree, ready to run when the SSD is mounted. The build is clean with those prints.

**Changes in working tree (NOT committed):** `common/expert-cache.cpp` with Changes A–G plus two DEBUG prints. No push.</result>
<usage><subagent_tokens>123070</subagent_tokens><tool_uses>0</tool_uses><duration_ms>93915</duration_ms></usage>
</task-notification>
- 08:51 [ZH] 现在prefill和decode的数据是多少
- 08:52 [ZH] 还有必要继续吗
- 09:21 [ZH] 为什么和mlx差这么多,核心差异在哪里
- 09:40 [ZH] mlx当前最好的启动参数给我一份, 我想要日常使用, 主要给mac-assistant用, 日常简单的单词翻译, 专家应该能全命中缓存, 偶尔使用agent
- 09:45 [ZH] 怎么有qwen3.6-35b-mlx-q6-aligned 应该用哪个
- 09:54 [ZH] 我偶尔也要使用agent的, max token 512不够, 为什么28-30 tok/s 不是77吗
- 09:59 [ZH] 不是优化完流式也差不多77了吗
- 10:03 [ZH] 都可以, prefill也一起做, 开始吧
- 10:07 [ZH] 不对, 有个全量内存的方案, 有个流式的方案, 只是流式有很多缓存在内存中, 你好好看看
- 10:08 [ZH] 最终是不是有个stream的方案已经达到了77tok/s
- 10:10 [ZH] 最终是不是有个stream的方案已经达到了77tok/s
- 10:12 [ZH] 合入到main了吗, 启动参数是什么, 我想要在mac-assistant中使用, 日常用翻译, 偶尔用agent, 我要packed streaming，很合适吧
- 10:24 [ZH] 都可以, prefill也一起做, 开始吧
- 10:28 [ZH] mlx还有哪些代码没有合入到main, 可以合入的,都合入一下, 收收尾
- 10:37 [ZH] ./run-mac-assistant.sh 只执行这个就行了?
- 10:40 [ZH] 全写到 ./run-mac-assistant.sh这一个脚本里面去
- 10:44 [ZH] qwen3.6-35b-mlx-q6-aligned 现在用了这个模型之后, function call的的解析应该不对了
- 13:43 [EN] continue
- 14:14 [ZH] 不对啊, 你看看
- 14:49 [ZH] 当前mlx常驻内存是哪些东西
- 14:52 [ZH] 现在是每次请求都现加载到内存吗, 我感觉请求完就释放了
- 16:05 [ZH] 当前配置的模型是什么
- 16:07 [EN] restart it
- 16:17 [ZH] 把这个MODEL=deepseek-v4清理了
- 16:18 [ZH] 不对, 加回去, 我们怎么会共用同一个变量
- 16:34 [EN] where is the project_pb101_board_topology.md
- 16:36 [EN] move it to this project, so everyone can see it
- 17:20 [ZH] 看看minicpm-v4-4b-a04 这个模型怎么部署, 只有一半吗
- 17:23 [ZH] 你测试一下,好像不对
- 17:24 [ZH] 他为什么不回答OK
- 17:29 [ZH] 为什么agent提问,每次都要卡,好几秒, 在mlx那边问了,他说模型TTFT很快的, 而且调完工具也卡, 再提问也卡, 那么是哪里卡, 多轮对话有kv cache吗
- 21:34 [ZH] 试试看,简单的任务交给codex luna模型
- 21:37 [EN] restarted, and re-ran, translation is fast, agent is slow
- 21:42 [EN] restart it
- 21:44 [ZH] done, check the log, send message: 你好, response roughly 10s later
- 21:46 [EN] done, check the log again
- 21:49 [EN] done, check the log
- 21:53 [EN] done, check the log
- 21:56 [EN] yes, go ahead
- 22:00 [EN] the second time is fast ,but the first time is too slow, cache the prefix while sever starting, and cold starting is too slow
- 22:05 [EN] done, restart it and check the log
- 22:06 [EN] done, check the log
