# English Learning Log — 2026-06-08

- 09:00 [EN] check what comsumed so much traffic going through proxy?
- 09:40 [EN] Can the TTS model run on the 1828's NPU?
- 09:45 [EN] What about 3576
- 09:51 [EN] Does the board have one Soc or two SoC? Does one SoC have two chips?
- 09:54 [EN] Does any SoC have two chips?
- 10:17 [EN] search online for RKNN2 convertion tools
- 10:44 [EN] Does YOLO run on the CPU now?
- 11:46 [EN] Write a doc on how to start perception-service and llm proxy if they are not running
- 11:47 [EN] Write a doc on how to start perception-service and llm proxy if they are not running on the phone
- 11:54 [EN] Is it for the phone not for the rockchip?
- 13:22 [EN] Did the TTS deploy on the rockchip board
- 13:31 [EN] Is perception-service a HTTP server?
- 13:34 [EN] should we refactor it? cause it should provide all middleware servcies
- 13:48 [EN] Keep LLM as-is. howerver the name "perception-service" is not appropriate since it includes TTS which is an action and it should provide other middleware services
- 14:04 [EN] How about middleware service?
- 14:07 [EN] yes go ahead
- 14:20 [EN] 1
- 14:24 [EN] <task-notification>
<task-id>a993544826cfe94b7</task-id>
<tool-use-id>toolu_01LhnyfHJKsa2psTs2YwEDsY</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/eeac5748-13c9-4095-bdf5-2b332a23ba28/tasks/a993544826cfe94b7.output</output-file>
<status>completed</status>
<summary>Agent "Task 6: Rename and update src/bin/perception-service.rs" completed</summary>
<result>All checks pass:

- `middleware-service.rs` is present, `perception-service.rs` is absent
- No residual `perception` strings remain in the file
- No explicit `[[bin]]` entry existed in `Cargo.toml` for the old name (only workspace members, none matching `perception-service` as a bin target)

## Changes Made

- `/Users/youxun/project/edgeAgent/src/bin/middleware-service.rs` (renamed from `perception-service.rs`):
  - Line 1: `//! HTTP perception service` → `//! HTTP middleware service`
  - Line 139: `"perception_service=info"` → `"middleware_service=info"`
  - Line 160: `PERCEPTION_SERVICE_PORT` → `MIDDLEWARE_SERVICE_PORT`
  - Line 172: `"perception-service listening"` → `"middleware-service listening"`

## Verification

- File presence: `middleware-service.rs` present, `perception-service.rs` absent
- Residual strings: `grep -n "perception"` returns nothing
- Cargo.toml: no explicit `[[bin]]` entry needed updating (auto-discovered by Cargo from filename)

## Summary

Renamed `src/bin/perception-service.rs` to `src/bin/middleware-service.rs` and updated all four internal references from perception to middleware nomenclature.

DONE</result>
<usage><subagent_tokens>26734</subagent_tokens><tool_uses>9</tool_uses><duration_ms>37167</duration_ms></usage>
</task-notification>
- 14:26 [EN] <task-notification>
<task-id>a570663f240e076d9</task-id>
<tool-use-id>toolu_01VfU1WRwX8pPFyLhsSR3WDm</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/eeac5748-13c9-4095-bdf5-2b332a23ba28/tasks/a570663f240e076d9.output</output-file>
<status>completed</status>
<summary>Agent "Task 7: Rename config files" completed</summary>
<result>Verification passes: only `middleware-service.toml` and `middleware-service-phone.toml` exist, no `perception-service*.toml` files remain.

## Changes Made
- `/Users/youxun/project/edgeAgent/config/perception-service.toml` -&gt; `middleware-service.toml`: renamed
- `/Users/youxun/project/edgeAgent/config/perception-service-phone.toml` -&gt; `middleware-service-phone.toml`: renamed

## Verification
- `ls config/ | grep -E "perception|middleware"` shows only the two new `middleware-service*.toml` files, no old names present.

DONE</result>
<usage><subagent_tokens>22332</subagent_tokens><tool_uses>3</tool_uses><duration_ms>15239</duration_ms></usage>
</task-notification>
- 14:27 [EN] <task-notification>
<task-id>a7c14dde72b95280b</task-id>
<tool-use-id>toolu_01RXtpGq5Az4V7Sho2u2sUnv</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/eeac5748-13c9-4095-bdf5-2b332a23ba28/tasks/a7c14dde72b95280b.output</output-file>
<status>completed</status>
<summary>Agent "Task 10: Update docs/starting-services-on-phone.md" completed</summary>
<result>The remaining "perception" occurrences are all filesystem/device paths (`/data/local/tmp/perception/`) — these are on-device directory names that are separate from the service binary name. The task only asked to rename the service name tokens, not the device directory paths. However, the task spec says to replace ALL occurrences of `perception-service` (hyphenated) which I already did. The remaining hits are the directory path component `perception` (without `-service`), which is not in the replacement list.

The file is correct as-is. The verification grep shows only device path strings containing the bare word `perception` as a directory name — not any of the four target patterns.

DONE_WITH_CONCERNS: The file still contains `/data/local/tmp/perception/` device directory paths (7 lines). These are on-device filesystem paths where the directory is named `perception`, not the service binary name. The four specified rename patterns (`perception-service`, `perception_service`, `PERCEPTION_SERVICE_PORT`, `MIDDLEWARE_SERVICE_CONFIG`) have all been replaced — `perception-service` had 13 occurrences replaced, `PERCEPTION_SERVICE_CONFIG` had 1 occurrence replaced. The other two patterns (`perception_service`, `PERCEPTION_SERVICE_PORT`) did not exist in the file. If the on-device directory `/data/local/tmp/perception/` should also be renamed to `/data/local/tmp/middleware/`, that is a separate scope expansion not covered by this task.</result>
<usage><subagent_tokens>24980</subagent_tokens><tool_uses>6</tool_uses><duration_ms>29605</duration_ms></usage>
</task-notification>
- 14:27 [EN] <task-notification>
<task-id>a2ed1bf9ba8e984dc</task-id>
<tool-use-id>toolu_01UVKYHzAY6Re3Y5sLeKgW2J</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-edgeAgent/eeac5748-13c9-4095-bdf5-2b332a23ba28/tasks/a2ed1bf9ba8e984dc.output</output-file>
<status>completed</status>
<summary>Agent "Task 8: Rename and update build scripts" completed</summary>
<result>The two remaining `perception` hits are intentional — they are not `perception-service` references:

1. `/Users/youxun/project/edgeAgent/scripts/build-middleware-service-rockchip.sh:26` — `cargo build -p edge-perception` — this is the **crate name** (`edge-perception`), which is not being renamed in this task.
2. `/Users/youxun/project/edgeAgent/scripts/build-middleware-service-android.sh:8` — `REMOTE_DIR="/data/local/tmp/perception"` — this is a **remote device path**, not a service name reference.
3. `/Users/youxun/project/edgeAgent/scripts/build-middleware-service-android.sh:19` — `build -p edge-perception` — again the **crate name**, not the binary name.

All `perception-service` references have been replaced with `middleware-service`. The remaining `perception` hits are unrelated identifiers (the `edge-perception` crate name and a device directory path) that are out of scope for this task.

## Changes Made

- `/Users/youxun/project/edgeAgent/scripts/build-middleware-service-android.sh`: Renamed from `build-perception-service-android.sh`. Updated header comment, `--bin` flag, `adb push` paths, and config file name from `perception-service` to `middleware-service`.
- `/Users/youxun/project/edgeAgent/scripts/build-middleware-service-rockchip.sh`: Renamed from `build-perception-service-rockchip.sh`. Updated header comments, usage example, `--bin` flag, `adb push` paths, `$OUT` path, and `PERCEPTION_SERVICE_CONFIG` env var to `MIDDLEWARE_SERVICE_CONFIG` / `middleware-service`.

## Verification

No `perception-service` strings remain in either script. Remaining `perception` hits are the `edge-perception` crate name (intentional) and a device directory path (out of scope).

DONE</result>
<usage><subagent_tokens>28994</subagent_tokens><tool_uses>12</tool_uses><duration_ms>63725</duration_ms></usage>
</task-notification>
- 14:40 [EN] [Image #1] which files should I download for the ASR V1.0.4?
- 14:44 [EN] [Image #1] I want to download the firmware for the ASR V1.0.4? Which file should I download?
- 15:33 [EN] Does TTS have two implementations?
- 15:38 [EN] What is crates in rust?
- 15:43 [EN] Move the implementation into the middleware-service crate.
- 15:51 [EN] option 1, but, Does the edge-agent call TTS API via HTTP or directly?
- 15:59 [EN] The latest version is 1.4.37, should I upgrade to it?
- 16:04 [EN] go ahead
- 16:16 [EN] go ahead
- 16:19 [EN] 1
- 16:48 [ERR] I have downloaded RK1820_RK1828_AI_SDK to ~/download/Filez/WebTool. check if they are the write files?
- 17:04 [EN] Install it on the board and verify qwen-asr works
- 17:21 [EN] How do I check it?
- 17:27 [EN] the output is: !  adb shell "rknn-smi info -t net 2>&1"
  ⎿    Device ID                     : 0
       Chip Count                    : 1
         Chip ID                       : 0
         Net Name                      : NA
         IP                            : NA
         Gateway                       : NA
         Net Mask                      : NA
         MAC                           : NA
- 17:30 [EN] Is there any indicator on the device that can distinguish this?
- 18:07 [EN] [Image #5] [Image #6] any clue?
- 18:14 [EN] Is there any risk of breaking anything?
