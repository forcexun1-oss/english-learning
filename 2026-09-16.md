# English Learning Log — 2026-09-16

- 09:00 [ZH] mac-assistant的启动参数改一下,模型改成全量加载到内存吧,
- 09:07 [ZH] 看看最近的请求,前面总有一个延时,为什么
- 09:18 [ZH] 看下
- 09:50 [ZH] 现在这个服务和图数据库都是部署在docker上的,导致占了很多内存,1之后也不会是部署在docker上,要给端侧智能体用的,2.端侧也分不出这么多内存, 改造一下,要为将来考虑
- 10:19 [ZH] review 一下docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md

- 10:24 [ZH] 拆解任务写详细方案,方案要详细,给dsh用flash模型去执行,他比较笨
- 10:25 [ZH] 发现 5 个需要在进入实现阶段前明确的问题，其中 2 个属于高风险。
高风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:82-92：进程管理方案并不能保证“父进程退出时无 orphan process”。FastAPI lifespan 只覆盖正常 shutdown；进程被 SIGKILL、崩溃、机器断电，或 MCP stdio 宿主异常退出时，子进程仍可能存活并占用端口。文档需要定义 orphan recovery 策略，例如启动时通过 pidfile/端口归属校验清理上一次遗留进程，使用独立 process group，并说明哪些异常场景不保证清理。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:61-69：只分发 falkordb.so 不足以构成可运行的部署包。运行时还依赖兼容版本的 redis-server、Redis ABI、系统动态库以及可能的 OpenMP/runtime 库。尤其是“从官方 Docker image 提取 .so”得到的模块，未必能被 edge 设备上的 Redis 版本加载。需要把 Redis server 版本、FalkorDB/Redis ABI、OS/arch、动态依赖和校验信息纳入 artifact manifest，并在 build/release 阶段做实际 redis-server --loadmodule ... 验证。
中风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:87-88：仅启用 RDB 不能充分支撑“重启不丢图”。RDB 可能在进程被杀或设备断电时丢失最近写入；同时没有定义快照触发策略、save/appendonly 配置、数据迁移、备份、恢复和磁盘空间耗尽行为。至少应明确 v1 的 durability 目标，例如允许丢失多久的数据，是否启用 AOF，以及首次启动/升级时如何处理已有数据目录。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:101-109：配置设计与当前两个服务的配置模型不一致。mcp_server 当前使用 uri，而 server 使用 falkordb_host、falkordb_port、falkordb_database；文档却描述了“两个服务都增加 provider config”，但没有定义统一 schema、环境变量优先级、uri 与 port 冲突时的行为，也没有说明 server 当前默认 db_backend='neo4j' 如何切换为 embedded FalkorDB。建议先给出完整配置示例和兼容矩阵，否则实现很容易只覆盖 MCP server。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:93-97 与 server/graph_service/main.py:27-29：设计假定已有 healthcheck 能反映 FalkorDB 子进程状态，但当前 /healthcheck 始终返回 200 healthy，没有检查 driver 或子进程；MCP 的 /health 也需要确认实际行为。必须明确 liveness/readiness 语义，并规定 FalkorDB crash 后返回什么状态码，否则“driver reports unhealthy”不会自动成立。
其他需要补充的设计点
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:84-86：启动命令未定义认证配置、日志 stdout/stderr 处理、超时后的 terminate/kill 流程、端口被占用时如何判断是自己管理的实例，及 data_dir 的创建权限。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:78-80：mcp_server 和 server 是两个独立应用，当前没有显然的共享运行时模块边界。直接把 manager 放入某一方会造成反向依赖；应明确放在 graphiti_core、新的共享 package，还是复制一层薄适配器。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:123-131：fake subprocess 单测覆盖不了 Redis/FalkorDB 的真实启动、模块加载、ABI、持久化和端口冲突问题；集成测试也不应只描述“binary 存在”，还应验证 artifact 可加载、写入后重启数据仍在、异常退出后的恢复行为。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:113-119：文档说 Docker 配置“exactly as-is”，但同时又说 embedded mode 成为默认；现有 mcp_server/config/config.yaml、Docker 配置和 server 默认值之间需要明确哪些默认值改变，避免 Docker 用户意外启动 embedded 模式或找不到模块。
总体建议：先补一节“运行时契约与兼容矩阵”，至少覆盖 artifact manifest、Redis 版本/动态依赖、配置 schema、数据 durability、进程异常退出、healthcheck 语义和两个服务的生命周期接入点。当前设计方向可行，但这些边界未定之前直接实现，后续很可能出现“本机能启动、发布包不能启动”或“服务显示 healthy 但数据库已死”的问题。
- 10:27 [ZH] 你现在能直接展示mermaid的流程图吗

- 10:34 [ZH] 文档他改了,你再看看

- 10:48 [ZH] 他有重点改动,你再review一下

- 11:14 [ZH] 现在有一个关键问题,我们的整个智能体都是没有评测体系的,首先智能体要有,再就是下面的各个模块,每个子功能要有,比如工具调用,检索效果,记忆写入,查询,外部记忆的写入查询,这些都是要有完整的评估体系的,当然不光是我说的这些,你先思考一下
- 11:16 [ZH] 再看看呢

- 11:21 [ZH] 但复审后仍有 3 个重要问题。
高风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:185-196：close backstop 的实现描述仍可能产生 PID 误杀。文档要求在 embedded_client.close() 之后再读取 embedded_client.client.pid，但 close() 可能修改或清理 client；更重要的是，单纯 psutil.pid_exists(pid) 后再 SIGTERM/SIGKILL 存在 PID reuse race。应在 close 前保存 PID、socket/pidfile 路径和启动时间，并在发送信号前再次确认进程命令行/启动时间仍匹配当前实例。还要明确 backstop 自己的异常不能覆盖原始 shutdown 异常。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:110-115, 345-358：目前只是验证了 serverconfig 参数“能启动并读写”，但还没有证明 AOF 配置真正生效。文档自己也承认“不能假设传入 key 就等价于 redis.conf”。如果 redislite 的配置格式、键名或生成配置逻辑不同，最终可能回退到 RDB，而测试仍然通过正常 .close() + reopen。集成验证必须检查运行中的 Redis 配置，例如确认 appendonly=yes 和 appendfsync=everysec，并单独覆盖强制 kill child 后恢复数据的场景。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:91-109：将 falkordblite 设计为两个 service 的“显式 opt-in extra”后，文档需要明确 native quickstart 如何安装这个 extra。否则“README quick-start 首先推荐 embedded”，但默认安装仍不包含运行所需依赖。尤其 mcp_server 当前的 runtime dependency 是 graphiti-core[falkordb]，而 uv sync --extra falkordblite 只有在用户进入 mcp_server 项目并执行对应命令时才会生效。需要给出完整安装命令，并确保发布包/启动脚本不会在用户只执行普通安装后才报错。
中风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:169-184, 269-285：healthcheck 说对 embedded_client 执行 PING，但没有定义它是同步还是异步调用，以及健康检查超时。HTTP health endpoint 不能无限等待 Redis；建议规定独立 timeout，并明确 async client 的调用方式和异常映射为 503。否则数据库异常时健康检查本身可能挂住。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:206-231：server 的生命周期方案仍缺少初始化失败清理路径。lifespan 中创建 singleton、建索引或初始化 Graphiti 失败时，必须关闭已经创建的 AsyncFalkorDB，否则 startup 失败会留下 child process。当前只描述了 after yield 的正常 shutdown，没有明确 try/except 覆盖 yield 之前的部分。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:116-125：文档把 redislite 的 stale-instance detection 当成可靠能力，但当前已经实测到 async cleanup 存在 bug。既然第三方 cleanup hand-off 已经出问题，不能只对 shutdown 加 backstop，却不验证 stale-instance adoption 本身是否会错误复用已失效 socket/pidfile。至少应把“异常残留 child + stale pidfile/socket + 下一次启动”列为集成测试场景。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:252-267：平台约束写的是 Linux manylinux glibc，但没有说明系统 glibc 的最低版本。锁文件中的 wheel 标签是 manylinux_2_39，这意味着许多常见 Linux edge 系统的 glibc 低于 2.39 时无法安装/运行；“manylinux”本身不足以表达这个约束。应明确 glibc >= 2.39，或者验证是否存在兼容更广的 wheel。
结论
这版已经接近可以进入实现，但我建议把以下 3 个点设为 implementation gate：
1. 验证 AOF 配置确实生效，并测试强杀 child 后的数据恢复；
2. 补齐 startup 失败时的 child cleanup；
3. 把 falkordblite extra 的安装方式和 Linux wheel/glibc 前置条件写进 native quickstart。
- 11:38 [ZH] 再看看

- 11:43 [ZH] 复审发现 2 个仍然成立的问题。
高风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:91-109：依赖方案仍然有“声明方式”和“实际安装结果”不一致的风险。文档说两个 service 新增名为 falkordblite 的 optional extra，并通过 uv sync --extra falkordblite 安装；但这个 extra 需要同时保证 graphiti-core[falkordblite] 的依赖约束被纳入解析，尤其是 redis<9。当前 mcp_server/pyproject.toml 使用本地 editable graphiti-core，而 server 的 extra 还没有实际落地。实现时不能只新增：
  falkordblite = ["falkordblite>=0.10.0", "redis<9"]
  还需要在两个干净环境中验证 uv sync --extra falkordblite 的最终 lock graph，确认没有被已有的 redis>=9 依赖冲掉，并确认 Python 3.10/3.11 下不会出现不可安装的条件依赖组合。建议把“两个 service 的 fresh install + uv run import smoke test”列为明确验收项。
中风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:534-557：设计要求 README 更新为 native quick-start，但当前仓库里的 mcp_server/README.md:79-99 仍然把 Docker 作为 prerequisite 和默认安装方式，仍是 uv sync，没有 uv sync --extra falkordblite，也没有 Python 3.12/glibc 2.39、AOF 和 orphan-process 限制说明。作为 spec 本身，这意味着“文档交付物”还未体现在当前工作区；如果这是 review 的目标分支而不是纯设计阶段，需要把它视为未完成项。至少应在实现计划中明确 README、native config、Docker config 三者的更新和一致性测试。
另外有一个值得在 implementation plan 中固定的细节：
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:367-369, 450-454：data_dir 与 dbfilename 的关系仍然容易误导。示例写 data_dir: "./data/falkordb"，代码却传入 data_dir / f'{database}.db'，而验证又发现 redislite 的 redis_dir（pid/socket）和 Redis 的实际 dir 是不同临时目录。实现计划应明确 data_dir 是项目的逻辑持久化根目录、dbfilename 的精确路径、AOF 实际存放目录，以及 pid/socket/log 是否允许落在临时目录。否则用户备份 data_dir 时可能遗漏真正的 AOF 文件。
结论：设计内容现在基本可以进入实现规划；剩下的主要不是架构漏洞，而是要把依赖安装和 README/native quick-start 真正落到仓库，并用干净环境验证最终安装结果。
- 13:06 [ZH] 再看看呢

- 13:09 [ZH] 高风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:68-109：文档仍然同时存在两个版本约束，容易导致实现落错版本。root pyproject.toml 当前声明的是 falkordblite>=0.5.0，而 service extra 计划使用 >=0.10.0；文档又说“已在真实 pyproject 中验证”，但当前工作区的 mcp_server/pyproject.toml 和 server/pyproject.toml 并没有这些 extra。建议把设计里的目标依赖定义成唯一来源，并明确需要同步修改 root/service 的 pyproject.toml 和各自 lockfile。否则后续实现者可能只改 service extra，导致 root editable package、发布包和 CI 使用不同版本约束。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:534-557：文档明确要求 native quick-start 必须是完整可运行步骤，但实际 mcp_server/README.md:79-99 仍然写的是 Docker prerequisite 和普通 uv sync，没有 uv sync --extra falkordblite，也没有 Python 3.12/glibc 2.39 前置条件。若当前任务是 review 设计文档，这属于“设计要求尚未落地”的交付缺口；如果 README 更新不在本次实现范围，需要在 spec 中明确它属于后续实现任务，而不是现状描述。
中风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:367-369, 478-483, 508-512：data_dir 的语义仍然不够稳定。文档示例把它描述为持久化目录，但实测说明 redislite 的 redis_dir（pid/socket）是临时目录，而 Redis 的 CONFIG GET dir 才是实际 AOF 目录；同时示例代码把 data_dir / f'{database}.db' 作为 dbfilename。实现计划需要明确一个正式契约：data_dir 是否保证包含 AOF/RDB、具体 dbfilename 路径是什么、pid/socket/log 是否允许在临时目录，以及备份时用户应该备份哪一个目录。否则“备份 data_dir”可能仍然无法恢复完整数据。
除此之外，上轮的主要问题已经处理：AOF 已通过 CONFIG GET 和 child SIGKILL 验证，healthcheck 有超时，启动失败清理有明确路径，PID reuse 也有 create-time 校验，Linux wheel 的 glibc 限制也已写明。
结论：架构层面已经可以进入实现；实现前只需要把依赖/lockfile 的单一版本来源、README 安装路径和 data_dir 物理布局定死。
- 13:22 [ZH] 再看看

- 13:25 [ZH] 新的仓库建了吗,这个应该放到新的仓库吧
- 13:29 [ZH] 中风险
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:448-460：data_dir 表格中的 RDB 文件名看起来不准确。文档写的是：
  \<data_dir>/\<database>.db.settings
  但示例实际传给 AsyncFalkorDB 的是：
  dbfilename=str(data_dir / f'{database}.db')
  这两个路径不一致。.settings 可能是 redislite 的内部辅助文件，而不是 graph data 的 RDB 文件。建议用实际目录 listing 和 CONFIG GET dbfilename 的结果分别确认，并把表格拆成：
  - dbfilename 对应的 RDB 文件；
  - appendonlydir/；
  - redislite 的 settings/metadata 文件；
  - 临时 pid/socket/config/log 文件。
  目前“备份 data_dir 足够恢复”的结论可能是对的，但文件名描述不应依赖推测。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:461-463, 484-489：appendfsync everysec 不应描述成严格的“最多约 1 秒丢失”。它通常意味着 Redis 每秒尝试 fsync，但调度延迟、IO 阻塞、OS/cache 行为可能使实际丢失窗口大于 1 秒。建议改成“目标为约 1 秒，实际上限不作严格保证”，并在 durability contract 中写明这是 best-effort durability，而非硬性 RPO。
交付状态提醒
- 当前 mcp_server/README.md:79-99 仍未更新，mcp_server/pyproject.toml 和 server/pyproject.toml 也仍未加入对应 extra；不过这版 spec 已经明确写成 implementation plan 的任务，而不是假装已经完成。因此这不再是设计缺陷，而是后续实现必须兑现的交付项。
结论：设计本身现在可以进入 implementation planning。实现前只需修正 data_dir 文件名事实，并把 AOF 的“约 1 秒”改成非严格保证；其余问题已经有足够明确的约束和验证方案。
- 13:33 [ZH] 再看看

- 13:43 [ZH] review agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md, 从最基本的问题开始思考,后面全面考虑一下

- 13:44 [ZH] 仍有两个低风险的实现注意点：
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:454-455：.settings 文件的用途仍未确认，但文档已经采取了安全策略——整体备份目录，因此不构成阻塞。实现时建议不要对该文件做清理、重命名或白名单复制。
- docs/superpowers/specs/2026-09-16-edge-native-falkordb-deployment-design.md:96-104：依赖和 README 仍是待实现状态，不是当前设计缺陷；落地时要确保 root、mcp_server、server 三处版本约束和对应 lockfile 一起更新，并实际更新 mcp_server/README.md。当前 README 仍是 Docker-first，但 spec 已明确把它列为 Task 交付物。
- 13:47 [ZH] 再看看

- 13:55 [ZH] 仍有两个非阻塞的交付提醒：
- mcp_server/README.md 当前工作区仍未实际切换到 native-first quick-start；不过文档已经明确把它列为后续实现任务。
- mcp_server/pyproject.toml、server/pyproject.toml 及对应 lockfile 当前仍未实际加入 extra；实现时必须和 root pyproject.toml 一起更新，并按文档中的 fresh-install 验收方式验证。
结论：设计评审通过。当前剩余内容属于实现阶段的落地任务，不再是设计本身的阻塞缺陷。
- 13:59 [ZH] Oracle是什么,不是数据库把

- 14:00 [ZH] 当前版本还不能作为实现依据。最大的风险是：文档把几个“相关指标”直接解释成了“因果归因”，而中间缺少可验证的实验对照和 provenance 设计。下面按严重程度列出问题。
高优先级问题
- [设计无法识别“抽取丢了”与“检索没找到” (line 108)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:108)
  Oracle 只保留 evidence sessions，S 使用完整 haystack。两者的记忆抽取输入不同，因此 Oracle − S 同时混合了：
  - 抽取阶段看到的 session 数不同；
  - distractor 对抽取结果的影响；
  - 记忆数量、内容和排序不同；
  - 检索竞争和上下文长度不同；
  - 回答阶段可见信息不同。
    所以这个差值不能被称为“记忆系统造成的全部损失”，更不能进一步拆成“抽取损失”和“检索损失”。至少需要增加独立对照：
  1. 完整 haystack 直注 evidence baseline；
  2. 完整 haystack 抽取后直注全部抽取结果；
  3. 固定同一批抽取记忆、只改变检索输入的实验。
     如果第一期确实不做直注管线，应把结论降级为“Oracle/S 差异”，不要写成因果归因。
- [turn-level recall 和 evidence id 匹配目前无法从系统输出计算 (line 122)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:122)
  LongMemEval 的 evidence 是原始会话中的轮次或 session，而 Hermes 返回的是新生成的 memory id。文档没有定义以下关键映射：
  - 一个 memory 由哪些 source session / turn 生成；
  - 一个 memory 合并多个 session 时如何归属；
  - memory 内容只是抽取了事实的一部分时如何判断覆盖；
  - memory_search 返回的是 memory id、headline、body 还是别的结构；
  - agent 只使用索引 headline、没有调用 memory_read 时，是否算检索命中。
    没有 source provenance，retrieved memory ids ∩ evidence ids 这个 L2 指标无法成立；没有覆盖判定规则，turn-level recall 也无法成立。实现前应把 provenance 作为一等数据写入每条记忆或旁路 manifest，例如 source_session_ids、source_turn_ids、抽取批次和模型版本，并明确“覆盖”是精确匹配、人工标注还是语义判定。
- [“检索到但没用上”无法由当前数据流判定 (line 141)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:141)
  记录“检索到的 id/session”只能说明工具返回了什么，不能说明回答模型是否读懂、是否采用、是否被更后面的结果覆盖。尤其当前设计同时有常驻索引注入、memory_search 和可能的 memory_read，需要区分至少三种情况：
  - evidence 只出现在 headline；
  - evidence 出现在 search 结果但没有 read；
  - evidence 被 read 展开并进入回答上下文。
    应记录完整的 tool-call/event trace、每次调用的 query、返回项、最终送入回答模型的内容，以及回答中使用的证据引用（如果协议允许）。否则“找到了没用上”只是推测。
- [Oracle 被描述成“检索完美时的回答上限”，但它并不保证检索完美 (line 110)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:110)
  Oracle 变体减少了历史数据，但被测系统仍然要抽取、索引、检索和回答。抽取可能丢失事实，检索仍可能漏召回，模型也可能答错。因此 Oracle 分数更准确的定义是“在 evidence-only haystack 上运行同一系统的分数”，而不是回答环节上限。真正的回答上限需要把 gold evidence 直接注入回答上下文，并固定回答 prompt/model。
- [时序字段设计与现有系统语义冲突 (line 160)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:160)
  文档要求把 haystack_dates 映射到记忆的 created 字段，并在风险项中要求断言二者相等。但根据现有 pi-hermes-memory 的运行方式，memory 的 created 通常是写入时由 store 生成的时间，而不是原始会话发生时间。仅改 session 文件名不能保证 created 等于 benchmark 日期。若通过改系统时钟、直接写 SQLite 或修改 memory schema 来伪造日期，就已经不再是“复刻生产行为”。应先明确时序题需要的时间来源：
  - 作为记忆正文中的事实；
  - 作为 source session metadata；
  - 还是作为 memory created/updated 字段。
    然后定义一种生产可解释、可复现的注入方式。当前“必须相等”的验收条件很可能无法实现。
中高优先级问题
- [“分数口径与公开榜单一致”没有被具体定义 (line 15)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:15)
  公开榜单的可比性不只取决于题目和 judge。还需要锁定并记录：
  - LongMemEval 数据集 commit/hash 和具体变体；
  - system prompt、回答 prompt、工具描述；
  - 被测模型、provider、量化版本、temperature、seed、最大输出长度；
  - 是否允许多次 search/read；
  - query 前是否把 index 注入上下文；
  - judge prompt、judge 模型版本、API 参数和重试策略。
    当前方案明确使用本地 Qwen，却又使用“官方协议”表述，容易让读者误以为可以直接和榜单数字比较。应把结论改成“使用同一数据和评分协议生成可参考分数”，并把模型与 harness 差异作为报告中的硬字段。
- [abstention 指标没有定义“正确拒答” (line 125)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:125)
  _abs 题的正确输出格式、允许的拒答措辞、部分回答、带不确定性回答和编造回答之间的边界都没有写清楚。abstention accuracy 也不能单独等价于“没找到却编了的幻觉率”：它至少需要同时报告：
  - no-evidence 条件下的拒答率；
  - no-evidence 条件下的 hallucination/false-answer rate；
  - evidence 存在时的错误拒答率。
    还要明确 judge rubric，否则同一答案可能被不同 judge 判成“谨慎回答”或“编造”。
- [judge 校准方案的统计依据不足 (line 177)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:177)
  30 题、90% exact agreement 不能稳定证明 judge 足够可靠，尤其题型分布、类别不平衡和抽样方法都没有定义。还存在两个问题：
  - 30 题中允许最多 3 题不一致，但这对不同题型的错误并不等价；
  - 只报告一致率，没有区分 judge 偏严、偏松和随机不一致。
    至少应按六类分层抽样，预先固定题目，报告混淆矩阵和每类结果；最好同时报告 Cohen’s kappa 或 Krippendorff’s alpha，以及一致率置信区间。若 calibration 失败，不能只说“该轮分数不作数”，还要定义是否重写 rubric、换 judge 或暂停整个 benchmark。
- [每题一个独立 `PI_CODING_AGENT_DIR` 会改变被测系统的行为 (line 141)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:141)
  如果每题都从空目录开始，系统不会经历连续运行时的记忆累积、跨题污染、索引增长和数据排序；如果一个 case 包含完整历史，这仍然是“每题隔离”的实验，而不是一个长期运行的 agent。两种模式测量的对象不同，必须明确：
  - case 是一个独立用户生命周期；
  - 还是所有题共享同一个记忆库；
  - ingest 是按 session 还是按时间顺序；
  - query 是否会修改 last_referenced；
  - 是否重复运行同一题。
    当前文档在“零污染”和“可重复”之间没有给出实验单元定义。
- [隔离护栏不够证明“没有写入真实记忆库” (line 180)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:180)
  只断言路径字符串不等于真实目录不够。还需要处理 symlink、相对路径、继承环境变量、settings 中的绝对路径、SQLite/WAL/lock 文件和扩展加载路径。验收应包括：
  - 解析后的 realpath；
  - 运行前后真实目录快照/hash；
  - 所有 memory、session、SQLite、WAL、lock 文件的写入清单；
  - 子进程环境快照；
  - 异常退出后的清理和残留目录处理。
    “跑完即删”也会删除失败现场，建议先保留失败 case 的 artifact，只在成功且通过污染检查后清理。
中优先级问题
- [session JSONL 转换缺少合法性和语义校验 (line 154)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:154)
  文档只描述了字段形状，没有规定：
  - 缺失或未知 role 怎么处理；
  - 多段 content、空 content、非字符串 content 怎么处理；
  - session 顺序、timezone、日期格式和 DST 怎么处理；
  - 原始 session id 如何保留；
  - 转换后如何证明没有丢轮次或改变内容。
    应增加转换前后 hash、轮次数、role 序列和 source-id 对照测试。否则时序题和 evidence 题可能在 adapter 阶段就已经失真。
- [“headline 覆盖率 100%”不是充分的有效性检查 (line 90)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:90)
  headline 全覆盖只能证明条目能进索引，不能证明：
  - memory 是否真的写进预期 store；
  - 索引是否刷新；
  - search 是否能返回；
  - project/global/user scope 是否按预期加载；
  - body 是否可 read；
  - id 是否稳定且唯一。
    应增加一条 deterministic smoke case：写入已知 memory，分别验证 index、search、read、scope 和 source provenance。否则 headline 断言可能通过，实际查询仍然不可用。
- [成本护栏没有处理方差和失败重试 (line 190)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:190)
  3–5 个 case 不能代表六类题型、不同 session 数量和不同抽取复杂度。还没有规定：
  - pilot 如何分层抽样；
  - token 是输入、输出还是 provider billable tokens；
  - 重试是否计入成本；
  - 超时、失败 case 是否重跑；
  - 并行度是否固定；
  - 本地模型是否存在缓存。
    应按题型和 haystack 规模做 stratified pilot，报告 p50/p95 和失败率，再外推区间而不是单个平均值。
- [文档引用的事实没有在仓库内留下可复核证据 (line 42)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:42)
  “85 条记忆中 52 条”“headline 覆盖率表”“入口只有 memory_read”都属于会影响设计的重要发现，但当前仓库只有 spec，没有查询脚本、输入快照、commit hash 或原始输出。未来代码变化后，读者无法判断这些数字是否仍成立。应把发现转成可运行的检查脚本或将证据放到 docs/experiments/，记录被测仓库 commit、数据库快照和命令输出。
- [阶段验收没有覆盖“结果可复现”本身 (line 237)](/Users/youxun/project/agent-eval/docs/superpowers/specs/2026-09-16-agent-eval-benchmark-design.md:237)
  阶段验收检查了有数字和无污染，但没有要求同一输入重复运行的稳定性。LLM judge、抽取模型和检索排序都可能产生非确定性。至少应定义：
  - 同一配置重复运行次数；
  - 允许的分数波动；
  - seed/temperature；
  - 结果 manifest；
  - 中断恢复策略；
  - 依赖锁定方式。
    “锁定 pi-hermes-memory commit”是必要条件，但远远不够。
需要先澄清的概念
- Oracle 是数据集提供的 evidence-only 变体，还是项目自定义的 gold-memory 注入模式？文档目前把这两者混用了。
- “抽取丢了”是指原始 evidence 没有出现在任何 memory 中，还是抽取出的 memory 没有被检索到？两者必须分开定义。
- “检索到”是 search 返回即可，还是必须进入最终回答模型上下文？这会直接改变 recall 数值。
- 六类别的具体枚举、每类题数和 _abs 的归属需要写入 schema，而不是只在报告中说“六类别”。
- 第一阶段是否允许修改 pi-hermes-memory 以保存 provenance 和 source timestamp？如果不允许，当前归因设计无法落地。
建议在实现前先把方案改成两层结论：
1. 第一期可靠产出：使用固定数据、固定模型和固定运行协议的 QA 分数、工具调用轨迹、索引开销、成本、延迟，以及“Oracle/S 差异”。
2. 第二阶段再产出因果归因：先补 provenance 和至少一个直注/固定记忆对照，再报告抽取、检索、使用和幻觉四段损失。
目前最需要修改的不是目录结构，而是第 5.1、5.2、5.3 和第 7 节的指标定义；在这些定义没有闭合之前，直接开始写 harness 很容易得到一套数字齐全、但无法解释的评测结果。
- 14:10 [ZH] 不用管我的提问了,我知道是什么了, 方案改了,你再review

- 14:15 [EN] continue

- 14:19 [EN] <task-notification>
<task-id>bg9vbwar9</task-id>
<tool-use-id>toolu_01ErgSAYYjSrpzrMQx3BseLX</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-graphiti-mcp-server/e972696d-1058-4dfa-9373-11c80780a3ad/tasks/bg9vbwar9.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch Task 1 to dsh flash model" completed (exit code 0)</summary>
</task-notification>
- 14:25 [EN] continue

- 14:32 [EN] <task-notification>
<task-id>b4byv6coo</task-id>
<tool-use-id>toolu_01XmPKM7mv9ws6gxfk86kutJ</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-graphiti-mcp-server/e972696d-1058-4dfa-9373-11c80780a3ad/tasks/b4byv6coo.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch Task 2 (file edits only) to dsh flash model" completed (exit code 0)</summary>
</task-notification>
- 14:38 [EN] <task-notification>
<task-id>bbezv4u87</task-id>
<tool-use-id>toolu_01FmhgHZXVb8jLvJnRbTh2vS</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-graphiti-mcp-server/e972696d-1058-4dfa-9373-11c80780a3ad/tasks/bbezv4u87.output</output-file>
<status>completed</status>
<summary>Background command "Dispatch Task 3 to dsh flash model" completed (exit code 0)</summary>
</task-notification>
