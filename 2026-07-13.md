# English Learning Log — 2026-07-13

- 09:18 [EN] review the latest optimization plan
- 09:18 [EN] review the latest MTP optimization plan
- 10:44 [ZH] 看看都跑完了, 结论是什么
- 10:47 [ZH] 好, 做独立评审
- 10:56 [ZH] 什么意思,有些代码写了不提交吗
- 10:58 [ZH] 所以不是不提交是你来提交, 交给 subagent 用 sonnet模型来提交吧
- 11:01 [EN] <task-notification>
<task-id>a09235b7f6889dfa7</task-id>
<tool-use-id>toolu_0149VLrgB8VnzRRkKZtWBpXq</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/8136b04c-2c3b-4a73-a91c-72b877f2d1cf/tasks/a09235b7f6889dfa7.output</output-file>
<status>completed</status>
<summary>Agent "Split MTP work into commits" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All 8 files/groups from the working tree were committed; the remaining untracked files are exactly the red-line, user-owned files that were never staged (`.claude/`, `.codex_logs/`, `.omc/`, `.venv312/`, `AGENTS.md`, `CLAUDE.md`, `MLX_EXPERT_STREAMING_SPEC.md`, `MLX_STEP*.md`, `RESUME_AFTER_REBOOT.md`, `docs/specs/*.svg`) — none were touched, staged, or removed.

## Summary

### Style Detected
- Language: English
- Format: plain imperative subject line (no `feat:`/`fix:` prefix), occasional `docs:` prefix for doc-only commits. I matched the plain style since these are code/feature commits, consistent with commits like "Implement fused MTP K1 verification" and "Add corrected MTP confidence-gated path".

### Commits Created (on top of 4dca3da, no rebase/amend, no push)
1. `1d4fb1c` — Enforce strict MTP sidecar loading with exact loading proof — mlx_lm/mtp.py + tests/test_mtp_loader.py
2. `63fe6f8` — Fix hybrid cache rollback in fused MTP K=1 generation — mlx_lm/generate.py + tests/test_mtp_generate.py
3. `f75083d` — Split TTFT from steady-decode timing in MTP benchmarks — benchmarks/mtp_fused_timing.py, benchmarks/mtp_revised_gate.py, tests/test_mtp_timing.py, tests/test_mtp_revised_gate.py
4. `7191024` — Add fixed-context cost-breakdown probe for MTP — benchmarks/mtp_cost_breakdown.py + tests/test_mtp_cost_breakdown.py
5. `3e568e5` — Add opt-in per-session MTP prompt-state manager to ollama bridge — serve_ollama_stream.py + tests/test_mtp_prompt_state_server.py
6. `f89005e` — Add standalone benchmark and memory/lifecycle gates for MTP prompt-state — benchmarks/mtp_prompt_state_benchmark.py, benchmarks/mtp_prompt_state_memory.py, tests/test_mtp_prompt_state_benchmark.py, tests/test_mtp_prompt_state_memory.py
7. `64778f5` — Add guarded real-model custom-bridge validation harness for MTP — benchmarks/mtp_custom_bridge_validation.py + tests/test_mtp_custom_bridge_validation.py
8. `4aa2b7d` — Record MTP results, execution notes, and full plan/checkpoint history — MLX_RESULTS.md, CODEX_NOTES.md, docs/plans/2026-07-12-mtp-correctness-benchmark-and-optimization.md, docs/plans/README.md (includes both required disclosures on test_generate pre-existing failures and TTFT boundary mismatch)

### Test Result
`/Users/youxun/project/mlx/.venv_build/bin/python -m unittest tests.test_mtp_generate tests.test_mtp_loader tests.test_mtp_timing tests.test_mtp_custom_bridge_validation tests.test_mtp_prompt_state_server` → **OK, 97 tests passed**. `tests.test_generate` was not run per instructions.

### detect-changes summary
`npx gitnexus mcp detect_changes(scope=all)` → risk_level **critical**, 142 changed symbols, 66 affected processes, 7 changed files — matches the expected baseline for this branch and was treated as not a blocker.

### git diff --check
Ran before every commit (individually and against the final cumulative diff) — no whitespace errors at any point.

### Confirmation
`git status --short` after all commits shows only the pre-existing user-owned untracked paths (`.claude/`, `.codex_logs/**`, `.omc/`, `.venv312/`, `AGENTS.md`, `CLAUDE.md`, all `MLX_STEP*_SPEC.md`, `MLX_EXPERT_STREAMING_SPEC.md`, `RESUME_AFTER_REBOOT.md`, `docs/specs/2026-07-10-qwen36-moe-model-file-architecture.svg`). No out-of-scope file was staged, no destructive git operation was used, no rebase/amend/push occurred.

Note: git reported a committer-identity warning ("Your name and email address were configured automatically...") on every commit — this is informational only (commits succeeded with the auto-detected identity); no git config was changed, per the no-config-modification rule.</result>
<usage><subagent_tokens>78460</subagent_tokens><tool_uses>23</tool_uses><duration_ms>106838</duration_ms></usage>
</task-notification>
- 11:53 [ZH] 对比一下,这个项目在llama.cpp之上有加了哪些优化, 写个方案, 让llama.cpp追平, 方案用英文, 只写方案,先
- 12:36 [ZH] 给subagent 用sonnet5的模型去实现
<<<<<<< Updated upstream
<<<<<<< Updated upstream
<<<<<<< Updated upstream
- 13:41 [ZH] 这个是北极熊芯部署到开发板的项目吗?
=======
=======
>>>>>>> Stashed changes
=======
>>>>>>> Stashed changes
- 13:41 [EN] <task-notification>
<task-id>af54ad44f1f29169c</task-id>
<tool-use-id>toolu_01HoF9y5fSoNoHP7YUc4yWZz</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/6c3945e6-6f81-469a-bb86-e86238f3f576/tasks/af54ad44f1f29169c.output</output-file>
<status>completed</status>
<summary>Agent "Close llama.cpp streaming-decode gap" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Summary

I worked in `/Users/youxun/project/llama.cpp` (branch `ds4-bindless`) profiling and attempting to close the streaming-vs-resident MoE decode gap for **Qwen3.6-35B-A3B Q8_0** (`models/qwen3.6-35b-a3b-q8_0/Qwen3.6-35B-A3B-Q8_0.gguf`, 36.9GB, this Mac's 48GB unified memory). Net code change in the working tree: **zero** — I implemented one targeted fix, verified it, found it made things worse, and reverted it in full (confirmed via `git diff` matching the pre-existing state exactly).

**1. Baseline** (oracle prompt from `docs/ds4/models/qwen3.6-35b-a3b-q8_0/RECIPE.md`, temp 0, seed 1, greedy, 120 generated tokens, `-ngl 99 --no-repack`):
- Full-resident (flag off): **54.7 tok/s**
- Cold streaming (flag on, GPU expert-slab bindless, 4GiB budget, `--no-mmap` + `LLAMA_EXPERT_STREAM_NOMMAP=1` + `--simulate-used-memory 30GB` forcing genuine cold pread): **12.9 tok/s** (reproduced at 12.6 on a control rerun)
- Gap: **~4.2×**. Both byte-identical to each other at temp 0.

**2. Per-component profile** (using this repo's own pre-existing `LLAMA_EXPERT_CACHE_TIMELINE=1` instrumentation in `common/expert-cache.cpp`, so this doubled as the task's Step-3 A1 instrumentation, already built): wait-pread 45.65 ms/tok (59%, near this Mac's documented 7–11 MiB/ms SSD ceiling), compute-residual 32.36 ms/tok (41%). Critically, that 32.36ms "compute" bucket alone exceeds the *entire* fully-resident per-token decode time (18.28ms) — the ~14ms/tok delta traces to `ggml-backend.cpp:1716-1753`, where the scheduler forces a blocking `ggml_backend_synchronize()` at every one of the 40 MoE layer boundaries whenever expert-cache's `cb_eval` is registered — llama.cpp's exact structural analog of MLX's per-layer `mx.eval()` barrier.

**3. What I tried**: the codebase already has a built, opt-in fix for exactly this (`LLAMA_EXPERT_CACHE_SHARED_EVENT_ENABLE=1`, `ggml_metal_encoder_bindless_stage_wait` in `ggml/src/ggml-metal/ggml-metal-device.m`) — it avoids the CPU sync via a Metal `MTLSharedEvent`. Live-measured: it **regresses** 12.9→7.9 tok/s (still byte-identical). I found and fixed a concrete plausible cause — `newBufferWithLength:` allocates a fresh tiny Metal buffer on every one of 4800 per-layer/per-token calls — by caching one reusable buffer per layer (safe: each layer's tensor is touched exactly once per token, and a token's graph fully retires before the next starts). Verified byte-identical, but it made things **worse still**: 7.9→5.8 tok/s. I ruled out machine drift via a clean sync-path control rerun (12.6 tok/s, matching the original). I reverted the fix.

**4. Honest conclusion**: I could not find a safe, verified, positive change on the "remove per-layer sync" lever within this real-hardware budget. Dominant cost is I/O near this Mac's own documented bandwidth ceiling; the one built candidate for the sync-removal lever underperforms on this exact model/quant/hardware combination, and my best-reasoned fix to it regressed further for reasons requiring GPU-level tracing (Instruments/Metal System Trace) not available here. Per the task's own instruction, reporting this as a correct negative result rather than shipping a speculative or regressive change.

**5. Next lever**: per this repo's own accumulated evidence (`docs/ds4/EDGE_GEMMA4_ROADMAP.md`), sync/prefetch-style fixes repeatedly fail to materialize on the real guarded harness here; byte-reduction (further expert quantization / lower top-k, already scoped as N1/B3 items) is the more promising next step, or a proper Xcode GPU Frame Capture of the shared-event path before another sync-removal attempt.

Relevant paths: `/Users/youxun/project/llama.cpp/common/expert-cache.cpp` (timeline instrumentation, `cb_eval`), `/Users/youxun/project/llama.cpp/ggml/src/ggml-metal/ggml-metal-device.m:637-780` (`ggml_metal_encoder_bindless_stage_wait`, reverted), `/Users/youxun/project/llama.cpp/ggml/src/ggml-backend.cpp:1716-1753` (the sync barrier itself), `/Users/youxun/project/llama.cpp/docs/ds4/models/qwen3.6-35b-a3b-q8_0/RECIPE.md`, `/Users/youxun/project/llama.cpp/docs/ds4/Q2_QWEN3_Q8_RESULTS.md`.</result>
<usage><subagent_tokens>251578</subagent_tokens><tool_uses>79</tool_uses><duration_ms>1906488</duration_ms></usage>
</task-notification>
<<<<<<< Updated upstream
<<<<<<< Updated upstream
>>>>>>> Stashed changes
=======
>>>>>>> Stashed changes
- 14:01 [EN] <task-notification>
<task-id>af54ad44f1f29169c</task-id>
<tool-use-id>toolu_01PZf9sAJSZtKjuEio2m8R9h</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/6c3945e6-6f81-469a-bb86-e86238f3f576/tasks/af54ad44f1f29169c.output</output-file>
<status>completed</status>
<summary>Agent "Close llama.cpp streaming-decode gap" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>---

## Warm Measurement Results

### Throughput

| regime | tok/s | ms/tok | gap vs resident |
|---|---|---|---|
| Resident (flag off, baseline) | **54.7** | 18.28 | 1.0× |
| Cold streaming (NOMMAP + sim-30GB) | **12.9** | 77.5 | 4.2× |
| **Warm streaming (mmap, no sim)** | **21.9** | 45.66 | **2.50×** |

Memory safety: stored compressor stayed flat at **5.8 GB** throughout both warm passes (watchdog threshold was 20.7 GB, never tripped). Free RAM consumed ~16 GB by mmap model pages — loaded into existing free memory, no compression.

---

### Per-component breakdown (warm, sum over 40 MoE layers per token)

| component | ms/tok | % of decode wall |
|---|---|---|
| wait-pread (mmap page-cache loads) | **18.48** | 40.5% |
| compute-residual (GPU FFN + sync barriers) | **27.04** | 59.2% |
| other/staging | 0.40 | 0.9% |
| **total** | **45.66** | |

Expert slab stats: hit-rate **55.90%**, pread bandwidth **18.87 MiB/ms** (vs cold SSD ~10.24 MiB/ms — page-cache is 1.84× faster), 9.96 MiB loaded per layer per token.

---

### Is warm I/O-bound or sync/compute-bound?

**Both, roughly 40/60.** Warm pread does NOT collapse to ~0. The reason: the expert GPU slab is recreated empty on each llama-cli invocation. With a 4 GB slab and 55.9% steady-state hit rate, every token still loads 44.1% of its 40-layer expert accesses from mmap — that's ~398 MiB/token of page-cache copies at 18.87 MiB/ms = ~21 ms/tok of unavoidable I/O. This is structurally different from the MLX finding (iostat≈0) because MLX's slab persists across the session whereas here the slab is process-scoped.

---

### Sync-barrier share of the warm gap (Step 3)

- Resident compute: **18.28 ms/tok** (all 40 layers, no streaming overhead)
- Warm compute-residual: **27.04 ms/tok**
- Excess attributable to 40× `ggml_backend_synchronize()` barriers: **8.76 ms/tok** = 0.219 ms/sync
- As % of warm gap (45.66 − 18.28 = 27.38 ms/tok): sync barriers = **32%**, I/O = **68%**

**Theoretical ceiling if all 40 sync barriers were perfectly eliminated (warm):**
→ (18.48 + 18.28 + 0.40) = **37.16 ms/tok** → **~26.9 tok/s**
→ Improvement: 21.9 → 26.9 tok/s (**+22.8%**)
→ Remaining gap to resident even then: 26.9 vs 54.7 = **2.03×**

The sync barrier is a real but secondary lever. Even a perfect fix captures only 32% of the warm gap; the remaining 68% is page-cache I/O that is not eliminatable without a larger slab (approaching 100% hit rate) or cross-token expert pipelining. The shared-event path (`LLAMA_EXPERT_CACHE_SHARED_EVENT_ENABLE=1`) is the existing attempt at sync removal and measured worse (7.9 tok/s) — the overhead of its Metal async machinery currently exceeds the 8.76 ms/tok it's trying to save.</result>
<usage><subagent_tokens>51310</subagent_tokens><tool_uses>21</tool_uses><duration_ms>737193</duration_ms></usage>
</task-notification>
=======
>>>>>>> Stashed changes
