["PR #990 已经实现了 Qwen3.5/3.6 的原生 MTP speculative decoding, 看看可以借鉴吗, 我们之前的MTP最终没有完成", "那就移植 1、2、3 点,跑一次干净的最终 A/B session 用英文写方案, 让 subagent 使用 sonnet 模型去执行", "<task-notification>\n<task-id>a94f20ecfda2b6655</task-id>\n<tool-use-id>toolu_019TE2JGs7R6SZwV7TQMtJZK</tool-use-id>\n<output-file>/private/tmp/claude-501/-Users-youxun-project-mlx-lm/c0769dfc-e0e2-4ca7-a6d8-a9b4bfd4287b/tasks/a94f20ecfda2b6655.output</output-file>\n<status>completed</status>\n<summary>Agent \"Execute plan Tasks 1-3 (MTP port)\" finished</summary>\n<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>\n<result>All three tasks completed and verified. Here is my report.\n\n## Report to Orchestrator\n\n**Branch:** `feat/mtp-pr990-port` (created from `main`)\n\n**Commits:**\n\n1. `055f0254f0cf9c7da8601d050114cb1f08e74d1b` — feat: add rollback_state snapshot slot to ArraysCache (PR #990 port)\n   - `docs/plans/2026-07-15-pr990-port-and-final-ab-session.md` (new, the plan itself, as instructed)\n   - `mlx_lm/models/cache.py` (+3)\n   - `tests/test_mtp_rollback.py` (new, 28 lines)\n\n2. `d66eb1779448e5f09da8f008bc02713d37af6575` — feat: n_confirmed chunked forward with rollback snapshot in GatedDeltaNet (PR #990 port)\n   - `mlx_lm/models/qwen3_5.py` (+127/-38): `GatedDeltaNet._process_chunk` (new), chunked `__call__`, `DecoderLayer.__call__`, `Qwen3_5TextModel.__call__` all gained `n_confirmed`\n   - `tests/test_mtp_rollback.py` (+69, extended)\n\n3. `93aa786` — feat: replay-free MTP reject via n_confirmed rollback + single-eval rounds (PR #990 port)\n   - `mlx_lm/generate.py` (+62/-15): `import inspect`; `use_fast_rollback`/`model_supports_n_confirmed` detection; `forward()` gains `n_confirmed`; `select_next` drops its eval/round-trip; top-of-round sync pack folds in `y_id`; three-way reject branch (trim-only / fast-rollback / legacy checkpoint-replay); rollback_state clearing on accept; cache-state evals reduced to `mx.eval(candidate_logits)` at 3 sites\n   - `tests/test_mtp_generate.py` (+72, pure additions — `_ToyCoreNConfirmed`, `_ToyTargetNConfirmed`, `TestMTPFastRollback` with its 4 tests)\n\n**Final verification** (`/Users/youxun/project/mlx/.venv_build/bin/python -m pytest tests/test_mtp_generate.py tests/test_mtp_rollback.py tests/test_mtp_loader.py tests/test_speculative_generate.py -v`):\n\n```\n51 passed, 2 warnings, 38 subtests passed in 1.57s\n```\n\nAll pre-existing tests passed unmodified (diff of `test_mtp_generate.py` is pure-addition, 0 deletions). New tests: `TestArraysCacheRollback` (2), `TestGatedDeltaNetNConfirmed` (3), `TestMTPFastRollback` (4) — 9 new tests total, all passing.\n\n**Deviations from the plan, with reasoning:**\n\n1. **Task 2 test fixture tolerance widened.** The plan's `test_rollback_state_equals_prefix_plus_confirmed_only` compared a chunked forward's snapshot against a reference call with a *different* sequence length (S=1 vs S=2) at `atol=1e-4`. On this machine's Metal backend, `nn.Linear`/GEMM produces materially different rounding for different batch-row counts (verified independently, ~9.5e-4 max diff in the raw `in_proj_qkv` projection itself, present even with zero relation to the n_confirmed chunking logic — reproduced with a bare script). I widened only this one assertion's `atol` to `2e-3` and documented why inline in the test. The same-shape comparison in `test_chunked_forward_matches_unchunked` (which actually exercises the chunking logic) still passes at the plan's original `1e-4`.\n2. **Task 2 tiny-model fixture head dims.** `_tiny_gdn()`'s `linear_key_head_dim`/`linear_value_head_dim` were bumped from the plan's `16` to `32`. The custom Metal `gated_delta_step` kernel computes `constexpr int n_per_t = Dk / 32`, and `Dk=16` produces a zero-length C++ array, failing to compile. This is purely a fixture-dimension fix (per the orchestrator's explicit allowance to fix test fixtures, not implementation), unrelated to the n_confirmed logic.\n3. **Task 3: second `target_cache_trim_only` recomputation.** The actual code has two assignments of `target_cache_trim_only` (the plan assumed one) — a second one inside the `prompt_state_valid` branch, since it reassigns `target_cache` to a restored bundle's cache. I mirrored `use_fast_rollback`'s recomputation there too (reusing the already-computed `model_supports_n_confirmed` boolean, no extra `inspect` call) so the fast/legacy branch selection stays correct when a prompt-state bundle is hit.\n4. **Line `mx.array([[candidate, int(y.item())]], ...)` in the accept branch's MTP-batch construction** (original line ~1146) was left as `int(y.item())`, not replaced with `y_id` — this `y` is the *next round's* freshly-selected token (reassigned via `select_next` earlier in the same accept branch), not the round's already-synced `y_id`. The plan's y_id replacement list only named 3 sites (plain yield, verify input, proposed yield), which matches.\n\n**Side effect worth flagging:** running `npx gitnexus analyze` (to un-stale the index before `detect_changes`, per project CLAUDE.md) updated the GitNexus symbol-count banner in `AGENTS.md`/`CLAUDE.md` (14840→16560 symbols etc.) as an uncommitted working-tree diff. I did **not** stage or commit these — they remain as unstaged noise in the working tree, same as the four pre-existing unrelated files. Orchestrator may want to `git checkout -- AGENTS.md CLAUDE.md` or leave it; it's cosmetic only.\n\n**GitNexus impact analysis:** Tasks 1 and 2 came back `risk_level: low`, `affected_count: 0`. Task 3 came back `risk_level: critical` (expected — `mtp_generate_step` is flagged CRITICAL in project policy since many callers depend on it) with `affected_count: 18`, all of which are call-graph paths reaching `mtp_generate_step` through its existing callers (`stream_generate`, `generate`, benchmarks) — none touch its signature, yield contract, or `mtp_stats` key set, which I verified explicitly unchanged (same 13 keys, same increment sites, replay-forward increments confined to the legacy `else` branch only).\n\n**Concerns for the reviewer:**\n- The widened tolerance in `test_rollback_state_equals_prefix_plus_confirmed_only` (deviation #1) is the one place where I made a judgment call about GPU float32 non-determinism rather than a clean pass/fail. Worth a second look if you want to double check the reasoning, though it's isolated to a toy-model test and doesn't touch production tolerances anywhere.\n- I did not touch Task 4/Task 5 (real-model correctness gate and final A/B session) as instructed — those remain for the human-reviewed phase.\n- Branch is left uncommitted-to-main for review, exactly as required.</result>\n<usage><subagent_tokens>164463</subagent_tokens><tool_uses>84</tool_uses><duration_ms>635055</duration_ms></usage>\n</task-notification>"]